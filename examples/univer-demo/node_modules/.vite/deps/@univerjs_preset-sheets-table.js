import {
  RangeSelector
} from "./chunk-EAUNV75L.js";
import {
  SortRangeCommand,
  SortType
} from "./chunk-CXZW3X4B.js";
import {
  AddRangeThemeMutation,
  AscendingIcon,
  Button,
  ButtonGroup,
  CURSOR_TYPE,
  CascaderList,
  Checkbox,
  ColorPicker,
  ComponentManager,
  ContextMenuGroup,
  ContextMenuPosition,
  CopySheetCommand,
  DEFAULT_FONTFACE_PLANE,
  DatePicker,
  DateRangePicker,
  DeleteColumnDoubleIcon,
  DescendingIcon,
  Dropdown,
  DropdownIcon,
  FWorkbook,
  FWorksheet,
  IDefinedNamesService,
  IDialogService,
  IExclusiveRangeService,
  IMenuManagerService,
  INTERCEPTOR_POINT,
  IRenderManagerService,
  ISheetSelectionRenderService,
  ISidebarService,
  Input,
  InputNumber,
  InsertColCommand,
  InsertColMutation,
  InsertRowCommand,
  InsertRowMutation,
  LeftInsertColumnDoubleIcon,
  MenuItemType,
  MoreDownIcon,
  RangeThemeStyle,
  Rect,
  RefRangeService,
  RemoveColCommand,
  RemoveColMutation,
  RemoveRangeThemeMutation,
  RemoveRowCommand,
  RemoveRowMutation,
  RemoveSheetCommand,
  RemoveSuperTableMutation,
  RibbonDataGroup,
  RightInsertColumnDoubleIcon,
  SHEET_VIEW_KEY,
  Segmented,
  Select,
  SelectAllCommand,
  SetRangeThemeMutation,
  SetRangeValuesMutation,
  SetScrollOperation,
  SetSelectionsOperation,
  SetSuperTableMutation,
  SetVerticalTextAlignCommand,
  SetZoomRatioOperation,
  Shape,
  SheetCanvasPopManagerService,
  SheetInterceptorService,
  SheetRangeThemeModel,
  SheetRangeThemeService,
  SheetSkeletonManagerService,
  SheetsSelectionsService,
  TableIcon,
  WorkbookEditablePermission,
  WorkbookPermissionService,
  ZebraCrossingCacheController,
  borderClassName,
  clsx,
  deserializeRangeWithSheet,
  expandToContinuousRange,
  getCoordByCell,
  getCurrentRangeDisable$,
  getMenuHiddenObservable,
  getMoveRangeUndoRedoMutations,
  getPrimaryForRange,
  getSheetCommandTarget,
  getTransformCoord,
  isSingleCellSelection,
  scrollbarClassName,
  serializeRange,
  useDependency,
  useObservable
} from "./chunk-FWGY4U6S.js";
import {
  require_jsx_runtime
} from "./chunk-YG5AX2YU.js";
import {
  FEnum
} from "./chunk-YVJOJ6TY.js";
import "./chunk-MCFHSGOC.js";
import "./chunk-UVXKGGW7.js";
import {
  require_react
} from "./chunk-73THXJN7.js";
import {
  BehaviorSubject,
  BorderStyleTypes,
  CellValueType,
  ColorKit,
  CommandType,
  DependentOn,
  Disposable,
  ErrorService,
  ICommandService,
  IConfigService,
  IContextService,
  ILogService,
  IPermissionService,
  IResourceManagerService,
  IUndoRedoService,
  IUniverInstanceService,
  Inject,
  Injector,
  InterceptorEffectEnum,
  LocaleService,
  ObjectMatrix,
  Plugin,
  RTree,
  Rectangle,
  RxDisposable,
  Subject,
  UniverType,
  VerticalAlign,
  cellToRange,
  createREGEXFromWildChar,
  customNameCharacterCheck,
  distinctUntilChanged,
  filter,
  fromCallback,
  generateRandomId,
  map,
  merge,
  merge_default,
  of,
  registerDependencies,
  sequenceExecute,
  startWith,
  switchMap,
  takeUntil,
  toDisposable,
  touchDependencies
} from "./chunk-X3WWXCVS.js";
import {
  __toESM
} from "./chunk-2TUXWMP5.js";

// node_modules/@univerjs/sheets-table/lib/es/index.js
var TableColumnDataTypeEnum = (function(TableColumnDataTypeEnum2) {
  TableColumnDataTypeEnum2["None"] = "none";
  TableColumnDataTypeEnum2["String"] = "string";
  TableColumnDataTypeEnum2["Number"] = "number";
  TableColumnDataTypeEnum2["Date"] = "date";
  TableColumnDataTypeEnum2["Bool"] = "bool";
  TableColumnDataTypeEnum2["Checkbox"] = "checkbox";
  TableColumnDataTypeEnum2["List"] = "list";
  return TableColumnDataTypeEnum2;
})({});
var TableColumnFilterTypeEnum = (function(TableColumnFilterTypeEnum2) {
  TableColumnFilterTypeEnum2["manual"] = "manual";
  TableColumnFilterTypeEnum2["condition"] = "condition";
  return TableColumnFilterTypeEnum2;
})({});
var TableConditionTypeEnum = (function(TableConditionTypeEnum2) {
  TableConditionTypeEnum2["Date"] = "date";
  TableConditionTypeEnum2["Number"] = "number";
  TableConditionTypeEnum2["String"] = "string";
  TableConditionTypeEnum2["Logic"] = "logic";
  return TableConditionTypeEnum2;
})({});
var TableNumberCompareTypeEnum = (function(TableNumberCompareTypeEnum2) {
  TableNumberCompareTypeEnum2["Equal"] = "equal";
  TableNumberCompareTypeEnum2["NotEqual"] = "notEqual";
  TableNumberCompareTypeEnum2["GreaterThan"] = "greaterThan";
  TableNumberCompareTypeEnum2["GreaterThanOrEqual"] = "greaterThanOrEqual";
  TableNumberCompareTypeEnum2["LessThan"] = "lessThan";
  TableNumberCompareTypeEnum2["LessThanOrEqual"] = "lessThanOrEqual";
  TableNumberCompareTypeEnum2["Between"] = "between";
  TableNumberCompareTypeEnum2["NotBetween"] = "notBetween";
  TableNumberCompareTypeEnum2["Above"] = "above";
  TableNumberCompareTypeEnum2["Below"] = "below";
  TableNumberCompareTypeEnum2["TopN"] = "topN";
  return TableNumberCompareTypeEnum2;
})({});
var TableStringCompareTypeEnum = (function(TableStringCompareTypeEnum2) {
  TableStringCompareTypeEnum2["Equal"] = "equal";
  TableStringCompareTypeEnum2["NotEqual"] = "notEqual";
  TableStringCompareTypeEnum2["Contains"] = "contains";
  TableStringCompareTypeEnum2["NotContains"] = "notContains";
  TableStringCompareTypeEnum2["StartsWith"] = "startsWith";
  TableStringCompareTypeEnum2["EndsWith"] = "endsWith";
  return TableStringCompareTypeEnum2;
})({});
var TableDateCompareTypeEnum = (function(TableDateCompareTypeEnum2) {
  TableDateCompareTypeEnum2["Equal"] = "equal";
  TableDateCompareTypeEnum2["NotEqual"] = "notEqual";
  TableDateCompareTypeEnum2["After"] = "after";
  TableDateCompareTypeEnum2["AfterOrEqual"] = "afterOrEqual";
  TableDateCompareTypeEnum2["Before"] = "before";
  TableDateCompareTypeEnum2["BeforeOrEqual"] = "beforeOrEqual";
  TableDateCompareTypeEnum2["Between"] = "between";
  TableDateCompareTypeEnum2["NotBetween"] = "notBetween";
  TableDateCompareTypeEnum2["Today"] = "today";
  TableDateCompareTypeEnum2["Yesterday"] = "yesterday";
  TableDateCompareTypeEnum2["Tomorrow"] = "tomorrow";
  TableDateCompareTypeEnum2["ThisWeek"] = "thisWeek";
  TableDateCompareTypeEnum2["LastWeek"] = "lastWeek";
  TableDateCompareTypeEnum2["NextWeek"] = "nextWeek";
  TableDateCompareTypeEnum2["ThisMonth"] = "thisMonth";
  TableDateCompareTypeEnum2["LastMonth"] = "lastMonth";
  TableDateCompareTypeEnum2["NextMonth"] = "nextMonth";
  TableDateCompareTypeEnum2["ThisQuarter"] = "thisQuarter";
  TableDateCompareTypeEnum2["LastQuarter"] = "lastQuarter";
  TableDateCompareTypeEnum2["NextQuarter"] = "nextQuarter";
  TableDateCompareTypeEnum2["ThisYear"] = "thisYear";
  TableDateCompareTypeEnum2["LastYear"] = "lastYear";
  TableDateCompareTypeEnum2["NextYear"] = "nextYear";
  TableDateCompareTypeEnum2["YearToDate"] = "yearToDate";
  TableDateCompareTypeEnum2["Quarter"] = "quarter";
  TableDateCompareTypeEnum2["Month"] = "month";
  TableDateCompareTypeEnum2["M1"] = "m1";
  TableDateCompareTypeEnum2["M2"] = "m2";
  TableDateCompareTypeEnum2["M3"] = "m3";
  TableDateCompareTypeEnum2["M4"] = "m4";
  TableDateCompareTypeEnum2["M5"] = "m5";
  TableDateCompareTypeEnum2["M6"] = "m6";
  TableDateCompareTypeEnum2["M7"] = "m7";
  TableDateCompareTypeEnum2["M8"] = "m8";
  TableDateCompareTypeEnum2["M9"] = "m9";
  TableDateCompareTypeEnum2["M10"] = "m10";
  TableDateCompareTypeEnum2["M11"] = "m11";
  TableDateCompareTypeEnum2["M12"] = "m12";
  TableDateCompareTypeEnum2["Q1"] = "q1";
  TableDateCompareTypeEnum2["Q2"] = "q2";
  TableDateCompareTypeEnum2["Q3"] = "q3";
  TableDateCompareTypeEnum2["Q4"] = "q4";
  return TableDateCompareTypeEnum2;
})({});
var SheetsTableButtonStateEnum = (function(SheetsTableButtonStateEnum2) {
  SheetsTableButtonStateEnum2[SheetsTableButtonStateEnum2["FilteredSortNone"] = 1] = "FilteredSortNone";
  SheetsTableButtonStateEnum2[SheetsTableButtonStateEnum2["FilteredSortAsc"] = 2] = "FilteredSortAsc";
  SheetsTableButtonStateEnum2[SheetsTableButtonStateEnum2["FilteredSortDesc"] = 3] = "FilteredSortDesc";
  SheetsTableButtonStateEnum2[SheetsTableButtonStateEnum2["FilterNoneSortNone"] = 4] = "FilterNoneSortNone";
  SheetsTableButtonStateEnum2[SheetsTableButtonStateEnum2["FilterNoneSortAsc"] = 5] = "FilterNoneSortAsc";
  SheetsTableButtonStateEnum2[SheetsTableButtonStateEnum2["FilterNoneSortDesc"] = 6] = "FilterNoneSortDesc";
  return SheetsTableButtonStateEnum2;
})({});
var SheetsTableSortStateEnum = (function(SheetsTableSortStateEnum2) {
  SheetsTableSortStateEnum2["Asc"] = "asc";
  SheetsTableSortStateEnum2["Desc"] = "desc";
  SheetsTableSortStateEnum2["None"] = "none";
  return SheetsTableSortStateEnum2;
})({});
function getColumnName(columnIndex, columnText) {
  return `${columnText} ${columnIndex}`;
}
var BooleanTrue = "TRUE";
var BooleanFalse = "FALSE";
var getStringFromDataStream$1 = (data) => {
  var _data$body;
  return ((_data$body = data.body) === null || _data$body === void 0 ? void 0 : _data$body.dataStream.replace(/\r\n$/, "")) || "";
};
function convertCellDataToString(cellData) {
  if (cellData) {
    const { v, t, p } = cellData;
    if (p) return getStringFromDataStream$1(p);
    if ((t === CellValueType.FORCE_STRING || t === CellValueType.STRING) && v !== void 0 && v !== null) return String(v);
    else if (t === CellValueType.BOOLEAN) return v ? BooleanTrue : BooleanFalse;
    else if (t === CellValueType.NUMBER) return String(v);
    else {
      if (typeof v === "boolean") return v ? BooleanTrue : BooleanFalse;
      return v === void 0 || v === null ? "" : String(v);
    }
  }
  return "";
}
function getTableFilterState(tableFilter, sortState) {
  if (tableFilter !== void 0 && tableFilter !== null) switch (sortState) {
    case "asc":
      return 2;
    case "desc":
      return 3;
    default:
      return 1;
  }
  else switch (sortState) {
    case "asc":
      return 5;
    case "desc":
      return 6;
    default:
      return 4;
  }
}
function isConditionFilter(filter2) {
  if (!filter2) return false;
  return filter2.filterType === "condition";
}
function isManualTableFilter(filter2) {
  if (!filter2) return false;
  return filter2.filterType === "manual";
}
function getExistingNamesSet(unitId, options) {
  const { univerInstanceService, tableManager, definedNamesService } = options;
  const existingNamesSet = /* @__PURE__ */ new Set();
  const workbook = univerInstanceService === null || univerInstanceService === void 0 ? void 0 : univerInstanceService.getUnit(unitId, UniverType.UNIVER_SHEET);
  if (workbook) workbook.getSheets().forEach((sheet) => {
    existingNamesSet.add(sheet.getName().toLowerCase());
  });
  const tableList = tableManager === null || tableManager === void 0 ? void 0 : tableManager.getTableList(unitId);
  if (tableList && tableList.length > 0) tableList.forEach((tableItem) => {
    existingNamesSet.add(tableItem.name.toLowerCase());
  });
  const definedNames = definedNamesService === null || definedNamesService === void 0 ? void 0 : definedNamesService.getDefinedNameMap(unitId);
  if (definedNames) Object.values(definedNames).forEach((definedName) => {
    existingNamesSet.add(definedName.name.toLowerCase());
  });
  return existingNamesSet;
}
var tableDefaultBorderStyle = {
  s: BorderStyleTypes.THIN,
  cl: { rgb: "rgb(95, 101, 116)" }
};
var SHEETS_TABLE_PLUGIN_CONFIG_KEY = "sheets-table.config";
var configSymbol = Symbol(SHEETS_TABLE_PLUGIN_CONFIG_KEY);
var defaultPluginConfig = {};
var customEmptyThemeWithBorderStyle = {
  headerRowStyle: { bd: { t: tableDefaultBorderStyle } },
  headerColumnStyle: { bd: { l: tableDefaultBorderStyle } },
  lastColumnStyle: { bd: { r: tableDefaultBorderStyle } },
  lastRowStyle: { bd: { b: tableDefaultBorderStyle } }
};
var processStyleWithBorderStyle = (key, style) => {
  if (key === "headerRowStyle") {
    if (!style.bd) return {
      ...style,
      bd: { t: tableDefaultBorderStyle }
    };
  } else if (key === "lastRowStyle") {
    if (!style.bd) return {
      ...style,
      bd: { b: tableDefaultBorderStyle }
    };
  } else if (key === "lastColumnStyle") {
    if (!style.bd) return {
      ...style,
      bd: { r: tableDefaultBorderStyle }
    };
  } else if (key === "headerColumnStyle") {
    if (!style.bd) return {
      ...style,
      bd: { l: tableDefaultBorderStyle }
    };
  }
  return style;
};
var tableDefaultThemeStyleArr = [
  [[
    "#6280F9",
    "#FFFFFF",
    "#EEF2FF",
    "#DCE4FF"
  ], ["#fff"]],
  [[
    "#16BDCA",
    "#FFFFFF",
    "#EDFAFA",
    "#AFECEF"
  ], ["#000"]],
  [[
    "#31C48D",
    "#FFFFFF",
    "#F3FAF7",
    "#BCF0DA"
  ], ["#fff"]],
  [[
    "#AC94FA",
    "#FFFFFF",
    "#F6F5FF",
    "#EDEBFE"
  ], ["#fff"]],
  [[
    "#F17EBB",
    "#FFFFFF",
    "#FDF2F8",
    "#FCE8F3"
  ], ["#fff"]],
  [[
    "#F98080",
    "#FFFFFF",
    "#FDF2F2",
    "#FDE8E8"
  ], ["#fff"]]
];
var tableThemeConfig = tableDefaultThemeStyleArr.map((item, index) => {
  const [backgroundArr, colorArr] = item;
  const [headerRowBg, firstRowBg, secondRowBg, lastRowBg] = backgroundArr;
  const [headerCl] = colorArr;
  return {
    name: `table-default-${index}`,
    style: {
      headerRowStyle: {
        bg: { rgb: headerRowBg },
        cl: { rgb: headerCl },
        bd: { t: tableDefaultBorderStyle }
      },
      headerColumnStyle: { bd: { l: tableDefaultBorderStyle } },
      firstRowStyle: { bg: { rgb: firstRowBg } },
      secondRowStyle: { bg: { rgb: secondRowBg } },
      lastRowStyle: {
        bg: { rgb: lastRowBg },
        bd: { b: tableDefaultBorderStyle }
      },
      lastColumnStyle: { bd: { r: tableDefaultBorderStyle } }
    }
  };
});
function _typeof(o) {
  "@babel/helpers - typeof";
  return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o2) {
    return typeof o2;
  } : function(o2) {
    return o2 && "function" == typeof Symbol && o2.constructor === Symbol && o2 !== Symbol.prototype ? "symbol" : typeof o2;
  }, _typeof(o);
}
function toPrimitive(t, r) {
  if ("object" != _typeof(t) || !t) return t;
  var e = t[Symbol.toPrimitive];
  if (void 0 !== e) {
    var i = e.call(t, r || "default");
    if ("object" != _typeof(i)) return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return ("string" === r ? String : Number)(t);
}
function toPropertyKey(t) {
  var i = toPrimitive(t, "string");
  return "symbol" == _typeof(i) ? i : i + "";
}
function _defineProperty(e, r, t) {
  return (r = toPropertyKey(r)) in e ? Object.defineProperty(e, r, {
    value: t,
    enumerable: true,
    configurable: true,
    writable: true
  }) : e[r] = t, e;
}
var TableColumn = class {
  constructor(id, name3) {
    _defineProperty(this, "dataType", void 0);
    _defineProperty(this, "id", void 0);
    _defineProperty(this, "displayName", void 0);
    _defineProperty(this, "formula", void 0);
    _defineProperty(this, "meta", void 0);
    _defineProperty(this, "style", void 0);
    this.id = id;
    this.displayName = name3;
    this.dataType = "string";
    this.formula = "";
    this.meta = {};
    this.style = {};
  }
  getMeta() {
    return this.meta;
  }
  setMeta(meta) {
    this.meta = meta;
  }
  getDisplayName() {
    return this.displayName;
  }
  toJSON() {
    return {
      id: this.id,
      displayName: this.displayName,
      dataType: this.dataType,
      formula: this.formula,
      meta: this.meta,
      style: this.style
    };
  }
  fromJSON(json) {
    this.id = json.id;
    this.displayName = json.displayName;
    this.dataType = json.dataType;
    this.formula = json.formula;
    this.meta = json.meta;
    this.style = json.style;
  }
};
var PLUGIN_NAME = "SHEET_TABLE_PLUGIN";
var FEATURE_TABLE_ID = "SHEET_TABLE";
var SHEET_TABLE_CUSTOM_THEME_PREFIX = "table-custom";
var TABLE_FILTER_EMPTY_VALUE = "__UNIVER_TABLE_FILTER_EMPTY__";
var dateQ1 = (date) => {
  return date.getMonth() <= 2;
};
var dateQ2 = (date) => {
  const month = date.getMonth();
  return month > 2 && month <= 5;
};
var dateQ3 = (date) => {
  const month = date.getMonth();
  return month > 5 && month <= 8;
};
var dateQ4 = (date) => {
  const month = date.getMonth();
  return month > 8 && month <= 11;
};
var dateM1 = (date) => {
  return date.getMonth() === 0;
};
var dateM2 = (date) => {
  return date.getMonth() === 1;
};
var dateM3 = (date) => {
  return date.getMonth() === 2;
};
var dateM4 = (date) => {
  return date.getMonth() === 3;
};
var dateM5 = (date) => {
  return date.getMonth() === 4;
};
var dateM6 = (date) => {
  return date.getMonth() === 5;
};
var dateM7 = (date) => {
  return date.getMonth() === 6;
};
var dateM8 = (date) => {
  return date.getMonth() === 7;
};
var dateM9 = (date) => {
  return date.getMonth() === 8;
};
var dateM10 = (date) => {
  return date.getMonth() === 9;
};
var dateM11 = (date) => {
  return date.getMonth() === 10;
};
var dateM12 = (date) => {
  return date.getMonth() === 11;
};
var today = (expectedDate, anchorTime = /* @__PURE__ */ new Date()) => {
  return expectedDate.toDateString() === anchorTime.toDateString();
};
var tomorrow = (date, anchorTime = /* @__PURE__ */ new Date()) => {
  const tomorrow2 = new Date(anchorTime);
  tomorrow2.setDate(tomorrow2.getDate() + 1);
  return date.toDateString() === tomorrow2.toDateString();
};
var yesterday = (date, anchorTime = /* @__PURE__ */ new Date()) => {
  const yesterday2 = new Date(anchorTime);
  yesterday2.setDate(yesterday2.getDate() - 1);
  return date.toDateString() === yesterday2.toDateString();
};
var getWeekStart = (date) => {
  const day = date.getDay();
  const diff = date.getDate() - day + (day === 0 ? -6 : 1);
  const weekStart = new Date(date);
  weekStart.setDate(diff);
  return weekStart;
};
var perWeek = 10080 * 60 * 1e3;
var thisWeek = (date, anchorTime = /* @__PURE__ */ new Date()) => {
  const weekStart = getWeekStart(date);
  const anchorTimeWeekStart = getWeekStart(anchorTime);
  return weekStart.toDateString() === anchorTimeWeekStart.toDateString();
};
var nextWeek = (date, anchorTime = /* @__PURE__ */ new Date()) => {
  const weekStart = getWeekStart(date);
  const anchorTimeNextWeekStart = new Date(getWeekStart(anchorTime).getTime() + perWeek);
  return weekStart.toDateString() === anchorTimeNextWeekStart.toDateString();
};
var lastWeek = (date, anchorTime = /* @__PURE__ */ new Date()) => {
  const weekStart = getWeekStart(date);
  const anchorTimeLastWeekStart = new Date(getWeekStart(anchorTime).getTime() - perWeek);
  return weekStart.toDateString() === anchorTimeLastWeekStart.toDateString();
};
var thisMonth = (date, anchorTime = /* @__PURE__ */ new Date()) => {
  return date.getFullYear() === anchorTime.getFullYear() && date.getMonth() === anchorTime.getMonth();
};
var getMonthStart = (date) => {
  const monthStart = new Date(date);
  monthStart.setHours(0, 0, 0, 0);
  monthStart.setDate(1);
  return monthStart;
};
var nextMonth = (date, anchorTime = /* @__PURE__ */ new Date()) => {
  const nextMonthStart = new Date(anchorTime);
  nextMonthStart.setHours(0, 0, 0, 0);
  nextMonthStart.setMonth(nextMonthStart.getMonth() + 1, 1);
  const monthEnd = new Date(nextMonthStart);
  monthEnd.setMonth(monthEnd.getMonth() + 1, 0);
  const dateTime = date.getTime();
  return dateTime >= nextMonthStart.getTime() && dateTime < monthEnd.getTime();
};
var lastMonth = (date, anchorTime = /* @__PURE__ */ new Date()) => {
  const lastMonthStart = getMonthStart(anchorTime);
  const monthEnd = new Date(lastMonthStart);
  monthEnd.setMonth(monthEnd.getMonth() + 1, 0);
  const dateTime = date.getTime();
  return dateTime >= lastMonthStart.getTime() && dateTime < monthEnd.getTime();
};
var getQuarterStart = (date) => {
  const quarterStart = new Date(date);
  quarterStart.setHours(0, 0, 0, 0);
  quarterStart.setDate(1);
  const month = quarterStart.getMonth();
  quarterStart.setMonth(month - month % 3);
  return quarterStart;
};
var thisQuarter = (date, anchorTime = /* @__PURE__ */ new Date()) => {
  const quarterStart = getQuarterStart(anchorTime);
  const nextQuarterStart = new Date(quarterStart);
  nextQuarterStart.setMonth(nextQuarterStart.getMonth() + 3);
  const dateTime = date.getTime();
  return dateTime >= quarterStart.getTime() && dateTime < nextQuarterStart.getTime();
};
var nextQuarter = (date, anchorTime = /* @__PURE__ */ new Date()) => {
  const quarterStart = getQuarterStart(anchorTime);
  const nextQuarterStart = new Date(quarterStart);
  nextQuarterStart.setMonth(nextQuarterStart.getMonth() + 3);
  const nextQuarterEnd = new Date(nextQuarterStart);
  nextQuarterEnd.setMonth(nextQuarterEnd.getMonth() + 3, 0);
  const dateTime = date.getTime();
  return dateTime >= nextQuarterStart.getTime() && dateTime < nextQuarterEnd.getTime();
};
var lastQuarter = (date, anchorTime = /* @__PURE__ */ new Date()) => {
  const quarterStart = getQuarterStart(anchorTime);
  const lastQuarterStart = new Date(quarterStart);
  lastQuarterStart.setMonth(lastQuarterStart.getMonth() - 3);
  const lastQuarterEnd = new Date(quarterStart);
  lastQuarterEnd.setDate(0);
  const dateTime = date.getTime();
  return dateTime >= lastQuarterStart.getTime() && dateTime < lastQuarterEnd.getTime();
};
var thisYear = (date, anchorTime = /* @__PURE__ */ new Date()) => {
  return date.getFullYear() === anchorTime.getFullYear();
};
var nextYear = (date, anchorTime = /* @__PURE__ */ new Date()) => {
  return date.getFullYear() === anchorTime.getFullYear() + 1;
};
var lastYear = (date, anchorTime = /* @__PURE__ */ new Date()) => {
  return date.getFullYear() === anchorTime.getFullYear() - 1;
};
var yearToDate = (date, anchorTime = /* @__PURE__ */ new Date()) => {
  const yearStart = new Date(anchorTime);
  yearStart.setHours(0, 0, 0, 0);
  yearStart.setMonth(0, 1);
  const dateTime = date.getTime();
  return dateTime >= yearStart.getTime() && dateTime < anchorTime.getTime();
};
function getDateFilterExecuteFunc(filterInfo) {
  switch (filterInfo.compareType) {
    case "equal": {
      const expected = new Date(filterInfo.expectedValue);
      return (date) => date.getTime() === expected.getTime();
    }
    case "notEqual": {
      const expected = new Date(filterInfo.expectedValue);
      return (date) => date.getTime() !== expected.getTime();
    }
    case "after": {
      const expected = new Date(filterInfo.expectedValue);
      return (date) => date.getTime() > expected.getTime();
    }
    case "before": {
      const expected = new Date(filterInfo.expectedValue);
      return (date) => date.getTime() < expected.getTime();
    }
    case "afterOrEqual": {
      const expected = new Date(filterInfo.expectedValue);
      return (date) => date.getTime() >= expected.getTime();
    }
    case "beforeOrEqual": {
      const expected = new Date(filterInfo.expectedValue);
      return (date) => date.getTime() <= expected.getTime();
    }
    case "between":
      return (date) => {
        const [start, end] = filterInfo.expectedValue;
        return date.getTime() >= new Date(start).getTime() && date.getTime() <= new Date(end).getTime();
      };
    case "notBetween":
      return (date) => {
        const [start, end] = filterInfo.expectedValue;
        return date.getTime() < new Date(start).getTime() || date.getTime() > new Date(end).getTime();
      };
    case "today": {
      const anchorTime = filterInfo.anchorTime ? new Date(filterInfo.anchorTime) : /* @__PURE__ */ new Date();
      return (date) => today(date, anchorTime);
    }
    case "yesterday": {
      const anchorTime = filterInfo.anchorTime ? new Date(filterInfo.anchorTime) : /* @__PURE__ */ new Date();
      return (date) => yesterday(date, anchorTime);
    }
    case "tomorrow": {
      const anchorTime = filterInfo.anchorTime ? new Date(filterInfo.anchorTime) : /* @__PURE__ */ new Date();
      return (date) => tomorrow(date, anchorTime);
    }
    case "thisWeek": {
      const anchorTime = filterInfo.anchorTime ? new Date(filterInfo.anchorTime) : /* @__PURE__ */ new Date();
      return (date) => thisWeek(date, anchorTime);
    }
    case "lastWeek": {
      const anchorTime = filterInfo.anchorTime ? new Date(filterInfo.anchorTime) : /* @__PURE__ */ new Date();
      return (date) => lastWeek(date, anchorTime);
    }
    case "nextWeek": {
      const anchorTime = filterInfo.anchorTime ? new Date(filterInfo.anchorTime) : /* @__PURE__ */ new Date();
      return (date) => nextWeek(date, anchorTime);
    }
    case "thisMonth": {
      const anchorTime = filterInfo.anchorTime ? new Date(filterInfo.anchorTime) : /* @__PURE__ */ new Date();
      return (date) => thisMonth(date, anchorTime);
    }
    case "lastMonth": {
      const anchorTime = filterInfo.anchorTime ? new Date(filterInfo.anchorTime) : /* @__PURE__ */ new Date();
      return (date) => lastMonth(date, anchorTime);
    }
    case "nextMonth": {
      const anchorTime = filterInfo.anchorTime ? new Date(filterInfo.anchorTime) : /* @__PURE__ */ new Date();
      return (date) => nextMonth(date, anchorTime);
    }
    case "thisQuarter": {
      const anchorTime = filterInfo.anchorTime ? new Date(filterInfo.anchorTime) : /* @__PURE__ */ new Date();
      return (date) => thisQuarter(date, anchorTime);
    }
    case "lastQuarter": {
      const anchorTime = filterInfo.anchorTime ? new Date(filterInfo.anchorTime) : /* @__PURE__ */ new Date();
      return (date) => lastQuarter(date, anchorTime);
    }
    case "nextQuarter": {
      const anchorTime = filterInfo.anchorTime ? new Date(filterInfo.anchorTime) : /* @__PURE__ */ new Date();
      return (date) => nextQuarter(date, anchorTime);
    }
    case "thisYear": {
      const anchorTime = filterInfo.anchorTime ? new Date(filterInfo.anchorTime) : /* @__PURE__ */ new Date();
      return (date) => thisYear(date, anchorTime);
    }
    case "lastYear": {
      const anchorTime = filterInfo.anchorTime ? new Date(filterInfo.anchorTime) : /* @__PURE__ */ new Date();
      return (date) => lastYear(date, anchorTime);
    }
    case "nextYear": {
      const anchorTime = filterInfo.anchorTime ? new Date(filterInfo.anchorTime) : /* @__PURE__ */ new Date();
      return (date) => nextYear(date, anchorTime);
    }
    case "yearToDate": {
      const anchorTime = filterInfo.anchorTime ? new Date(filterInfo.anchorTime) : /* @__PURE__ */ new Date();
      return (date) => yearToDate(date, anchorTime);
    }
    case "m1":
      return dateM1;
    case "m2":
      return dateM2;
    case "m3":
      return dateM3;
    case "m4":
      return dateM4;
    case "m5":
      return dateM5;
    case "m6":
      return dateM6;
    case "m7":
      return dateM7;
    case "m8":
      return dateM8;
    case "m9":
      return dateM9;
    case "m10":
      return dateM10;
    case "m11":
      return dateM11;
    case "m12":
      return dateM12;
    case "q1":
      return dateQ1;
    case "q2":
      return dateQ2;
    case "q3":
      return dateQ3;
    case "q4":
      return dateQ4;
    default:
      throw new Error("Unsupported compare type");
  }
}
var Heap = class {
  /**
  * Initializes a new instance of the Heap class.
  */
  constructor() {
    _defineProperty(this, "heap", void 0);
    this.heap = [];
  }
  /**
  * Swaps the elements at the given indices in the heap.
  * @param index1 The index of the first element.
  * @param index2 The index of the second element.
  */
  swap(index1, index2) {
    const temp = this.heap[index1];
    this.heap[index1] = this.heap[index2];
    this.heap[index2] = temp;
  }
  /**
  * Returns the index of the parent node for the given index.
  * @param index The index of the node.
  * @returns The index of the parent node.
  */
  getParentIndex(index) {
    return Math.floor((index - 1) / 2);
  }
  /**
  * Returns the index of the left child node for the given index.
  * @param index The index of the node.
  * @returns The index of the left child node.
  */
  getLeftIndex(index) {
    return index * 2 + 1;
  }
  /**
  * Returns the index of the right child node for the given index.
  * @param index The index of the node.
  * @returns The index of the right child node.
  */
  getRightIndex(index) {
    return index * 2 + 2;
  }
  /**
  * Returns the number of elements in the heap.
  * @returns The number of elements in the heap.
  */
  size() {
    return this.heap.length;
  }
  /**
  * Returns the minimum value in the heap without removing it.
  * @returns The minimum value in the heap.
  */
  peek() {
    return this.heap[0];
  }
  /**
  * @description Returns whether the heap includes the given value.
  * @param {number} value  The value to be checked.
  * @returns {boolean} return true if the heap includes the given value
  */
  include(value) {
    return this.heap.includes(value);
  }
};
var MinHeap = class extends Heap {
  /**
  * Initializes a new instance of the MinHeap class.
  */
  constructor() {
    super();
  }
  /**
  * Moves the element at the given index up the heap until it satisfies the min heap property.
  * @param index The index of the element to be shifted up.
  */
  shiftUp(index) {
    if (index === 0) return;
    const parentIndex = this.getParentIndex(index);
    if (this.heap[parentIndex] > this.heap[index]) {
      this.swap(parentIndex, index);
      this.shiftUp(parentIndex);
    }
  }
  /**
  * Moves the element at the given index down the heap until it satisfies the min heap property.
  * @param index The index of the element to be shifted down.
  */
  shiftDown(index) {
    const leftIndex = this.getLeftIndex(index);
    const rightIndex = this.getRightIndex(index);
    if (this.heap[leftIndex] < this.heap[index]) {
      this.swap(leftIndex, index);
      this.shiftDown(leftIndex);
    }
    if (this.heap[rightIndex] < this.heap[index]) {
      this.swap(rightIndex, index);
      this.shiftDown(rightIndex);
    }
  }
  /**
  * Inserts a new value into the min heap.
  * @param value The value to be inserted.
  */
  insert(value) {
    this.heap.push(value);
    this.shiftUp(this.heap.length - 1);
  }
  /**
  * Removes and returns the minimum value from the min heap.
  */
  pop() {
    this.heap[0] = this.heap.pop();
    this.shiftDown(0);
  }
};
var getLargestK = (list, k) => {
  const minHeap = new MinHeap();
  for (const item of list) {
    minHeap.insert(item);
    if (minHeap.size() > k) minHeap.pop();
  }
  return minHeap.heap;
};
var above = (value, average) => {
  return value > average;
};
var below = (value, average) => {
  return value < average;
};
var getTopN = (list, top, expectedValue) => {
  return getLargestK(list, top).includes(expectedValue);
};
function getNumberFilterExecuteFunc(filter2, calculatedOptions) {
  switch (filter2.compareType) {
    case "equal": {
      const expectedValue = Number(filter2.expectedValue);
      return (value) => value === expectedValue;
    }
    case "notEqual": {
      const expectedValue = Number(filter2.expectedValue);
      return (value) => value !== expectedValue;
    }
    case "greaterThan": {
      const expectedValue = Number(filter2.expectedValue);
      return (value) => value > expectedValue;
    }
    case "greaterThanOrEqual": {
      const expectedValue = Number(filter2.expectedValue);
      return (value) => value >= expectedValue;
    }
    case "lessThan": {
      const expectedValue = Number(filter2.expectedValue);
      return (value) => value < expectedValue;
    }
    case "lessThanOrEqual": {
      const expectedValue = Number(filter2.expectedValue);
      return (value) => value <= expectedValue;
    }
    case "between": {
      const [min, max] = filter2.expectedValue;
      const minValue = Number(min);
      const maxValue = Number(max);
      if (minValue > maxValue) return (value) => value >= maxValue && value <= minValue;
      return (value) => value >= minValue && value <= maxValue;
    }
    case "notBetween": {
      const [min, max] = filter2.expectedValue;
      const minValue = Number(min);
      const maxValue = Number(max);
      if (minValue > maxValue) return (value) => value < maxValue || value > minValue;
      return (value) => value < minValue || value > maxValue;
    }
    case "above": {
      const average = calculatedOptions.average;
      return (value) => above(value, average);
    }
    case "below": {
      const average = calculatedOptions.average;
      return (value) => below(value, average);
    }
    case "topN": {
      const list = calculatedOptions.list;
      const top = Number(filter2.expectedValue);
      return (value) => getTopN(list, top, value);
    }
  }
}
var textEqual = (compareValue, expectedValue) => {
  return createREGEXFromWildChar(expectedValue).test(compareValue);
};
var textNotEqual = (compareValue, expectedValue) => {
  return !createREGEXFromWildChar(expectedValue).test(compareValue);
};
var textContain = (compareValue, expectedValue) => {
  return createREGEXFromWildChar(`*${expectedValue}*`).test(compareValue);
};
var textNotContain = (compareValue, expectedValue) => {
  return !createREGEXFromWildChar(`*${expectedValue}*`).test(compareValue);
};
var textStartWith = (compareValue, expectedValue) => {
  return createREGEXFromWildChar(`${expectedValue}*`).test(compareValue);
};
var textEndWith = (compareValue, expectedValue) => {
  return createREGEXFromWildChar(`*${expectedValue}`).test(compareValue);
};
function getTextFilterExecuteFunc(filter2) {
  switch (filter2.compareType) {
    case "equal":
      return (value) => textEqual(value, filter2.expectedValue);
    case "notEqual":
      return (value) => textNotEqual(value, filter2.expectedValue);
    case "contains":
      return (value) => textContain(value, filter2.expectedValue);
    case "notContains":
      return (value) => textNotContain(value, filter2.expectedValue);
    case "startsWith":
      return (value) => textStartWith(value, filter2.expectedValue);
    case "endsWith":
      return (value) => textEndWith(value, filter2.expectedValue);
    default:
      console.error(`Unknown filter operator: ${filter2.compareType}`);
      return (value) => true;
  }
}
var NumberDynamicFilterCompareTypeSet = /* @__PURE__ */ new Set([
  "above",
  "below",
  "topN"
]);
function isNumberDynamicFilter(compareType) {
  return NumberDynamicFilterCompareTypeSet.has(compareType);
}
function getConditionExecuteFunc(filter2, calculatedOptions) {
  if (isNumberDynamicFilter(filter2.filterInfo.compareType)) return (value) => true;
  else switch (filter2.filterInfo.conditionType) {
    case "date":
      return getDateFilterExecuteFunc(filter2.filterInfo);
    case "number":
      return getNumberFilterExecuteFunc(filter2.filterInfo, calculatedOptions);
    case "string":
      return getTextFilterExecuteFunc(filter2.filterInfo);
    case "logic":
    default:
      return (value) => true;
  }
}
function getCellValueWithConditionType(sheet, row, col, conditionType) {
  switch (conditionType) {
    case "date": {
      const dateNumber = getNumberCellValue(sheet, row, col);
      return dateNumber ? excelSerialToDateTime(dateNumber) : null;
    }
    case "number":
      return getNumberCellValue(sheet, row, col);
    case "string":
    default:
      return getStringCellValue(sheet, row, col);
  }
}
var getStringFromDataStream = (data) => {
  var _data$body;
  return ((_data$body = data.body) === null || _data$body === void 0 ? void 0 : _data$body.dataStream.replace(/\r\n$/, "")) || "";
};
function getStringCellValue(sheet, row, col) {
  const cellData = sheet.getCell(row, col);
  if (!cellData) return null;
  const { v, t, p } = cellData;
  if (p) return getStringFromDataStream(p);
  if (typeof v === "string") {
    if (t === CellValueType.BOOLEAN) return v.toUpperCase();
    return v;
  }
  if (typeof v === "number") {
    if (t === CellValueType.BOOLEAN) return v ? "TRUE" : "FALSE";
    return v;
  }
  if (typeof v === "boolean") return v ? "TRUE" : "FALSE";
  if (v === void 0) return;
  return String(v);
}
function getNumberCellValue(sheet, row, col) {
  const cellData = sheet.getCell(row, col);
  if (!cellData) return null;
  const { v, t, p } = cellData;
  if (p) return null;
  if (typeof v === "string" && t === CellValueType.NUMBER) return Number(sheet.getCellRaw(row, col).v);
  return Number(v);
}
function excelSerialToDateTime(serial) {
  const baseDate = new Date(Date.UTC(1900, 0, 1, 0, 0, 0));
  const leapDayDate = new Date(Date.UTC(1900, 1, 28, 0, 0, 0));
  let dayDifference = serial - 1;
  if (dayDifference > (leapDayDate.getTime() - baseDate.getTime()) / (1e3 * 3600 * 24)) dayDifference -= 1;
  if (dayDifference < 0) dayDifference = serial;
  return new Date(baseDate.getTime() + dayDifference * (1e3 * 3600 * 24));
}
var TableFilters = class {
  constructor() {
    _defineProperty(this, "_tableColumnFilterList", void 0);
    _defineProperty(this, "_tableSortInfo", void 0);
    _defineProperty(this, "_filterOutRows", void 0);
    this._tableColumnFilterList = [];
  }
  setColumnFilter(columnIndex, filter2) {
    if (!filter2) this._tableColumnFilterList[columnIndex] = void 0;
    else this._tableColumnFilterList[columnIndex] = filter2;
  }
  setSortState(columnIndex, sortState) {
    this._tableSortInfo = {
      columnIndex,
      sortState
    };
  }
  getColumnFilter(columnIndex) {
    return this._tableColumnFilterList[columnIndex];
  }
  getFilterState(columnIndex) {
    var _this$_tableSortInfo;
    const sortState = ((_this$_tableSortInfo = this._tableSortInfo) === null || _this$_tableSortInfo === void 0 ? void 0 : _this$_tableSortInfo.columnIndex) === columnIndex ? this._tableSortInfo.sortState : "none";
    return getTableFilterState(this._tableColumnFilterList[columnIndex], sortState);
  }
  getSortState() {
    var _this$_tableSortInfo2;
    return (_this$_tableSortInfo2 = this._tableSortInfo) !== null && _this$_tableSortInfo2 !== void 0 ? _this$_tableSortInfo2 : {};
  }
  getFilterStates(range) {
    const states = [];
    const { startColumn, endColumn } = range;
    for (let i = startColumn; i <= endColumn; i++) states.push(this.getFilterState(i - startColumn));
    return states;
  }
  getFilterOutRows() {
    return this._filterOutRows;
  }
  doFilter(sheet, range) {
    const filterOutRows = /* @__PURE__ */ new Set();
    const tableColumnFilterList = this._tableColumnFilterList;
    for (let i = 0; i < tableColumnFilterList.length; i++) if (tableColumnFilterList[i]) this.doColumnFilter(sheet, range, i, filterOutRows);
    this._filterOutRows = filterOutRows;
    return filterOutRows;
  }
  doColumnFilter(sheet, range, columnIndex, filterOutRows) {
    const filter2 = this._tableColumnFilterList[columnIndex];
    if (filter2 && sheet) {
      const { startRow, endRow, startColumn } = range;
      const column = startColumn + columnIndex;
      const executeFunc = this.getExecuteFunc(sheet, range, columnIndex, filter2);
      for (let row = startRow; row <= endRow; row++) {
        const conditionType = isConditionFilter(filter2) ? filter2.filterInfo.conditionType : "string";
        const cellValue = getCellValueWithConditionType(sheet, row, column, conditionType);
        if (cellValue === null && !executeFunc(cellValue)) filterOutRows.add(row);
        else if (!executeFunc(getCellValueWithConditionType(sheet, row, column, conditionType))) filterOutRows.add(row);
      }
    }
  }
  _getNumberCalculatedOptions(sheet, range, columnIndex) {
    const { startRow, endRow, startColumn } = range;
    const column = startColumn + columnIndex;
    const list = [];
    let count = 0;
    let sum = 0;
    for (let row = startRow; row <= endRow; row++) {
      const val = getCellValueWithConditionType(sheet, row, column, "number");
      if (val !== null) {
        list.push(val);
        count++;
        sum += val;
      }
    }
    return {
      list,
      average: count > 0 ? sum / count : 0
    };
  }
  getExecuteFunc(sheet, range, columnIndex, filter2) {
    if (filter2.filterType === "manual") {
      const valuesSet = new Set(filter2.values);
      return (value) => {
        if (value == null) return valuesSet.has(TABLE_FILTER_EMPTY_VALUE);
        return valuesSet.has(value);
      };
    } else if (filter2.filterType === "condition") return getConditionExecuteFunc(filter2, isNumberDynamicFilter(filter2.filterInfo.compareType) ? this._getNumberCalculatedOptions(sheet, range, columnIndex) : void 0);
    else return (value) => {
      return true;
    };
  }
  toJSON() {
    return {
      tableColumnFilterList: this._tableColumnFilterList,
      tableSortInfo: this._tableSortInfo
    };
  }
  fromJSON(json) {
    var _json$tableColumnFilt;
    this._tableColumnFilterList = (_json$tableColumnFilt = json.tableColumnFilterList) !== null && _json$tableColumnFilt !== void 0 ? _json$tableColumnFilt : [];
    if (json.tableSortInfo) this._tableSortInfo = json.tableSortInfo;
  }
  dispose() {
    this._tableColumnFilterList = [];
  }
};
var Table = class {
  constructor(id, name3, range, header, options = {}) {
    _defineProperty(this, "_id", void 0);
    _defineProperty(this, "_name", void 0);
    _defineProperty(this, "_tableStyleId", void 0);
    _defineProperty(this, "_showHeader", void 0);
    _defineProperty(this, "_showFooter", void 0);
    _defineProperty(this, "_range", void 0);
    _defineProperty(this, "_columns", /* @__PURE__ */ new Map());
    _defineProperty(this, "_columnOrder", []);
    _defineProperty(this, "tableMeta", void 0);
    _defineProperty(this, "_tableFilters", void 0);
    _defineProperty(this, "_subUnitId", void 0);
    this._id = id;
    this._range = range;
    this._name = name3;
    this._tableFilters = new TableFilters();
    this._init(header, options);
  }
  _init(header, options) {
    var _options$showHeader;
    this._tableStyleId = options === null || options === void 0 ? void 0 : options.tableStyleId;
    this._showHeader = (_options$showHeader = options === null || options === void 0 ? void 0 : options.showHeader) !== null && _options$showHeader !== void 0 ? _options$showHeader : true;
    this._showFooter = false;
    const range = this.getRange();
    const startColumn = range.startColumn;
    const endColumn = range.endColumn;
    for (let i = startColumn; i <= endColumn; i++) {
      var _options$columns;
      const index = i - startColumn;
      let id;
      let columnName;
      if ((_options$columns = options.columns) === null || _options$columns === void 0 ? void 0 : _options$columns[index]) {
        id = options.columns[index].id;
        columnName = options.columns[index].displayName;
      } else {
        id = generateRandomId();
        columnName = header[i - startColumn];
      }
      const column = new TableColumn(id, columnName);
      this._columns.set(id, column);
      this._columnOrder.push(id);
    }
    if (options.filters) options.filters.forEach((filter2, index) => {
      if (filter2) this._tableFilters.setColumnFilter(index, filter2);
    });
  }
  setTableFilterColumn(columnIndex, filter2) {
    this._tableFilters.setColumnFilter(columnIndex, filter2);
  }
  getTableFilterColumn(columnIndex) {
    return this._tableFilters.getColumnFilter(columnIndex);
  }
  getTableFilters() {
    return this._tableFilters;
  }
  getTableFilterRange() {
    const tableRange = this.getRange();
    const showHeader = this.isShowHeader();
    const isShowFooter = this.isShowFooter();
    const { startRow, startColumn, endRow, endColumn } = tableRange;
    return {
      startRow: showHeader ? startRow + 1 : startRow,
      startColumn,
      endRow: isShowFooter ? endRow - 1 : endRow,
      endColumn
    };
  }
  setColumns(columns) {
    this._columns.clear();
    this._columnOrder = [];
    columns.forEach((columnJson) => {
      const column = new TableColumn(columnJson.id, columnJson.displayName);
      column.fromJSON(columnJson);
      this._columns.set(columnJson.id, column);
      this._columnOrder.push(columnJson.id);
    });
  }
  getColumnsCount() {
    return this._columnOrder.length;
  }
  insertColumn(index, column) {
    const columnId = column.id;
    this._columns.set(columnId, column);
    this._columnOrder.splice(index, 0, columnId);
  }
  removeColumn(index) {
    const columnId = this._columnOrder[index];
    this._columns.delete(columnId);
    this._columnOrder.splice(index, 1);
  }
  setTableMeta(meta) {
    this.tableMeta = meta;
  }
  getTableMeta() {
    return this.tableMeta;
  }
  getColumn(columnId) {
    return this._columns.get(columnId);
  }
  getTableColumnByIndex(index) {
    const id = this._columnOrder[index];
    return this.getColumn(id);
  }
  getColumnNameByIndex(index) {
    var _this$getColumn;
    const id = this._columnOrder[index];
    return ((_this$getColumn = this.getColumn(id)) === null || _this$getColumn === void 0 ? void 0 : _this$getColumn.getDisplayName()) || "";
  }
  getId() {
    return this._id;
  }
  getRangeInfo() {
    return { ...this._range };
  }
  getRange() {
    return { ...this._range };
  }
  setRange(range) {
    this._range = range;
  }
  setDisplayName(name3) {
    this._name = name3;
  }
  getDisplayName() {
    return this._name;
  }
  getSubunitId() {
    return this._subUnitId;
  }
  setSubunitId(subUnitId) {
    this._subUnitId = subUnitId;
  }
  getTableStyleId() {
    var _this$_tableStyleId;
    return (_this$_tableStyleId = this._tableStyleId) !== null && _this$_tableStyleId !== void 0 ? _this$_tableStyleId : tableThemeConfig[0].name;
  }
  setTableStyleId(tableStyleId) {
    this._tableStyleId = tableStyleId;
  }
  isShowHeader() {
    var _this$_showHeader;
    return (_this$_showHeader = this._showHeader) !== null && _this$_showHeader !== void 0 ? _this$_showHeader : true;
  }
  setShowHeader(showHeader) {
    this._showHeader = showHeader;
  }
  isShowFooter() {
    var _this$_showFooter;
    return (_this$_showFooter = this._showFooter) !== null && _this$_showFooter !== void 0 ? _this$_showFooter : false;
  }
  getTableInfo() {
    return {
      id: this._id,
      subUnitId: this._subUnitId,
      name: this._name,
      range: this.getRangeInfo(),
      meta: this.tableMeta,
      showHeader: this._showHeader,
      columns: this._columnOrder.map((columnId) => this._columns.get(columnId).toJSON())
    };
  }
  getTableConfig() {
    return {
      name: this.getDisplayName(),
      range: this.getRangeInfo(),
      options: {
        showHeader: this._showHeader,
        showFooter: this._showFooter
      },
      tableStyleId: this._tableStyleId
    };
  }
  getFilterStates(range) {
    return this._tableFilters.getFilterStates(range);
  }
  toJSON() {
    const columns = [];
    this._columns.forEach((column) => {
      columns.push(column.toJSON());
    });
    return {
      id: this._id,
      name: this._name,
      range: this.getRangeInfo(),
      options: {
        showHeader: this._showHeader,
        showFooter: this._showFooter,
        tableStyleId: this._tableStyleId
      },
      filters: this._tableFilters.toJSON(),
      columns,
      meta: this.tableMeta
    };
  }
  fromJSON(json) {
    var _json$options$showHea, _json$options$showFoo;
    this._id = json.id;
    this._name = json.name;
    this._range = json.range;
    this.tableMeta = json.meta;
    this._tableStyleId = json.options.tableStyleId || "";
    this._showHeader = (_json$options$showHea = json.options.showHeader) !== null && _json$options$showHea !== void 0 ? _json$options$showHea : true;
    this._showFooter = (_json$options$showFoo = json.options.showFooter) !== null && _json$options$showFoo !== void 0 ? _json$options$showFoo : true;
    json.columns.forEach((column) => {
      const tableColumn = new TableColumn(column.id, column.displayName);
      tableColumn.fromJSON(column);
      this._columns.set(column.id, tableColumn);
      this._columnOrder.push(column.id);
    });
    this._tableFilters = new TableFilters();
    this._tableFilters.fromJSON(json.filters);
  }
  dispose() {
    this._id = "";
    this._name = "";
    this._tableStyleId = "";
    this._showHeader = true;
    this._showFooter = true;
    delete this._range;
    this._columns.clear();
    this._columnOrder = [];
  }
};
function __decorateParam(paramIndex, decorator) {
  return function(target, key) {
    decorator(target, key, paramIndex);
  };
}
function __decorate(decorators, target, key, desc) {
  var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
  if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
  else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
  return c > 3 && r && Object.defineProperty(target, key, r), r;
}
var TableManager = class TableManager2 extends Disposable {
  constructor(_univerInstanceService, _localeService) {
    super();
    this._univerInstanceService = _univerInstanceService;
    this._localeService = _localeService;
    _defineProperty(this, "_tableMap", void 0);
    _defineProperty(this, "_tableAdd$", new Subject());
    _defineProperty(this, "tableAdd$", this._tableAdd$.asObservable());
    _defineProperty(this, "_tableDelete$", new Subject());
    _defineProperty(this, "tableDelete$", this._tableDelete$.asObservable());
    _defineProperty(this, "_tableNameChanged$", new Subject());
    _defineProperty(this, "tableNameChanged$", this._tableNameChanged$.asObservable());
    _defineProperty(this, "_tableRangeChanged$", new Subject());
    _defineProperty(this, "tableRangeChanged$", this._tableRangeChanged$.asObservable());
    _defineProperty(this, "_tableThemeChanged$", new Subject());
    _defineProperty(this, "tableThemeChanged$", this._tableThemeChanged$.asObservable());
    _defineProperty(this, "_tableFilterChanged$", new Subject());
    _defineProperty(this, "tableFilterChanged$", this._tableFilterChanged$.asObservable());
    _defineProperty(this, "_tableInitStatus", new BehaviorSubject(false));
    _defineProperty(this, "tableInitStatus$", this._tableInitStatus.asObservable());
    this._tableMap = /* @__PURE__ */ new Map();
  }
  _ensureUnit(unitId) {
    if (!this._tableMap.has(unitId)) this._tableMap.set(unitId, /* @__PURE__ */ new Map());
    return this._tableMap.get(unitId);
  }
  getColumnHeader(unitId, subUnitId, range, prefixText) {
    var _this$_univerInstance;
    const worksheet = (_this$_univerInstance = this._univerInstanceService.getUnit(unitId)) === null || _this$_univerInstance === void 0 ? void 0 : _this$_univerInstance.getSheetBySheetId(subUnitId);
    const { startRow, startColumn, endColumn } = range;
    const header = [];
    const columnText = prefixText !== null && prefixText !== void 0 ? prefixText : "Column";
    for (let i = startColumn; i <= endColumn; i++) header.push(convertCellDataToString(worksheet === null || worksheet === void 0 ? void 0 : worksheet.getCell(startRow, i)) || getColumnName(i - startColumn + 1, columnText));
    return header;
  }
  /**
  * Add a table to univer.
  * @param {string} unitId The unit id of the table.
  * @param {string} subUnitId The subunit id of the table.
  * @param {string} name The table name, it should be unique in the unit or it will be appended with a number.
  * @param {ITableRange} range The range of the table, it contains the unit id and subunit id.
  * @param {string[]} [header] The header of the table, if not provided, it will be generated based on the range.
  * @param {string} [initId] The initial id of the table, if not provided, a random id will be generated.
  * @param {ITableOptions} [options] Other options of the table.
  * @returns {string} The table id.
  */
  addTable(unitId, subUnitId, name3, range, header, initId, options) {
    const id = initId !== null && initId !== void 0 ? initId : generateRandomId();
    const table = new Table(id, name3, range, header || this.getColumnHeader(unitId, subUnitId, range), options);
    table.setSubunitId(subUnitId);
    this._ensureUnit(unitId).set(id, table);
    this._tableAdd$.next({
      unitId,
      subUnitId,
      range,
      tableName: name3,
      tableId: id,
      tableStyleId: options === null || options === void 0 ? void 0 : options.tableStyleId
    });
    if (options === null || options === void 0 ? void 0 : options.filters) {
      var _this$_univerInstance2;
      const worksheet = (_this$_univerInstance2 = this._univerInstanceService.getUnit(unitId)) === null || _this$_univerInstance2 === void 0 ? void 0 : _this$_univerInstance2.getSheetBySheetId(subUnitId);
      table.getTableFilters().doFilter(worksheet, range);
      this._tableFilterChanged$.next({
        unitId,
        subUnitId,
        tableId: id
      });
    }
    return id;
  }
  addFilter(unitId, tableId, column, filter2) {
    const table = this.getTable(unitId, tableId);
    if (table) {
      table.getTableFilters().setColumnFilter(column, filter2);
      const subUnitId = table.getSubunitId();
      this._tableFilterChanged$.next({
        unitId,
        subUnitId,
        tableId
      });
    }
  }
  getFilterRanges(unitId, subUnitId) {
    const unitMap = this._tableMap.get(unitId);
    if (!unitMap) return [];
    const filterRanges = [];
    unitMap.forEach((table) => {
      if (table.getSubunitId() === subUnitId && table.isShowHeader()) filterRanges.push(table.getRange());
    });
    return filterRanges;
  }
  getSheetFilterRangeWithState(unitId, subUnitId) {
    const unitMap = this._tableMap.get(unitId);
    if (!unitMap) return [];
    const filterRanges = [];
    unitMap.forEach((table) => {
      if (table.getSubunitId() === subUnitId && table.isShowHeader()) filterRanges.push({
        tableId: table.getId(),
        range: table.getRange(),
        states: table.getFilterStates(table.getRange())
      });
    });
    return filterRanges;
  }
  getTable(unitId, tableId) {
    const unitMap = this._tableMap.get(unitId);
    if (!unitMap) return;
    return unitMap.get(tableId);
  }
  /**
  * Get the unique table name, in excel, the table name should be unique because it is used as a reference.
  * @param {string} unitId The unit id of the table.
  * @param {string} baseName The base name of the table.
  * @returns {string} The unique table name
  */
  getUniqueTableName(unitId, baseName) {
    const unitMap = this._tableMap.get(unitId);
    if (!unitMap) return baseName;
    const tableNamesSet = new Set(Array.from(unitMap.values()).map((table) => table.getDisplayName()));
    let newName = baseName;
    let count = 1;
    while (tableNamesSet.has(newName)) {
      newName = `${baseName}-${count}`;
      count++;
    }
    return newName;
  }
  /**
  * Get table by unit id and table id.
  * @param {string} unitId  The unit id of the table.
  * @param {string} tableId The table id.
  * @returns {Table} The table.
  */
  getTableById(unitId, tableId) {
    return this.getTable(unitId, tableId);
  }
  getTableList(unitId) {
    const unitMap = this._tableMap.get(unitId);
    if (!unitMap) return [];
    return Array.from(unitMap.values()).map((table) => {
      return {
        ...table.getTableInfo(),
        unitId
      };
    });
  }
  /**
  * Get the table list by unit id and subunit id.
  * @param {string} unitId The unit id of the table.
  * @param {string} subUnitId The subunit id of the table.
  * @returns {Table[]} The table list.
  */
  getTablesBySubunitId(unitId, subUnitId) {
    const unitMap = this._tableMap.get(unitId);
    if (!unitMap) return [];
    return Array.from(unitMap.values()).filter((table) => table.getSubunitId() === subUnitId);
  }
  getTablesInfoBySubunitId(unitId, subUnitId) {
    return this.getTablesBySubunitId(unitId, subUnitId).map((table) => {
      return {
        id: table.getId(),
        name: table.getDisplayName(),
        range: table.getRange()
      };
    });
  }
  deleteTable(unitId, tableId) {
    const unitMap = this._tableMap.get(unitId);
    const table = unitMap === null || unitMap === void 0 ? void 0 : unitMap.get(tableId);
    if (table) {
      const tableInfo = table.getTableInfo();
      const tableStyleId = table.getTableStyleId();
      unitMap === null || unitMap === void 0 || unitMap.delete(tableId);
      const { subUnitId, range, name: name3 } = tableInfo;
      this._tableDelete$.next({
        unitId,
        subUnitId,
        tableId,
        range,
        tableName: name3,
        tableStyleId
      });
    }
  }
  operationTableRowCol(unitId, tableId, config) {
    const table = this.getTableById(unitId, tableId);
    if (!table) return;
    const { operationType, rowColType, index, count, columnsJson } = config;
    const oldRange = table.getRange();
    const newRange = { ...oldRange };
    if (operationType === "insert") {
      if (rowColType === "row") newRange.endRow += count;
      else if (rowColType === "column") {
        newRange.endColumn += count;
        for (let i = 0; i < count; i++) {
          const columnPrefix = this._localeService.t("sheets-table.columnPrefix");
          const column = new TableColumn(generateRandomId(), getColumnName(table.getColumnsCount() + 1 + i, columnPrefix));
          if (columnsJson === null || columnsJson === void 0 ? void 0 : columnsJson[i]) column.fromJSON(columnsJson[i]);
          const columnIndex = index + i - oldRange.startColumn;
          table.insertColumn(columnIndex, column);
        }
      }
    } else if (rowColType === "row") newRange.endRow -= count;
    else if (rowColType === "column") {
      newRange.endColumn -= count;
      for (let i = count - 1; i >= 0; i--) {
        const columnIndex = index + i - oldRange.startColumn;
        table.removeColumn(columnIndex);
      }
    }
    table.setRange(newRange);
    this._tableRangeChanged$.next({
      unitId,
      subUnitId: table.getSubunitId(),
      tableId,
      range: newRange,
      oldRange
    });
  }
  updateTableRange(unitId, tableId, config) {
    const table = this.getTableById(unitId, tableId);
    if (!table) return;
    const oldRange = table.getRange();
    const newRange = config.newRange;
    if (newRange.startColumn < oldRange.startColumn) {
      const diff = oldRange.startColumn - newRange.startColumn;
      const columnPrefix = this._localeService.t("sheets-table.columnPrefix");
      for (let i = 0; i < diff; i++) table.insertColumn(oldRange.startColumn, new TableColumn(generateRandomId(), getColumnName(table.getColumnsCount() + 1, columnPrefix)));
    } else if (newRange.startColumn > oldRange.startColumn) {
      const diff = newRange.startColumn - oldRange.startColumn;
      for (let i = diff - 1; i >= 0; i--) {
        const columnIndex = newRange.startColumn + i - oldRange.startColumn;
        table.removeColumn(columnIndex);
      }
    }
    if (newRange.endColumn < oldRange.endColumn) {
      const diff = oldRange.endColumn - newRange.endColumn;
      for (let i = diff - 1; i >= 0; i--) {
        const columnIndex = newRange.endColumn + i - oldRange.startColumn;
        table.removeColumn(columnIndex);
      }
    } else if (newRange.endColumn > oldRange.endColumn) {
      const diff = newRange.endColumn - oldRange.endColumn;
      const columnPrefix = this._localeService.t("sheets-table.columnPrefix");
      for (let i = 0; i < diff; i++) table.insertColumn(oldRange.endColumn, new TableColumn(generateRandomId(), getColumnName(table.getColumnsCount() + 1, columnPrefix)));
    }
    table.setRange(newRange);
    this._tableRangeChanged$.next({
      unitId,
      subUnitId: table.getSubunitId(),
      tableId,
      range: newRange,
      oldRange
    });
  }
  setTableByConfig(unitId, tableId, config) {
    const unitMap = this._tableMap.get(unitId);
    const table = unitMap === null || unitMap === void 0 ? void 0 : unitMap.get(tableId);
    if (!table) return;
    const subUnitId = table.getSubunitId();
    const { name: name3, updateRange, rowColOperation, theme, options } = config;
    if (name3) {
      const oldTableName = table.getDisplayName();
      table.setDisplayName(name3);
      this._tableNameChanged$.next({
        unitId,
        subUnitId,
        tableId,
        tableName: name3,
        oldTableName
      });
    }
    if (rowColOperation) this.operationTableRowCol(unitId, tableId, rowColOperation);
    if (updateRange) this.updateTableRange(unitId, tableId, updateRange);
    if (theme) {
      var _table$getTableStyleI;
      const oldTheme = (_table$getTableStyleI = table.getTableStyleId()) !== null && _table$getTableStyleI !== void 0 ? _table$getTableStyleI : "default";
      table.setTableStyleId(theme);
      this._tableThemeChanged$.next({
        unitId,
        subUnitId,
        tableId,
        theme,
        oldTheme
      });
    }
    if (options) {
      if (options.showHeader !== void 0) table.setShowHeader(options.showHeader);
    }
  }
  toJSON(unitId) {
    const result = {};
    const unitMap = this._tableMap.get(unitId);
    if (unitMap) unitMap.forEach((table) => {
      const subUnitId = table.getSubunitId();
      if (!result[subUnitId]) {
        const tableFilteredOutRows = /* @__PURE__ */ new Set();
        this.getTablesBySubunitId(unitId, subUnitId).forEach((table2) => {
          const tableFilteredRows = table2.getTableFilters().getFilterOutRows();
          if (!tableFilteredRows) return;
          for (const row of tableFilteredRows) tableFilteredOutRows.add(row);
        });
        result[subUnitId] = {
          tables: [],
          tableFilteredOutRows: Array.from(tableFilteredOutRows)
        };
      }
      result[subUnitId].tables.push(table.toJSON());
    });
    return result;
  }
  fromJSON(unitId, data) {
    const unitMap = this._ensureUnit(unitId);
    Object.keys(data).forEach((subUnitId) => {
      const target = getSheetCommandTarget(this._univerInstanceService, {
        unitId,
        subUnitId
      });
      if (!target) return;
      const sheet = target.worksheet;
      let tables;
      if (data[subUnitId].tables) tables = data[subUnitId].tables;
      else if (Array.isArray(data[subUnitId])) tables = data[subUnitId];
      if (!tables) return;
      tables.forEach((table) => {
        const header = this.getColumnHeader(unitId, subUnitId, table.range);
        const tableInstance = new Table(table.id, table.name, table.range, header, table.options);
        tableInstance.setTableMeta(table.meta);
        if (table.columns.length) tableInstance.setColumns(table.columns);
        if (table.filters) {
          const tableFilter = tableInstance.getTableFilters();
          tableFilter.fromJSON(table.filters);
          tableFilter.doFilter(sheet, tableInstance.getTableFilterRange());
        }
        tableInstance.setSubunitId(subUnitId);
        unitMap.set(table.id, tableInstance);
        this._tableAdd$.next({
          unitId,
          subUnitId,
          range: table.range,
          tableName: table.name,
          tableId: table.id
        });
      });
    });
    this._tableInitStatus.next(true);
  }
  deleteUnitId(unitId) {
    const unitMap = this._tableMap.get(unitId);
    if (unitMap) unitMap.forEach((table) => table.dispose());
    this._tableMap.delete(unitId);
  }
  dispose() {
    super.dispose();
    this._tableAdd$.complete();
    this._tableDelete$.complete();
    this._tableNameChanged$.complete();
    this._tableRangeChanged$.complete();
    this._tableThemeChanged$.complete();
    this._tableFilterChanged$.complete();
    this._tableInitStatus.complete();
    this._tableMap.forEach((unitMap) => {
      unitMap.forEach((table) => table.dispose());
      unitMap.clear();
    });
    this._tableMap.clear();
  }
};
TableManager = __decorate([__decorateParam(0, IUniverInstanceService), __decorateParam(1, Inject(LocaleService))], TableManager);
var SheetTableService = class SheetTableService2 extends Disposable {
  constructor(_tableManager) {
    super();
    this._tableManager = _tableManager;
  }
  getTableInfo(unitId, tableId) {
    const table = this._tableManager.getTable(unitId, tableId);
    if (!table) return;
    return {
      unitId,
      ...table.getTableInfo()
    };
  }
  getTableList(unitId) {
    return this._tableManager.getTableList(unitId);
  }
  addTable(unitId, subUnitId, tableName, rangeInfo, tableHeader, tableId, options) {
    return this._tableManager.addTable(unitId, subUnitId, tableName, rangeInfo, tableHeader, tableId, options);
  }
  deleteTable(unitId, subUnitId, tableId) {
    this._tableManager.deleteTable(unitId, tableId);
  }
  getTableMeta(unitId, tableId) {
    var _this$_tableManager$g;
    return (_this$_tableManager$g = this._tableManager.getTable(unitId, tableId)) === null || _this$_tableManager$g === void 0 ? void 0 : _this$_tableManager$g.getTableMeta();
  }
  setTableMeta(unitId, tableId, meta) {
    var _this$_tableManager$g2;
    (_this$_tableManager$g2 = this._tableManager.getTable(unitId, tableId)) === null || _this$_tableManager$g2 === void 0 || _this$_tableManager$g2.setTableMeta(meta);
  }
  getTableColumnMeta(unitId, tableId, index) {
    var _this$_tableManager$g3;
    return (_this$_tableManager$g3 = this._tableManager.getTable(unitId, tableId)) === null || _this$_tableManager$g3 === void 0 || (_this$_tableManager$g3 = _this$_tableManager$g3.getTableColumnByIndex(index)) === null || _this$_tableManager$g3 === void 0 ? void 0 : _this$_tableManager$g3.getMeta();
  }
  selTableColumnMeta(unitId, tableId, index, meta) {
    var _this$_tableManager$g4;
    (_this$_tableManager$g4 = this._tableManager.getTable(unitId, tableId)) === null || _this$_tableManager$g4 === void 0 || (_this$_tableManager$g4 = _this$_tableManager$g4.getTableColumnByIndex(index)) === null || _this$_tableManager$g4 === void 0 || _this$_tableManager$g4.setMeta(meta);
  }
  addFilter(unitId, tableId, column, filter2) {
    this._tableManager.addFilter(unitId, tableId, column, filter2);
  }
  getCellValueWithConditionType(sheet, row, col, conditionType = "string") {
    return getCellValueWithConditionType(sheet, row, col, conditionType);
  }
};
SheetTableService = __decorate([__decorateParam(0, Inject(TableManager))], SheetTableService);
var AddSheetTableMutation = {
  id: "sheet.mutation.add-table",
  type: CommandType.MUTATION,
  handler: (accessor, params) => {
    const { tableId, unitId, subUnitId, name: name3, range, header, options } = params;
    accessor.get(SheetTableService).addTable(unitId, subUnitId, name3, range, header, tableId, options);
    return true;
  }
};
var DeleteSheetTableMutation = {
  id: "sheet.mutation.delete-table",
  type: CommandType.MUTATION,
  handler: (accessor, params) => {
    const { unitId, subUnitId, tableId } = params;
    accessor.get(SheetTableService).deleteTable(unitId, subUnitId, tableId);
    return true;
  }
};
var AddSheetTableCommand = {
  id: "sheet.command.add-table",
  type: CommandType.COMMAND,
  handler: (accessor, params) => {
    var _params$id;
    if (!params) return false;
    const undoRedoService = accessor.get(IUndoRedoService);
    const commandService = accessor.get(ICommandService);
    const localeService = accessor.get(LocaleService);
    const tableManager = accessor.get(TableManager);
    const tableId = (_params$id = params.id) !== null && _params$id !== void 0 ? _params$id : generateRandomId();
    const existingNamesSet = getExistingNamesSet(params.unitId, {
      univerInstanceService: accessor.get(IUniverInstanceService),
      tableManager,
      definedNamesService: accessor.get(IDefinedNamesService)
    });
    let tableName = params.name;
    if (!tableName || !customNameCharacterCheck(tableName.toLowerCase(), existingNamesSet)) {
      const prefix = localeService.t("sheets-table.tablePrefix");
      let index = tableManager.getTableList(params.unitId).length + 1;
      for (const name3 of existingNamesSet) if (name3.startsWith(prefix.toLowerCase())) {
        const n = Number(name3.slice(prefix.length));
        if (Number.isInteger(n) && n >= index) index = n + 1;
      }
      tableName = `${prefix}${index}`;
    }
    const redos = [];
    const undos = [];
    const { unitId, subUnitId, range } = params;
    const header = tableManager.getColumnHeader(unitId, subUnitId, range, localeService.t("sheets-table.columnPrefix"));
    redos.push({
      id: AddSheetTableMutation.id,
      params: {
        ...params,
        tableId,
        name: tableName,
        header
      }
    });
    undos.push({
      id: DeleteSheetTableMutation.id,
      params: {
        tableId,
        unitId: params.unitId
      }
    });
    if (sequenceExecute(redos, commandService).result) {
      undoRedoService.pushUndoRedo({
        unitID: params.unitId,
        undoMutations: undos,
        redoMutations: redos
      });
      return true;
    }
    return false;
  }
};
var SetSheetTableMutation = {
  id: "sheet.mutation.set-sheet-table",
  type: CommandType.MUTATION,
  handler: (accessor, params) => {
    if (!params) return false;
    const { unitId, tableId, config } = params;
    accessor.get(TableManager).setTableByConfig(unitId, tableId, config);
    return true;
  }
};
var AddTableThemeCommand = {
  id: "sheet.command.add-table-theme",
  type: CommandType.COMMAND,
  handler: (accessor, params) => {
    if (!params) return false;
    const tableManager = accessor.get(TableManager);
    const { unitId, tableId, themeStyle } = params;
    const redos = [];
    const undos = [];
    const table = tableManager.getTableById(unitId, tableId);
    if (!table) return false;
    const subUnitId = table.getSubunitId();
    redos.push({
      id: AddRangeThemeMutation.id,
      params: {
        unitId,
        subUnitId,
        styleJSON: themeStyle.toJson()
      }
    });
    redos.push({
      id: SetSheetTableMutation.id,
      params: {
        unitId,
        subUnitId,
        tableId,
        config: { theme: themeStyle.getName() }
      }
    });
    undos.push({
      id: SetSheetTableMutation.id,
      params: {
        unitId,
        subUnitId,
        tableId,
        config: { themeStyle: table.getTableStyleId() }
      }
    });
    undos.push({
      id: RemoveRangeThemeMutation.id,
      params: {
        unitId,
        subUnitId,
        styleName: themeStyle.getName()
      }
    });
    if (sequenceExecute(redos, accessor.get(ICommandService)).result) {
      accessor.get(IUndoRedoService).pushUndoRedo({
        unitID: unitId,
        undoMutations: undos,
        redoMutations: redos
      });
      return true;
    }
    return false;
  }
};
var DeleteSheetTableCommand = {
  id: "sheet.command.delete-table",
  type: CommandType.COMMAND,
  handler: (accessor, params) => {
    var _interceptorCommands$, _interceptorCommands$2;
    if (!params) return false;
    const undoRedoService = accessor.get(IUndoRedoService);
    const commandService = accessor.get(ICommandService);
    const sheetTableManager = accessor.get(TableManager);
    const logService = accessor.get(ILogService);
    const redos = [];
    const undos = [];
    const tableInstance = sheetTableManager.getTable(params.unitId, params.tableId);
    const tableConfig = tableInstance === null || tableInstance === void 0 ? void 0 : tableInstance.toJSON();
    if (!tableConfig) {
      logService.error("[TableManager]: Table not found");
      return false;
    }
    const interceptorCommands = accessor.get(SheetInterceptorService).onCommandExecute({
      id: DeleteSheetTableCommand.id,
      params: {
        ...params,
        tableName: tableConfig.name
      }
    });
    redos.push(...(_interceptorCommands$ = interceptorCommands.preRedos) !== null && _interceptorCommands$ !== void 0 ? _interceptorCommands$ : []);
    redos.push({
      id: DeleteSheetTableMutation.id,
      params: { ...params }
    });
    redos.push(...interceptorCommands.redos);
    undos.push(...(_interceptorCommands$2 = interceptorCommands.preUndos) !== null && _interceptorCommands$2 !== void 0 ? _interceptorCommands$2 : []);
    undos.push({
      id: AddSheetTableMutation.id,
      params: {
        unitId: params.unitId,
        subUnitId: params.subUnitId,
        tableId: params.tableId,
        name: tableConfig.name,
        range: tableConfig.range,
        options: tableConfig.options
      }
    });
    undos.push(...interceptorCommands.undos);
    if (sequenceExecute(redos, commandService).result) {
      undoRedoService.pushUndoRedo({
        unitID: params.unitId,
        undoMutations: undos,
        redoMutations: redos
      });
      return true;
    }
    return false;
  }
};
var RemoveTableThemeCommand = {
  id: "sheet.command.remove-table-theme",
  type: CommandType.COMMAND,
  handler: (accessor, params) => {
    if (!params) return false;
    const { unitId, tableId, themeName } = params;
    const tableManager = accessor.get(TableManager);
    const rangeThemeModel = accessor.get(SheetRangeThemeModel);
    const table = tableManager.getTableById(unitId, tableId);
    if (!table) return false;
    const subUnitId = table.getSubunitId();
    const redos = [];
    const undos = [];
    const defaultRangeThemes = rangeThemeModel.getRegisteredRangeThemes().filter((item) => item === null || item === void 0 ? void 0 : item.startsWith("table-default"));
    let shouldBeSelectedTheme = rangeThemeModel.getRegisteredRangeThemes().filter((item) => item === null || item === void 0 ? void 0 : item.startsWith(SHEET_TABLE_CUSTOM_THEME_PREFIX)).find((item) => item !== themeName);
    if (!shouldBeSelectedTheme) shouldBeSelectedTheme = defaultRangeThemes[0];
    redos.push({
      id: SetSheetTableMutation.id,
      params: {
        unitId,
        subUnitId,
        tableId,
        config: { theme: shouldBeSelectedTheme }
      }
    });
    redos.push({
      id: RemoveRangeThemeMutation.id,
      params: {
        unitId,
        subUnitId,
        styleName: themeName
      }
    });
    const themeStyle = rangeThemeModel.getDefaultRangeThemeStyle(themeName);
    if (themeStyle) {
      undos.push({
        id: AddRangeThemeMutation.id,
        params: {
          unitId,
          subUnitId,
          styleJSON: themeStyle.toJson()
        }
      });
      undos.push({
        id: SetSheetTableMutation.id,
        params: {
          unitId,
          subUnitId,
          tableId,
          config: { theme: themeName }
        }
      });
    }
    if (sequenceExecute(redos, accessor.get(ICommandService)).result) {
      accessor.get(IUndoRedoService).pushUndoRedo({
        unitID: unitId,
        redoMutations: redos,
        undoMutations: undos
      });
      return true;
    }
    return false;
  }
};
function validateSheetTableName(name3, existingNamesSet) {
  const trimmedName = name3.trim();
  if (!trimmedName) return {
    valid: false,
    reason: "empty"
  };
  const normalizedExistingNames = new Set(Array.from(existingNamesSet, (item) => item.toLowerCase()));
  if (!customNameCharacterCheck(trimmedName.toLowerCase(), normalizedExistingNames)) return {
    valid: false,
    reason: "invalid"
  };
  return { valid: true };
}
var SetSheetTableCommand = {
  id: "sheet.command.set-table-config",
  type: CommandType.COMMAND,
  handler: (accessor, params) => {
    var _interceptorCommands$, _interceptorCommands$2;
    if (!params) return false;
    const { unitId, tableId, name: name3, updateRange, rowColOperation, theme } = params;
    const tableManager = accessor.get(TableManager);
    const table = tableManager.getTableById(unitId, tableId);
    if (!table) return false;
    const oldTableConfig = {};
    const newTableConfig = {};
    const localeService = accessor.get(LocaleService);
    const existingNamesSet = getExistingNamesSet(unitId, {
      univerInstanceService: accessor.get(IUniverInstanceService),
      tableManager,
      definedNamesService: accessor.get(IDefinedNamesService)
    });
    if (name3) {
      if (!validateSheetTableName(name3, existingNamesSet).valid) {
        accessor.get(ILogService).warn(localeService.t("sheets-table.tableNameError"));
        return false;
      }
      oldTableConfig.name = table.getDisplayName();
      newTableConfig.name = name3;
    }
    if (rowColOperation) {
      oldTableConfig.rowColOperation = {
        operationType: rowColOperation.operationType === "insert" ? "delete" : "insert",
        rowColType: rowColOperation.rowColType,
        index: rowColOperation.index,
        count: rowColOperation.count
      };
      newTableConfig.rowColOperation = rowColOperation;
    }
    if (updateRange) {
      oldTableConfig.updateRange = { newRange: table.getRange() };
      newTableConfig.updateRange = updateRange;
    }
    if (theme) {
      oldTableConfig.theme = table.getTableStyleId();
      newTableConfig.theme = theme;
    }
    const redoParams = {
      unitId,
      subUnitId: table.getSubunitId(),
      tableId,
      config: newTableConfig
    };
    const undoParams = {
      unitId,
      subUnitId: table.getSubunitId(),
      tableId,
      config: oldTableConfig
    };
    const interceptorCommands = accessor.get(SheetInterceptorService).onCommandExecute({
      id: SetSheetTableCommand.id,
      params: {
        ...params,
        oldTableName: oldTableConfig.name
      }
    });
    const redos = [
      ...(_interceptorCommands$ = interceptorCommands.preRedos) !== null && _interceptorCommands$ !== void 0 ? _interceptorCommands$ : [],
      {
        id: SetSheetTableMutation.id,
        params: redoParams
      },
      ...interceptorCommands.redos
    ];
    const undos = [
      ...(_interceptorCommands$2 = interceptorCommands.preUndos) !== null && _interceptorCommands$2 !== void 0 ? _interceptorCommands$2 : [],
      {
        id: SetSheetTableMutation.id,
        params: undoParams
      },
      ...interceptorCommands.undos
    ];
    const commandService = accessor.get(ICommandService);
    redos.forEach((mutation) => {
      commandService.executeCommand(mutation.id, mutation.params);
    });
    accessor.get(IUndoRedoService).pushUndoRedo({
      unitID: unitId,
      undoMutations: undos,
      redoMutations: redos
    });
    return true;
  }
};
var SetSheetTableFilterMutation = {
  id: "sheet.mutation.set-table-filter",
  type: CommandType.MUTATION,
  handler: (accessor, params) => {
    const { tableId, unitId, column, tableFilter } = params;
    accessor.get(TableManager).addFilter(unitId, tableId, column, tableFilter);
    return true;
  }
};
var SetSheetTableFilterCommand = {
  id: "sheet.command.set-table-filter",
  type: CommandType.COMMAND,
  handler: (accessor, params) => {
    if (!params) return false;
    const undoRedoService = accessor.get(IUndoRedoService);
    const commandService = accessor.get(ICommandService);
    const tableId = params.tableId || generateRandomId();
    const redos = [];
    const undos = [];
    redos.push({
      id: SetSheetTableFilterMutation.id,
      params: {
        ...params,
        tableId
      }
    });
    undos.push({
      id: SetSheetTableFilterMutation.id,
      params: {
        ...params,
        tableId,
        tableFilter: void 0
      }
    });
    if (sequenceExecute(redos, commandService).result) {
      undoRedoService.pushUndoRedo({
        unitID: params.unitId,
        undoMutations: undos,
        redoMutations: redos
      });
      return true;
    }
    return false;
  }
};
var SheetsTableController = class SheetsTableController2 extends Disposable {
  constructor(_univerInstanceService, _sheetInterceptorService, _tableManager, _resourceManagerService) {
    super();
    this._univerInstanceService = _univerInstanceService;
    this._sheetInterceptorService = _sheetInterceptorService;
    this._tableManager = _tableManager;
    this._resourceManagerService = _resourceManagerService;
    _defineProperty(this, "_tableRangeRTree", /* @__PURE__ */ new Map());
    this._initSnapshot();
    this._initSheetChange();
    this.registerTableChangeEvent();
    this.registerTableHeaderInterceptor();
  }
  getContainerTableWithRange(unitId, subUnitId, range) {
    const rTree = this._ensureTableRangeRTree(unitId);
    const wrapperTableId = Array.from(rTree.bulkSearch([{
      unitId,
      sheetId: subUnitId,
      range
    }])).find((id) => {
      const table = this._tableManager.getTable(unitId, String(id));
      if (table) return Rectangle.contains(table.getRange(), range);
      return false;
    });
    if (wrapperTableId) return this._tableManager.getTable(unitId, String(wrapperTableId));
  }
  _ensureTableRangeRTree(unitId) {
    if (!this._tableRangeRTree.has(unitId)) this._tableRangeRTree.set(unitId, new RTree());
    return this._tableRangeRTree.get(unitId);
  }
  registerTableChangeEvent() {
    this.disposeWithMe(this._tableManager.tableAdd$.subscribe((event) => {
      const { range, tableId, unitId, subUnitId } = event;
      this._ensureTableRangeRTree(unitId).insert({
        unitId,
        sheetId: subUnitId,
        id: tableId,
        range: { ...range }
      });
    }));
    this.disposeWithMe(this._tableManager.tableRangeChanged$.subscribe((event) => {
      const { range, tableId, unitId, subUnitId, oldRange } = event;
      const rTree = this._ensureTableRangeRTree(unitId);
      rTree.remove({
        unitId,
        sheetId: subUnitId,
        id: tableId,
        range: { ...oldRange }
      });
      rTree.insert({
        unitId,
        sheetId: subUnitId,
        id: tableId,
        range: { ...range }
      });
    }));
    this.disposeWithMe(this._tableManager.tableDelete$.subscribe((event) => {
      const { tableId, unitId, subUnitId, range } = event;
      this._ensureTableRangeRTree(unitId).remove({
        unitId,
        sheetId: subUnitId,
        id: tableId,
        range: { ...range }
      });
    }));
  }
  registerTableHeaderInterceptor() {
    this.disposeWithMe(this._sheetInterceptorService.intercept(INTERCEPTOR_POINT.CELL_CONTENT, {
      effect: InterceptorEffectEnum.Value,
      handler: (cell, context, next) => {
        const { row, col, unitId, subUnitId } = context;
        const rTree = this._ensureTableRangeRTree(unitId);
        if ((cell === null || cell === void 0 ? void 0 : cell.v) === void 0 && rTree) {
          const ids = Array.from(rTree.bulkSearch([{
            unitId,
            sheetId: subUnitId,
            range: {
              startColumn: col,
              endColumn: col,
              startRow: row,
              endRow: row
            }
          }]));
          if (ids.length > 0) {
            const table = this._tableManager.getTable(unitId, ids[0]);
            if (table) {
              const tableRange = table.getRange();
              const index = col - tableRange.startColumn;
              if (tableRange.startRow === row) {
                const columnName = table.getColumnNameByIndex(index);
                if (!cell || cell === context.rawData) cell = { ...context.rawData };
                cell.v = columnName;
                return next(cell);
              }
            }
          }
        }
        return next(cell);
      }
    }));
  }
  _toJson(unitId) {
    return this._tableManager.toJSON(unitId);
  }
  _fromJSON(unitId, resources) {
    return this._tableManager.fromJSON(unitId, resources);
  }
  _deleteUnitId(unitId) {
    this._tableManager.deleteUnitId(unitId);
  }
  _initSnapshot() {
    this.disposeWithMe(this._resourceManagerService.registerPluginResource({
      toJson: (unitId) => {
        return JSON.stringify(this._toJson(unitId));
      },
      parseJson: (json) => {
        if (!json) return {};
        try {
          return JSON.parse(json);
        } catch (error) {
          return {};
        }
      },
      businesses: [UniverType.UNIVER_SHEET],
      pluginName: PLUGIN_NAME,
      onLoad: (unitId, resources) => {
        this._fromJSON(unitId, resources);
      },
      onUnLoad: (unitId) => {
        this._deleteUnitId(unitId);
      }
    }));
  }
  _initSheetChange() {
    this.disposeWithMe(this._sheetInterceptorService.interceptCommand({ getMutations: (commandInfo) => {
      if (commandInfo.id === RemoveSheetCommand.id) {
        var _getActiveSheet;
        const params = commandInfo.params;
        const unitId = params.unitId || this._univerInstanceService.getCurrentUnitOfType(UniverType.UNIVER_SHEET).getUnitId();
        const subUnitId = params.subUnitId || ((_getActiveSheet = this._univerInstanceService.getCurrentUnitOfType(UniverType.UNIVER_SHEET).getActiveSheet()) === null || _getActiveSheet === void 0 ? void 0 : _getActiveSheet.getSheetId());
        if (!unitId || !subUnitId) return {
          redos: [],
          undos: []
        };
        const tables = this._tableManager.getTablesBySubunitId(unitId, subUnitId);
        if (tables.length === 0) return {
          redos: [],
          undos: []
        };
        const redos = [];
        const undos = [];
        tables.forEach((table) => {
          const tableJson = table.toJSON();
          redos.push({
            id: DeleteSheetTableMutation.id,
            params: {
              unitId,
              subUnitId,
              tableId: tableJson.id
            }
          });
          undos.push({
            id: AddSheetTableMutation.id,
            params: {
              unitId,
              subUnitId,
              name: tableJson.name,
              range: tableJson.range,
              tableId: tableJson.id,
              options: {
                ...tableJson.options,
                columns: tableJson.columns,
                filters: tableJson.filters.tableColumnFilterList
              }
            }
          });
        });
        return {
          redos,
          undos
        };
      } else if (commandInfo.id === CopySheetCommand.id) {
        const { unitId, subUnitId, targetSubUnitId } = commandInfo.params;
        if (!unitId || !subUnitId || !targetSubUnitId) return {
          redos: [],
          undos: []
        };
        const tables = this._tableManager.getTablesBySubunitId(unitId, subUnitId);
        if (tables.length === 0) return {
          redos: [],
          undos: []
        };
        const redos = [];
        const undos = [];
        tables.forEach((table) => {
          const tableJson = table.toJSON();
          const tableId = generateRandomId();
          redos.push({
            id: AddSheetTableMutation.id,
            params: {
              unitId,
              subUnitId: targetSubUnitId,
              name: tableJson.name,
              range: {
                ...tableJson.range,
                sheetId: targetSubUnitId
              },
              tableId,
              options: {
                ...tableJson.options,
                columns: tableJson.columns,
                filters: tableJson.filters.tableColumnFilterList
              }
            }
          });
          undos.push({
            id: DeleteSheetTableMutation.id,
            params: {
              unitId,
              subUnitId: targetSubUnitId,
              tableId
            }
          });
        });
        return {
          redos,
          undos
        };
      }
      return {
        redos: [],
        undos: []
      };
    } }));
  }
  dispose() {
    super.dispose();
    this._tableRangeRTree.clear();
  }
};
SheetsTableController = __decorate([
  __decorateParam(0, Inject(IUniverInstanceService)),
  __decorateParam(1, Inject(SheetInterceptorService)),
  __decorateParam(2, Inject(TableManager)),
  __decorateParam(3, Inject(IResourceManagerService))
], SheetsTableController);
function executeTableMutationSequence(accessor, unitId, redos, undos) {
  if (sequenceExecute(redos, accessor.get(ICommandService)).result) {
    accessor.get(IUndoRedoService).pushUndoRedo({
      unitID: unitId,
      undoMutations: undos,
      redoMutations: redos
    });
    return true;
  }
  return false;
}
var SheetTableInsertRowCommand = {
  id: "sheet.command.table-insert-row",
  type: CommandType.COMMAND,
  handler: (accessor) => {
    const target = getSheetCommandTarget(accessor.get(IUniverInstanceService));
    if (!target) return false;
    const { workbook, worksheet, unitId, subUnitId } = target;
    const selections = accessor.get(SheetsSelectionsService).getCurrentSelections();
    if (!selections.length || selections.length > 1) return false;
    accessor.get(TableManager);
    const range = selections[0].range;
    const table = accessor.get(SheetsTableController).getContainerTableWithRange(unitId, subUnitId, range);
    if (!table) return false;
    const insertRowCount = range.endRow - range.startRow + 1;
    const worksheetLastRowIndex = worksheet.getRowCount() - 1;
    const rowContentIndex = worksheet.getCellMatrix().getDataRange().endRow;
    const redos = [];
    const undos = [];
    if (worksheetLastRowIndex - rowContentIndex < insertRowCount) {
      redos.push({
        id: InsertRowMutation.id,
        params: {
          unitId,
          subUnitId,
          range: { ...range }
        }
      });
      redos.push({
        id: SetSheetTableMutation.id,
        params: {
          unitId,
          subUnitId,
          tableId: table.getId(),
          config: { updateRange: { newRange: {
            ...table.getRange(),
            endRow: table.getRange().endRow + insertRowCount
          } } }
        }
      });
      undos.push({
        id: SetSheetTableMutation.id,
        params: {
          unitId,
          subUnitId,
          tableId: table.getId(),
          config: { updateRange: { newRange: table.getRange() } }
        }
      });
      undos.push({
        id: RemoveRowMutation.id,
        params: {
          unitId,
          subUnitId,
          range: { ...range }
        }
      });
    } else {
      const oldRange = { ...table.getRange() };
      redos.push({
        id: SetSheetTableMutation.id,
        params: {
          unitId,
          subUnitId,
          tableId: table.getId(),
          config: { updateRange: { newRange: {
            ...oldRange,
            endRow: oldRange.endRow + insertRowCount
          } } }
        }
      });
      undos.push({
        id: SetSheetTableMutation.id,
        params: {
          unitId,
          subUnitId,
          tableId: table.getId(),
          config: { updateRange: { newRange: { ...oldRange } } }
        }
      });
      const moveRangeMutations = getMoveRangeUndoRedoMutations(accessor, {
        unitId,
        subUnitId,
        range: {
          startRow: range.startRow,
          endRow: rowContentIndex,
          startColumn: oldRange.startColumn,
          endColumn: oldRange.endColumn
        }
      }, {
        unitId,
        subUnitId,
        range: {
          startRow: range.startRow + insertRowCount,
          endRow: rowContentIndex + insertRowCount,
          startColumn: oldRange.startColumn,
          endColumn: oldRange.endColumn
        }
      });
      if (moveRangeMutations) {
        redos.push(...moveRangeMutations.redos);
        undos.push(...moveRangeMutations.undos);
      }
    }
    if (sequenceExecute(redos, accessor.get(ICommandService)).result) {
      accessor.get(IUndoRedoService).pushUndoRedo({
        unitID: unitId,
        undoMutations: undos,
        redoMutations: redos
      });
      return true;
    }
    return false;
  }
};
var SheetTableInsertRowAtCommand = {
  id: "sheet.command.table-insert-row-at",
  type: CommandType.COMMAND,
  handler: (accessor, params) => {
    if (!params) return false;
    const { unitId, subUnitId, tableId, index, count = 1 } = params;
    if (count <= 0) return false;
    const target = getSheetCommandTarget(accessor.get(IUniverInstanceService), {
      unitId,
      subUnitId
    });
    if (!target) return false;
    const table = accessor.get(TableManager).getTableById(unitId, tableId);
    if (!table || table.getSubunitId() !== subUnitId) return false;
    const oldRange = table.getRange();
    if (index <= oldRange.startRow || index > oldRange.endRow + 1) return false;
    const redos = [{
      id: SetSheetTableMutation.id,
      params: {
        unitId,
        subUnitId,
        tableId,
        config: { updateRange: { newRange: {
          ...oldRange,
          endRow: oldRange.endRow + count
        } } }
      }
    }];
    const undos = [{
      id: SetSheetTableMutation.id,
      params: {
        unitId,
        subUnitId,
        tableId,
        config: { updateRange: { newRange: { ...oldRange } } }
      }
    }];
    const rowContentIndex = target.worksheet.getCellMatrix().getDataRange().endRow;
    const moveRangeMutations = getMoveRangeUndoRedoMutations(accessor, {
      unitId,
      subUnitId,
      range: {
        startRow: index,
        endRow: rowContentIndex,
        startColumn: oldRange.startColumn,
        endColumn: oldRange.endColumn
      }
    }, {
      unitId,
      subUnitId,
      range: {
        startRow: index + count,
        endRow: rowContentIndex + count,
        startColumn: oldRange.startColumn,
        endColumn: oldRange.endColumn
      }
    });
    if (moveRangeMutations) {
      redos.push(...moveRangeMutations.redos);
      undos.push(...moveRangeMutations.undos);
    }
    return executeTableMutationSequence(accessor, unitId, redos, undos);
  }
};
var SheetTableInsertColCommand = {
  id: "sheet.command.table-insert-col",
  type: CommandType.COMMAND,
  handler: (accessor) => {
    const target = getSheetCommandTarget(accessor.get(IUniverInstanceService));
    if (!target) return false;
    const { worksheet, unitId, subUnitId } = target;
    const selections = accessor.get(SheetsSelectionsService).getCurrentSelections();
    if (!selections.length || selections.length > 1) return false;
    const range = selections[0].range;
    const table = accessor.get(SheetsTableController).getContainerTableWithRange(unitId, subUnitId, range);
    if (!table) return false;
    const insertColCount = range.endColumn - range.startColumn + 1;
    const worksheetLastColIndex = worksheet.getColumnCount() - 1;
    const colContentIndex = worksheet.getCellMatrix().getDataRange().endColumn;
    const redos = [];
    const undos = [];
    if (worksheetLastColIndex - colContentIndex < insertColCount) {
      redos.push({
        id: InsertColMutation.id,
        params: {
          unitId,
          subUnitId,
          range: { ...range }
        }
      });
      redos.push({
        id: SetSheetTableMutation.id,
        params: {
          unitId,
          subUnitId,
          tableId: table.getId(),
          config: { rowColOperation: {
            operationType: "insert",
            rowColType: "column",
            index: range.startColumn,
            count: insertColCount
          } }
        }
      });
      undos.push({
        id: SetSheetTableMutation.id,
        params: {
          unitId,
          subUnitId,
          tableId: table.getId(),
          config: { rowColOperation: {
            operationType: "delete",
            rowColType: "column",
            index: range.startColumn,
            count: insertColCount
          } }
        }
      });
      undos.push({
        id: RemoveColMutation.id,
        params: {
          unitId,
          subUnitId,
          range: { ...range }
        }
      });
    } else {
      const oldRange = table.getRange();
      redos.push({
        id: SetSheetTableMutation.id,
        params: {
          unitId,
          subUnitId,
          tableId: table.getId(),
          config: { rowColOperation: {
            operationType: "insert",
            rowColType: "column",
            index: range.startColumn,
            count: insertColCount
          } }
        }
      });
      undos.push({
        id: SetSheetTableMutation.id,
        params: {
          unitId,
          subUnitId,
          tableId: table.getId(),
          config: { rowColOperation: {
            operationType: "delete",
            rowColType: "column",
            index: range.startColumn,
            count: insertColCount
          } }
        }
      });
      const moveRangeMutations = getMoveRangeUndoRedoMutations(accessor, {
        unitId,
        subUnitId,
        range: {
          startRow: oldRange.startRow,
          endRow: oldRange.endRow,
          startColumn: range.startColumn,
          endColumn: colContentIndex
        }
      }, {
        unitId,
        subUnitId,
        range: {
          startRow: oldRange.startRow,
          endRow: oldRange.endRow,
          startColumn: range.startColumn + insertColCount,
          endColumn: colContentIndex + insertColCount
        }
      });
      if (moveRangeMutations) {
        redos.push(...moveRangeMutations.redos);
        undos.push(...moveRangeMutations.undos);
      }
    }
    if (sequenceExecute(redos, accessor.get(ICommandService)).result) {
      accessor.get(IUndoRedoService).pushUndoRedo({
        unitID: unitId,
        undoMutations: undos,
        redoMutations: redos
      });
      return true;
    }
    return false;
  }
};
var SheetTableInsertColumnAtCommand = {
  id: "sheet.command.table-insert-column-at",
  type: CommandType.COMMAND,
  handler: (accessor, params) => {
    if (!params) return false;
    const { unitId, subUnitId, tableId, index, count = 1 } = params;
    if (count <= 0) return false;
    const target = getSheetCommandTarget(accessor.get(IUniverInstanceService), {
      unitId,
      subUnitId
    });
    if (!target) return false;
    const table = accessor.get(TableManager).getTableById(unitId, tableId);
    if (!table || table.getSubunitId() !== subUnitId) return false;
    const oldRange = table.getRange();
    if (index < oldRange.startColumn || index > oldRange.endColumn + 1) return false;
    const redos = [{
      id: SetSheetTableMutation.id,
      params: {
        unitId,
        subUnitId,
        tableId,
        config: { rowColOperation: {
          operationType: "insert",
          rowColType: "column",
          index,
          count
        } }
      }
    }];
    const undos = [{
      id: SetSheetTableMutation.id,
      params: {
        unitId,
        subUnitId,
        tableId,
        config: { rowColOperation: {
          operationType: "delete",
          rowColType: "column",
          index,
          count
        } }
      }
    }];
    const colContentIndex = target.worksheet.getCellMatrix().getDataRange().endColumn;
    if (index <= colContentIndex) {
      const moveRangeMutations = getMoveRangeUndoRedoMutations(accessor, {
        unitId,
        subUnitId,
        range: {
          startRow: oldRange.startRow,
          endRow: oldRange.endRow,
          startColumn: index,
          endColumn: colContentIndex
        }
      }, {
        unitId,
        subUnitId,
        range: {
          startRow: oldRange.startRow,
          endRow: oldRange.endRow,
          startColumn: index + count,
          endColumn: colContentIndex + count
        }
      });
      if (moveRangeMutations) {
        redos.push(...moveRangeMutations.redos);
        undos.push(...moveRangeMutations.undos);
      }
    }
    return executeTableMutationSequence(accessor, unitId, redos, undos);
  }
};
var SheetTableRemoveColumnAtCommand = {
  id: "sheet.command.table-remove-column-at",
  type: CommandType.COMMAND,
  handler: (accessor, params) => {
    var _interceptorCommands$, _interceptorCommands$2;
    if (!params) return false;
    const { unitId, subUnitId, tableId, index, count = 1 } = params;
    if (count <= 0) return false;
    const target = getSheetCommandTarget(accessor.get(IUniverInstanceService), {
      unitId,
      subUnitId
    });
    if (!target) return false;
    const table = accessor.get(TableManager).getTableById(unitId, tableId);
    if (!table || table.getSubunitId() !== subUnitId) return false;
    const oldRange = table.getRange();
    if (index < oldRange.startColumn || index + count - 1 > oldRange.endColumn || count >= oldRange.endColumn - oldRange.startColumn + 1) return false;
    const tableInfo = table.getTableInfo();
    const columns = [];
    const gap = index - oldRange.startColumn;
    for (let i = 0; i < count; i++) {
      const column = tableInfo.columns[gap + i];
      if (column) columns.push(column);
    }
    const interceptorCommands = accessor.get(SheetInterceptorService).onCommandExecute({
      id: SheetTableRemoveColumnAtCommand.id,
      params: {
        ...params,
        tableName: tableInfo.name,
        removedColumnNames: columns.map((column) => column.displayName)
      }
    });
    const redos = [
      ...(_interceptorCommands$ = interceptorCommands.preRedos) !== null && _interceptorCommands$ !== void 0 ? _interceptorCommands$ : [],
      {
        id: SetSheetTableMutation.id,
        params: {
          unitId,
          subUnitId,
          tableId,
          config: { rowColOperation: {
            operationType: "delete",
            rowColType: "column",
            index,
            count
          } }
        }
      },
      ...interceptorCommands.redos
    ];
    const undos = [
      ...(_interceptorCommands$2 = interceptorCommands.preUndos) !== null && _interceptorCommands$2 !== void 0 ? _interceptorCommands$2 : [],
      {
        id: SetSheetTableMutation.id,
        params: {
          unitId,
          subUnitId,
          tableId,
          config: { rowColOperation: {
            operationType: "insert",
            rowColType: "column",
            index,
            count,
            columnsJson: columns
          } }
        }
      },
      ...interceptorCommands.undos
    ];
    const colContentIndex = target.worksheet.getCellMatrix().getDataRange().endColumn;
    if (index + count <= colContentIndex) {
      const moveRangeMutations = getMoveRangeUndoRedoMutations(accessor, {
        unitId,
        subUnitId,
        range: {
          startRow: oldRange.startRow,
          endRow: oldRange.endRow,
          startColumn: index + count,
          endColumn: colContentIndex
        }
      }, {
        unitId,
        subUnitId,
        range: {
          startRow: oldRange.startRow,
          endRow: oldRange.endRow,
          startColumn: index,
          endColumn: colContentIndex - count
        }
      });
      if (moveRangeMutations) {
        redos.push(...moveRangeMutations.redos);
        undos.push(...moveRangeMutations.undos);
      }
    }
    return executeTableMutationSequence(accessor, unitId, redos, undos);
  }
};
var SheetTableRemoveRowCommand = {
  id: "sheet.command.table-remove-row",
  type: CommandType.COMMAND,
  handler: (accessor, params) => {
    if (!params) return false;
    const target = getSheetCommandTarget(accessor.get(IUniverInstanceService), params);
    if (!target) return false;
    const { unitId, subUnitId } = target;
    const selections = accessor.get(SheetsSelectionsService).getCurrentSelections();
    if (!selections.length || selections.length > 1) return false;
    const range = selections[0].range;
    const table = accessor.get(SheetsTableController).getContainerTableWithRange(unitId, subUnitId, range);
    if (!table) return false;
    const removeRowCount = range.endRow - range.startRow + 1;
    const redos = [];
    const undos = [];
    const oldRange = table.getRange();
    redos.push({
      id: SetSheetTableMutation.id,
      params: {
        unitId,
        subUnitId,
        tableId: table.getId(),
        config: { updateRange: { newRange: {
          ...oldRange,
          endRow: oldRange.endRow - removeRowCount
        } } }
      }
    });
    undos.push({
      id: SetSheetTableMutation.id,
      params: {
        unitId,
        subUnitId,
        tableId: table.getId(),
        config: { updateRange: { newRange: { ...oldRange } } }
      }
    });
    const rowContentIndex = target.worksheet.getCellMatrix().getDataRange().endRow;
    const moveRangeMutations = getMoveRangeUndoRedoMutations(accessor, {
      unitId,
      subUnitId,
      range: {
        startRow: range.endRow + 1,
        endRow: rowContentIndex,
        startColumn: oldRange.startColumn,
        endColumn: oldRange.endColumn
      }
    }, {
      unitId,
      subUnitId,
      range: {
        startRow: range.startRow,
        endRow: rowContentIndex - removeRowCount,
        startColumn: oldRange.startColumn,
        endColumn: oldRange.endColumn
      }
    });
    if (moveRangeMutations) {
      redos.push(...moveRangeMutations.redos);
      undos.push(...moveRangeMutations.undos);
    }
    if (sequenceExecute(redos, accessor.get(ICommandService)).result) {
      accessor.get(IUndoRedoService).pushUndoRedo({
        unitID: unitId,
        undoMutations: undos,
        redoMutations: redos
      });
      return true;
    }
    return false;
  }
};
var SheetTableRemoveColCommand = {
  id: "sheet.command.table-remove-col",
  type: CommandType.COMMAND,
  handler: (accessor, params) => {
    var _interceptorCommands$3, _interceptorCommands$4;
    if (!params) return false;
    const target = getSheetCommandTarget(accessor.get(IUniverInstanceService), params);
    if (!target) return false;
    const { workbook, unitId, subUnitId } = target;
    accessor.get(TableManager);
    const selections = accessor.get(SheetsSelectionsService).getCurrentSelections();
    if (!selections.length || selections.length > 1) return false;
    const range = selections[0].range;
    const table = accessor.get(SheetsTableController).getContainerTableWithRange(unitId, subUnitId, range);
    if (!table) return false;
    const removeColCount = range.endColumn - range.startColumn + 1;
    const redos = [];
    const undos = [];
    const oldRange = table.getRange();
    redos.push({
      id: SetSheetTableMutation.id,
      params: {
        unitId,
        subUnitId,
        tableId: table.getId(),
        config: { rowColOperation: {
          operationType: "delete",
          rowColType: "column",
          index: range.startColumn,
          count: removeColCount
        } }
      }
    });
    const tableInfo = table.getTableInfo();
    const columns = [];
    const gap = range.startColumn - oldRange.startColumn;
    for (let i = 0; i < removeColCount; i++) {
      const column = tableInfo.columns[gap + i];
      if (column) columns.push(column);
    }
    const interceptorCommands = accessor.get(SheetInterceptorService).onCommandExecute({
      id: SheetTableRemoveColCommand.id,
      params: {
        ...params,
        tableName: tableInfo.name,
        removedColumnNames: columns.map((column) => column.displayName)
      }
    });
    redos.unshift(...(_interceptorCommands$3 = interceptorCommands.preRedos) !== null && _interceptorCommands$3 !== void 0 ? _interceptorCommands$3 : []);
    redos.push(...interceptorCommands.redos);
    undos.unshift(...(_interceptorCommands$4 = interceptorCommands.preUndos) !== null && _interceptorCommands$4 !== void 0 ? _interceptorCommands$4 : []);
    undos.push({
      id: SetSheetTableMutation.id,
      params: {
        unitId,
        subUnitId,
        tableId: table.getId(),
        config: { rowColOperation: {
          operationType: "insert",
          rowColType: "column",
          index: range.startColumn,
          count: removeColCount,
          columnsJson: columns
        } }
      }
    });
    undos.push(...interceptorCommands.undos);
    const colContentIndex = target.worksheet.getCellMatrix().getDataRange().endColumn;
    const moveRangeMutations = getMoveRangeUndoRedoMutations(accessor, {
      unitId,
      subUnitId,
      range: {
        startRow: oldRange.startRow,
        endRow: oldRange.endRow,
        startColumn: range.endColumn + 1,
        endColumn: colContentIndex
      }
    }, {
      unitId,
      subUnitId,
      range: {
        startRow: oldRange.startRow,
        endRow: oldRange.endRow,
        startColumn: range.startColumn,
        endColumn: colContentIndex - removeColCount
      }
    });
    if (moveRangeMutations) {
      redos.push(...moveRangeMutations.redos);
      undos.push(...moveRangeMutations.undos);
    }
    if (sequenceExecute(redos, accessor.get(ICommandService)).result) {
      accessor.get(IUndoRedoService).pushUndoRedo({
        unitID: unitId,
        undoMutations: undos,
        redoMutations: redos
      });
      return true;
    }
    return false;
  }
};
var name = "@univerjs/sheets-table";
var version = "0.25.1";
var SheetTableFormulaController = class SheetTableFormulaController2 extends Disposable {
  constructor(_tableManager, _commandService) {
    super();
    this._tableManager = _tableManager;
    this._commandService = _commandService;
    this._initRangeListener();
  }
  _initRangeListener() {
    this.disposeWithMe(this._tableManager.tableRangeChanged$.subscribe((event) => {
      const { tableId, unitId } = event;
      const table = this._tableManager.getTableById(unitId, tableId);
      if (!table) return;
      this._updateSuperTable(unitId, table);
    }));
    this.disposeWithMe(this._tableManager.tableAdd$.subscribe((event) => {
      const { tableId, unitId } = event;
      const table = this._tableManager.getTableById(unitId, tableId);
      if (!table) return;
      this._updateSuperTable(unitId, table);
    }));
    this.disposeWithMe(this._tableManager.tableDelete$.subscribe((event) => {
      const { unitId, tableName } = event;
      this._commandService.executeCommand(RemoveSuperTableMutation.id, {
        unitId,
        tableName
      });
    }));
    this.disposeWithMe(this._tableManager.tableNameChanged$.subscribe((event) => {
      const { tableId, unitId, oldTableName } = event;
      this._commandService.executeCommand(RemoveSuperTableMutation.id, {
        unitId,
        tableName: oldTableName
      });
      const table = this._tableManager.getTableById(unitId, tableId);
      if (!table) return;
      this._updateSuperTable(unitId, table, oldTableName);
    }));
  }
  _updateSuperTable(unitId, table, oldTableName) {
    const tableInfo = table.getTableInfo();
    const name3 = tableInfo.name;
    const columns = tableInfo.columns;
    const titleMap = /* @__PURE__ */ new Map();
    columns.forEach((column, index) => {
      titleMap.set(column.displayName, index);
    });
    this._commandService.executeCommand(SetSuperTableMutation.id, {
      unitId,
      tableName: name3,
      oldTableName,
      reference: {
        range: tableInfo.range,
        sheetId: tableInfo.subUnitId,
        titleMap
      }
    });
  }
};
SheetTableFormulaController = __decorate([__decorateParam(0, Inject(TableManager)), __decorateParam(1, ICommandService)], SheetTableFormulaController);
var SheetTableRangeController = class SheetTableRangeController2 extends Disposable {
  constructor(_tableManager, _exclusiveRangeService) {
    super();
    this._tableManager = _tableManager;
    this._exclusiveRangeService = _exclusiveRangeService;
    this._initRangeListener();
  }
  _initRangeListener() {
    this.disposeWithMe(this._tableManager.tableRangeChanged$.subscribe((event) => {
      const { range, tableId, unitId, subUnitId } = event;
      this._exclusiveRangeService.clearExclusiveRangesByGroupId(unitId, subUnitId, FEATURE_TABLE_ID, tableId);
      this._exclusiveRangeService.addExclusiveRange(unitId, subUnitId, FEATURE_TABLE_ID, [{
        range: { ...range },
        groupId: tableId
      }]);
    }));
    this.disposeWithMe(this._tableManager.tableAdd$.subscribe((event) => {
      const { tableId, unitId, subUnitId, range } = event;
      this._exclusiveRangeService.addExclusiveRange(unitId, subUnitId, FEATURE_TABLE_ID, [{
        range: { ...range },
        groupId: tableId
      }]);
    }));
    this.disposeWithMe(this._tableManager.tableDelete$.subscribe((event) => {
      const { tableId, unitId, subUnitId } = event;
      this._exclusiveRangeService.clearExclusiveRangesByGroupId(unitId, subUnitId, FEATURE_TABLE_ID, tableId);
    }));
  }
};
SheetTableRangeController = __decorate([__decorateParam(0, Inject(TableManager)), __decorateParam(1, Inject(IExclusiveRangeService))], SheetTableRangeController);
var SHEET_TABLE_REMOVE_COL_COMMAND_ID = "sheet.command.table-remove-col";
var DELETE_SHEET_TABLE_COMMAND_ID = "sheet.command.delete-table";
var SheetTableRefRangeController = class SheetTableRefRangeController2 extends Disposable {
  constructor(_commandService, _refRangeService, _univerInstanceService, _injector, _sheetInterceptorService, _tableManager, _localeService) {
    super();
    this._commandService = _commandService;
    this._refRangeService = _refRangeService;
    this._univerInstanceService = _univerInstanceService;
    this._injector = _injector;
    this._sheetInterceptorService = _sheetInterceptorService;
    this._tableManager = _tableManager;
    this._localeService = _localeService;
    this._initCommandInterceptor();
    this._initCommandListener();
  }
  _initCommandInterceptor() {
    const self = this;
    this._sheetInterceptorService.interceptCommand({
      priority: -1,
      getMutations(commandInfo) {
        const defaultReturn = {
          redos: [],
          undos: []
        };
        const { id, params } = commandInfo;
        switch (id) {
          case InsertRowCommand.id:
            return self._generateTableMutationWithInsertRow(params);
          case InsertColCommand.id:
            return self._generateTableMutationWithInsertCol(params);
          case RemoveRowCommand.id:
            return self._generateTableMutationWithRemoveRow(params);
          case RemoveColCommand.id:
            return self._generateTableMutationWithRemoveCol(params);
        }
        return defaultReturn;
      }
    });
  }
  _generateTableMutationWithInsertRow(insertParams) {
    const undos = [];
    const redos = [];
    const target = getSheetCommandTarget(this._univerInstanceService, insertParams);
    if (!target) return {
      undos,
      redos
    };
    const { unitId, subUnitId } = target;
    const allSubUnitTables = this._tableManager.getTablesBySubunitId(unitId, subUnitId);
    if (!allSubUnitTables.length) return {
      undos,
      redos
    };
    const { range } = insertParams;
    allSubUnitTables.forEach((table) => {
      const tableRange = table.getRange();
      if (range.startRow > tableRange.startRow && range.startRow <= tableRange.endRow) {
        const insertRowCount = range.endRow - range.startRow + 1;
        redos.push({
          id: SetSheetTableMutation.id,
          params: {
            unitId,
            subUnitId,
            tableId: table.getId(),
            config: { updateRange: { newRange: {
              ...tableRange,
              endRow: tableRange.endRow + insertRowCount
            } } }
          }
        });
        undos.push({
          id: SetSheetTableMutation.id,
          params: {
            unitId,
            subUnitId,
            tableId: table.getId(),
            config: { updateRange: { newRange: { ...tableRange } } }
          }
        });
      }
    });
    return {
      undos,
      redos
    };
  }
  _generateTableMutationWithInsertCol(insertParams) {
    const undos = [];
    const redos = [];
    const target = getSheetCommandTarget(this._univerInstanceService, insertParams);
    if (!target) return {
      undos,
      redos
    };
    const { unitId, subUnitId } = target;
    const allSubUnitTables = this._tableManager.getTablesBySubunitId(unitId, subUnitId);
    if (!allSubUnitTables.length) return {
      undos,
      redos
    };
    const { range } = insertParams;
    allSubUnitTables.forEach((table) => {
      const tableRange = table.getRange();
      if (range.startColumn > tableRange.startColumn && range.startColumn <= tableRange.endColumn) {
        const insertColCount = range.endColumn - range.startColumn + 1;
        redos.push({
          id: SetSheetTableMutation.id,
          params: {
            unitId,
            subUnitId,
            tableId: table.getId(),
            config: { rowColOperation: {
              operationType: "insert",
              rowColType: "column",
              index: range.startColumn,
              count: insertColCount
            } }
          }
        });
        undos.push({
          id: SetSheetTableMutation.id,
          params: {
            unitId,
            subUnitId,
            tableId: table.getId(),
            config: { rowColOperation: {
              operationType: "delete",
              rowColType: "column",
              index: range.startColumn,
              count: insertColCount
            } }
          }
        });
      }
    });
    return {
      undos,
      redos
    };
  }
  _generateTableMutationWithRemoveRow(removeParams) {
    const undos = [];
    const redos = [];
    const preRedos = [];
    const preUndos = [];
    const target = getSheetCommandTarget(this._univerInstanceService);
    if (!target) return {
      undos,
      redos,
      preRedos,
      preUndos
    };
    const { unitId, subUnitId } = target;
    const allSubUnitTables = this._tableManager.getTablesBySubunitId(unitId, subUnitId);
    if (!allSubUnitTables.length) return {
      undos,
      redos,
      preRedos,
      preUndos
    };
    const { range } = removeParams;
    const removeRowCount = range.endRow - range.startRow + 1;
    allSubUnitTables.forEach((table) => {
      const tableRange = table.getRange();
      if (Rectangle.intersects(tableRange, range)) {
        if (range.startRow <= tableRange.startRow && range.endRow >= tableRange.startRow) {
          preRedos.push({
            id: DeleteSheetTableMutation.id,
            params: {
              unitId,
              subUnitId,
              tableId: table.getId()
            }
          });
          const tableJson = table.toJSON();
          undos.push({
            id: AddSheetTableMutation.id,
            params: {
              unitId,
              subUnitId,
              tableId: tableJson.id,
              name: tableJson.name,
              range: tableJson.range,
              options: tableJson.options
            }
          });
        } else if (range.startRow > tableRange.startRow && range.startRow <= tableRange.endRow) {
          redos.push({
            id: SetSheetTableMutation.id,
            params: {
              unitId,
              subUnitId,
              tableId: table.getId(),
              config: { updateRange: { newRange: {
                ...tableRange,
                endRow: tableRange.endRow - removeRowCount
              } } }
            }
          });
          undos.push({
            id: SetSheetTableMutation.id,
            params: {
              unitId,
              subUnitId,
              tableId: table.getId(),
              config: { updateRange: { newRange: { ...tableRange } } }
            }
          });
        } else if (range.startRow < tableRange.endRow && range.endRow >= tableRange.endRow) {
          redos.push({
            id: SetSheetTableMutation.id,
            params: {
              unitId,
              subUnitId,
              tableId: table.getId(),
              config: { updateRange: { newRange: {
                ...tableRange,
                endRow: range.startRow - 1
              } } }
            }
          });
          undos.push({
            id: SetSheetTableMutation.id,
            params: {
              unitId,
              subUnitId,
              tableId: table.getId(),
              config: { updateRange: { newRange: { ...tableRange } } }
            }
          });
        }
      }
    });
    return {
      undos,
      redos,
      preRedos,
      preUndos
    };
  }
  _generateTableMutationWithRemoveCol(removeParams) {
    const undos = [];
    const redos = [];
    const preRedos = [];
    const preUndos = [];
    const target = getSheetCommandTarget(this._univerInstanceService);
    if (!target) return {
      undos,
      redos,
      preRedos,
      preUndos
    };
    const { unitId, subUnitId } = target;
    const allSubUnitTables = this._tableManager.getTablesBySubunitId(unitId, subUnitId);
    if (!allSubUnitTables.length) return {
      undos,
      redos,
      preRedos,
      preUndos
    };
    const { range } = removeParams;
    const removeColCount = range.endColumn - range.startColumn + 1;
    allSubUnitTables.forEach((table) => {
      const tableRange = table.getRange();
      if (Rectangle.intersects(tableRange, range)) {
        if (range.startColumn <= tableRange.startColumn && range.endColumn >= tableRange.endColumn) {
          var _formulaMutations$pre, _formulaMutations$pre2;
          const tableInfo = table.getTableInfo();
          const formulaMutations = this._sheetInterceptorService.onCommandExecute({
            id: DELETE_SHEET_TABLE_COMMAND_ID,
            params: {
              unitId,
              subUnitId,
              tableId: table.getId(),
              tableName: tableInfo.name
            }
          });
          preRedos.push(...(_formulaMutations$pre = formulaMutations.preRedos) !== null && _formulaMutations$pre !== void 0 ? _formulaMutations$pre : [], ...formulaMutations.redos);
          preRedos.push({
            id: DeleteSheetTableMutation.id,
            params: {
              unitId,
              subUnitId,
              tableId: table.getId()
            }
          });
          const tableJson = table.toJSON();
          const { startRow, startColumn, endColumn } = tableJson.range;
          const workbook = this._univerInstanceService.getUnit(unitId);
          const worksheet = workbook === null || workbook === void 0 ? void 0 : workbook.getSheetBySheetId(subUnitId);
          if (!worksheet) return {
            undos,
            redos,
            preRedos,
            preUndos
          };
          const header = [];
          for (let i = startColumn; i <= endColumn; i++) header.push(convertCellDataToString(worksheet === null || worksheet === void 0 ? void 0 : worksheet.getCell(startRow, i)) || getColumnName(i - startColumn + 1, this._localeService.t("sheets-table.columnPrefix")));
          undos.push({
            id: AddSheetTableMutation.id,
            params: {
              unitId,
              subUnitId,
              tableId: tableJson.id,
              name: tableJson.name,
              header,
              range: tableJson.range,
              options: tableJson.options
            }
          });
          undos.push(...(_formulaMutations$pre2 = formulaMutations.preUndos) !== null && _formulaMutations$pre2 !== void 0 ? _formulaMutations$pre2 : [], ...formulaMutations.undos);
        } else if (range.startColumn <= tableRange.startColumn && range.endColumn >= tableRange.startColumn) {
          const tableJson = table.toJSON();
          const removeColumnCount = range.endColumn - tableRange.startColumn + 1;
          redos.push({
            id: SetSheetTableMutation.id,
            params: {
              unitId,
              subUnitId,
              tableId: table.getId(),
              config: { rowColOperation: {
                operationType: "delete",
                rowColType: "column",
                index: tableRange.startColumn,
                count: removeColumnCount
              } }
            }
          });
          const columns = [];
          for (let i = 0; i < removeColumnCount; i++) {
            const column = table.getTableColumnByIndex(i);
            if (column) columns.push(column.toJSON());
          }
          preUndos.push(this._getDeleteTableMutation(unitId, subUnitId, table.getId()));
          undos.push(this._getAddTableMutation(unitId, subUnitId, tableJson));
          this._appendTableColumnFormulaMutations(redos, undos, {
            unitId,
            subUnitId,
            tableId: table.getId(),
            tableName: table.getTableInfo().name,
            range,
            columns
          });
        } else if (range.startColumn > tableRange.startColumn && range.endColumn > tableRange.endColumn) {
          const tableJson = table.toJSON();
          const removeColumnCount = tableRange.endColumn - range.startColumn + 1;
          redos.push({
            id: SetSheetTableMutation.id,
            params: {
              unitId,
              subUnitId,
              tableId: table.getId(),
              config: { rowColOperation: {
                operationType: "delete",
                rowColType: "column",
                index: range.startColumn,
                count: removeColumnCount
              } }
            }
          });
          const columns = [];
          const gap = range.startColumn - tableRange.startColumn;
          for (let i = 0; i < removeColumnCount; i++) {
            const column = table.getTableColumnByIndex(i + gap);
            if (column) columns.push(column.toJSON());
          }
          preUndos.push(this._getDeleteTableMutation(unitId, subUnitId, table.getId()));
          undos.push(this._getAddTableMutation(unitId, subUnitId, tableJson));
          this._appendTableColumnFormulaMutations(redos, undos, {
            unitId,
            subUnitId,
            tableId: table.getId(),
            tableName: table.getTableInfo().name,
            range,
            columns
          });
        } else if (range.startColumn > tableRange.startColumn && range.endColumn <= tableRange.endColumn) {
          const tableJson = table.toJSON();
          redos.push({
            id: SetSheetTableMutation.id,
            params: {
              unitId,
              subUnitId,
              tableId: table.getId(),
              config: { rowColOperation: {
                operationType: "delete",
                rowColType: "column",
                index: range.startColumn,
                count: removeColCount
              } }
            }
          });
          const columns = [];
          const gap = range.startColumn - tableRange.startColumn;
          for (let i = 0; i < removeColCount; i++) {
            const column = table.getTableColumnByIndex(i + gap);
            if (column) columns.push(column.toJSON());
          }
          preUndos.push(this._getDeleteTableMutation(unitId, subUnitId, table.getId()));
          undos.push(this._getAddTableMutation(unitId, subUnitId, tableJson));
          this._appendTableColumnFormulaMutations(redos, undos, {
            unitId,
            subUnitId,
            tableId: table.getId(),
            tableName: table.getTableInfo().name,
            range,
            columns
          });
        }
      }
    });
    return {
      undos,
      redos,
      preRedos,
      preUndos
    };
  }
  _getDeleteTableMutation(unitId, subUnitId, tableId) {
    return {
      id: DeleteSheetTableMutation.id,
      params: {
        unitId,
        subUnitId,
        tableId
      }
    };
  }
  _getAddTableMutation(unitId, subUnitId, tableJson) {
    const header = tableJson.columns.map((column) => column.displayName);
    return {
      id: AddSheetTableMutation.id,
      params: {
        unitId,
        subUnitId,
        tableId: tableJson.id,
        name: tableJson.name,
        header,
        range: tableJson.range,
        options: {
          ...tableJson.options,
          columns: tableJson.columns,
          filters: tableJson.filters.tableColumnFilterList
        }
      }
    };
  }
  _appendTableColumnFormulaMutations(redos, undos, info) {
    var _formulaMutations$pre3, _formulaMutations$pre4;
    const removedColumnNames = info.columns.map((column) => column.displayName);
    if (!removedColumnNames.length) return;
    const formulaMutations = this._sheetInterceptorService.onCommandExecute({
      id: SHEET_TABLE_REMOVE_COL_COMMAND_ID,
      params: {
        unitId: info.unitId,
        subUnitId: info.subUnitId,
        tableId: info.tableId,
        tableName: info.tableName,
        range: info.range,
        removedColumnNames
      }
    });
    redos.splice(Math.max(redos.length - 1, 0), 0, ...(_formulaMutations$pre3 = formulaMutations.preRedos) !== null && _formulaMutations$pre3 !== void 0 ? _formulaMutations$pre3 : [], ...formulaMutations.redos);
    undos.push(...(_formulaMutations$pre4 = formulaMutations.preUndos) !== null && _formulaMutations$pre4 !== void 0 ? _formulaMutations$pre4 : [], ...formulaMutations.undos);
  }
  _initCommandListener() {
    this._commandService.onCommandExecuted((commandInfo) => {
      if (commandInfo.id === InsertRowMutation.id) {
        const { unitId, subUnitId, range } = commandInfo.params;
        const insertCount = range.endRow - range.startRow + 1;
        this._tableManager.getTablesBySubunitId(unitId, subUnitId).forEach((table) => {
          const tableRange = table.getRange();
          if (range.startRow <= tableRange.startRow) this._tableManager.updateTableRange(unitId, table.getId(), { newRange: {
            ...tableRange,
            startRow: tableRange.startRow + insertCount,
            endRow: tableRange.endRow + insertCount
          } });
        });
      } else if (commandInfo.id === InsertColMutation.id) {
        const { unitId, subUnitId, range } = commandInfo.params;
        const insertCount = range.endColumn - range.startColumn + 1;
        this._tableManager.getTablesBySubunitId(unitId, subUnitId).forEach((table) => {
          const tableRange = table.getRange();
          if (range.startColumn <= tableRange.startColumn) this._tableManager.updateTableRange(unitId, table.getId(), { newRange: {
            ...tableRange,
            startColumn: tableRange.startColumn + insertCount,
            endColumn: tableRange.endColumn + insertCount
          } });
        });
      } else if (commandInfo.id === RemoveRowMutation.id) {
        const { unitId, subUnitId, range } = commandInfo.params;
        const removeCount = range.endRow - range.startRow + 1;
        this._tableManager.getTablesBySubunitId(unitId, subUnitId).forEach((table) => {
          const tableRange = table.getRange();
          if (range.startRow < tableRange.startRow) this._tableManager.updateTableRange(unitId, table.getId(), { newRange: {
            ...tableRange,
            startRow: tableRange.startRow - removeCount,
            endRow: tableRange.endRow - removeCount
          } });
        });
      } else if (commandInfo.id === RemoveColMutation.id) {
        const { unitId, subUnitId, range } = commandInfo.params;
        const removeCount = range.endColumn - range.startColumn + 1;
        this._tableManager.getTablesBySubunitId(unitId, subUnitId).forEach((table) => {
          const tableRange = table.getRange();
          if (range.startColumn < tableRange.startColumn) this._tableManager.updateTableRange(unitId, table.getId(), { newRange: {
            ...tableRange,
            startColumn: tableRange.startColumn - removeCount,
            endColumn: tableRange.endColumn - removeCount
          } });
        });
      }
    });
  }
};
SheetTableRefRangeController = __decorate([
  __decorateParam(0, Inject(ICommandService)),
  __decorateParam(1, Inject(RefRangeService)),
  __decorateParam(2, Inject(IUniverInstanceService)),
  __decorateParam(3, Inject(Injector)),
  __decorateParam(4, Inject(SheetInterceptorService)),
  __decorateParam(5, Inject(TableManager)),
  __decorateParam(6, Inject(LocaleService))
], SheetTableRefRangeController);
var SheetsTableThemeController = class SheetsTableThemeController2 extends Disposable {
  constructor(_tableManager, _sheetRangeThemeService, _sheetRangeThemeModel, _configService) {
    super();
    this._tableManager = _tableManager;
    this._sheetRangeThemeService = _sheetRangeThemeService;
    this._sheetRangeThemeModel = _sheetRangeThemeModel;
    this._configService = _configService;
    _defineProperty(this, "_defaultThemeIndex", 0);
    _defineProperty(this, "_allThemes", []);
    this._initUserTableTheme();
    this.registerTableChangeEvent();
    this._initDefaultTableTheme();
  }
  registerTableChangeEvent() {
    this.disposeWithMe(this._tableManager.tableAdd$.subscribe((event) => {
      const { range, tableId, unitId, subUnitId, tableStyleId } = event;
      const table = this._tableManager.getTable(unitId, tableId);
      const _tableStyleId = tableStyleId || this._allThemes[this._defaultThemeIndex].name;
      table.setTableStyleId(_tableStyleId);
      this._sheetRangeThemeService.registerRangeThemeStyle(_tableStyleId, {
        unitId,
        subUnitId,
        range: { ...range }
      });
    }));
    this.disposeWithMe(this._tableManager.tableRangeChanged$.subscribe((event) => {
      const { range, oldRange, tableId, unitId, subUnitId } = event;
      const table = this._tableManager.getTable(unitId, tableId);
      let tableStyleId = table.getTableStyleId();
      if (!tableStyleId) {
        tableStyleId = this._allThemes[this._defaultThemeIndex].name;
        table.setTableStyleId(tableStyleId);
      }
      this._sheetRangeThemeService.removeRangeThemeRule(tableStyleId, {
        unitId,
        subUnitId,
        range: { ...oldRange }
      });
      this._sheetRangeThemeService.registerRangeThemeStyle(tableStyleId, {
        unitId,
        subUnitId,
        range: { ...range }
      });
    }));
    this.disposeWithMe(this._tableManager.tableThemeChanged$.subscribe((event) => {
      const { theme, oldTheme, tableId, unitId, subUnitId } = event;
      const range = this._tableManager.getTable(unitId, tableId).getRange();
      this._sheetRangeThemeService.removeRangeThemeRule(oldTheme, {
        unitId,
        subUnitId,
        range: { ...range }
      });
      this._sheetRangeThemeService.registerRangeThemeStyle(theme, {
        unitId,
        subUnitId,
        range: { ...range }
      });
    }));
    this.disposeWithMe(this._tableManager.tableDelete$.subscribe((event) => {
      const { range, unitId, subUnitId, tableStyleId = this._allThemes[this._defaultThemeIndex].name } = event;
      this._sheetRangeThemeService.removeRangeThemeRule(tableStyleId, {
        unitId,
        subUnitId,
        range: { ...range }
      });
    }));
  }
  _initUserTableTheme() {
    const tableConfig = this._configService.getConfig("sheets-table.config") || {};
    const defaultThemeIndex = tableConfig.defaultThemeIndex || 0;
    const userThemes = tableConfig.userThemes || [];
    this._defaultThemeIndex = defaultThemeIndex;
    this._allThemes = userThemes.concat(tableThemeConfig);
  }
  _initDefaultTableTheme() {
    for (let i = 0; i < this._allThemes.length; i++) {
      const { name: name3, style } = this._allThemes[i];
      const rangeThemeStyle = new RangeThemeStyle(name3, style);
      this._sheetRangeThemeModel.registerDefaultRangeTheme(rangeThemeStyle);
    }
  }
  dispose() {
    super.dispose();
    this._allThemes = [];
    this._defaultThemeIndex = 0;
  }
};
SheetsTableThemeController = __decorate([
  __decorateParam(0, Inject(TableManager)),
  __decorateParam(1, Inject(SheetRangeThemeService)),
  __decorateParam(2, Inject(SheetRangeThemeModel)),
  __decorateParam(3, IConfigService)
], SheetsTableThemeController);
var TableFilterController = class TableFilterController2 extends Disposable {
  constructor(_tableManager, _sheetInterceptorService, _univerInstanceService, _zebraCrossingCacheController) {
    super();
    this._tableManager = _tableManager;
    this._sheetInterceptorService = _sheetInterceptorService;
    this._univerInstanceService = _univerInstanceService;
    this._zebraCrossingCacheController = _zebraCrossingCacheController;
    _defineProperty(this, "_tableFilteredOutRows", /* @__PURE__ */ new Map());
    _defineProperty(this, "_subscription", null);
    this.registerFilterChangeEvent();
    this.initTableHiddenRowIntercept();
    this._initFilteredOutRows();
  }
  initTableHiddenRowIntercept() {
    this.disposeWithMe(this._sheetInterceptorService.intercept(INTERCEPTOR_POINT.ROW_FILTERED, {
      priority: 100,
      handler: (filtered, rowLocation, next) => {
        if (filtered) return true;
        const isTableFiltered = this._getTableFilteredOutRows(rowLocation.unitId, rowLocation.subUnitId).has(rowLocation.row);
        return isTableFiltered ? true : next(isTableFiltered);
      }
    }));
  }
  _initFilteredOutRows() {
    this._tableManager.tableInitStatus$.pipe(filter((initialized) => initialized), switchMap(() => {
      return this._univerInstanceService.getCurrentTypeOfUnit$(UniverType.UNIVER_SHEET);
    }), filter((workbook) => workbook !== null && workbook !== void 0), switchMap((workbook) => workbook.activeSheet$), filter((sheet) => sheet !== null && sheet !== void 0)).subscribe(() => {
      const target = getSheetCommandTarget(this._univerInstanceService);
      if (!target) return;
      const { unitId, subUnitId } = target;
      this._refreshTableFilteredOutRows(unitId, subUnitId);
    });
  }
  registerFilterChangeEvent() {
    this.disposeWithMe(this._tableManager.tableFilterChanged$.subscribe((event) => {
      var _this$_univerInstance;
      const { unitId, subUnitId, tableId } = event;
      const worksheet = (_this$_univerInstance = this._univerInstanceService.getUnit(unitId)) === null || _this$_univerInstance === void 0 ? void 0 : _this$_univerInstance.getSheetBySheetId(subUnitId);
      const table = this._tableManager.getTable(unitId, tableId);
      if (!worksheet || !table) return;
      table.getTableFilters().doFilter(worksheet, table.getTableFilterRange());
      this._refreshTableFilteredOutRows(unitId, subUnitId);
      this._zebraCrossingCacheController.updateZebraCrossingCache(unitId, subUnitId);
    }));
  }
  _refreshTableFilteredOutRows(unitId, subUnitId) {
    const filteredOutRows = /* @__PURE__ */ new Set();
    this._tableManager.getTablesBySubunitId(unitId, subUnitId).forEach((table) => {
      const tableFilteredRows = table.getTableFilters().getFilterOutRows();
      if (!tableFilteredRows) return;
      for (const row of tableFilteredRows) filteredOutRows.add(row);
    });
    this._tableFilteredOutRows.set(this._getSheetKey(unitId, subUnitId), filteredOutRows);
  }
  _getTableFilteredOutRows(unitId, subUnitId) {
    var _this$_tableFilteredO;
    return (_this$_tableFilteredO = this._tableFilteredOutRows.get(this._getSheetKey(unitId, subUnitId))) !== null && _this$_tableFilteredO !== void 0 ? _this$_tableFilteredO : /* @__PURE__ */ new Set();
  }
  _getSheetKey(unitId, subUnitId) {
    return `${unitId}|${subUnitId}`;
  }
  dispose() {
    var _this$_subscription;
    super.dispose();
    (_this$_subscription = this._subscription) === null || _this$_subscription === void 0 || _this$_subscription.unsubscribe();
  }
};
TableFilterController = __decorate([
  __decorateParam(0, Inject(TableManager)),
  __decorateParam(1, Inject(SheetInterceptorService)),
  __decorateParam(2, Inject(IUniverInstanceService)),
  __decorateParam(3, Inject(ZebraCrossingCacheController))
], TableFilterController);
var UniverSheetsTablePlugin = class UniverSheetsTablePlugin2 extends Plugin {
  constructor(_config = defaultPluginConfig, _injector, _configService, _commandService) {
    super();
    this._config = _config;
    this._injector = _injector;
    this._configService = _configService;
    this._commandService = _commandService;
    const { ...rest } = merge_default({}, defaultPluginConfig, this._config);
    this._configService.setConfig(SHEETS_TABLE_PLUGIN_CONFIG_KEY, rest);
    this._initRegisterCommand();
  }
  onStarting() {
    registerDependencies(this._injector, [
      [TableManager],
      [SheetsTableThemeController],
      [SheetsTableController],
      [SheetTableService],
      [TableFilterController],
      [SheetTableRangeController],
      [SheetTableRefRangeController],
      [SheetTableFormulaController]
    ]);
  }
  onReady() {
    touchDependencies(this._injector, [
      [SheetTableFormulaController],
      [SheetTableRangeController],
      [SheetTableRefRangeController],
      [SheetsTableThemeController],
      [SheetsTableController],
      [SheetTableService],
      [TableFilterController]
    ]);
    touchDependencies(this._injector, [[TableManager]]);
  }
  _initRegisterCommand() {
    [
      AddSheetTableCommand,
      AddSheetTableMutation,
      DeleteSheetTableCommand,
      DeleteSheetTableMutation,
      SetSheetTableFilterMutation,
      SetSheetTableFilterCommand,
      SetSheetTableCommand,
      SetSheetTableMutation,
      AddTableThemeCommand,
      RemoveTableThemeCommand,
      SheetTableInsertRowCommand,
      SheetTableInsertColCommand,
      SheetTableInsertRowAtCommand,
      SheetTableInsertColumnAtCommand,
      SheetTableRemoveRowCommand,
      SheetTableRemoveColCommand,
      SheetTableRemoveColumnAtCommand
    ].forEach((m) => this._commandService.registerCommand(m));
  }
};
_defineProperty(UniverSheetsTablePlugin, "pluginName", PLUGIN_NAME);
_defineProperty(UniverSheetsTablePlugin, "packageName", name);
_defineProperty(UniverSheetsTablePlugin, "version", version);
_defineProperty(UniverSheetsTablePlugin, "type", UniverType.UNIVER_SHEET);
UniverSheetsTablePlugin = __decorate([
  __decorateParam(1, Inject(Injector)),
  __decorateParam(2, IConfigService),
  __decorateParam(3, Inject(ICommandService))
], UniverSheetsTablePlugin);

// node_modules/@univerjs/sheets-table-ui/lib/es/index.js
var import_react = __toESM(require_react());
var import_jsx_runtime = __toESM(require_jsx_runtime());
var name2 = "@univerjs/sheets-table-ui";
var version2 = "0.25.1";
var PLUGIN_NAME2 = "SHEET_TABLE_UI_PLUGIN";
var SHEETS_TABLE_FILTER_PANEL_OPENED_KEY = "SHEETS_TABLE_FILTER_PANEL_OPENED_KEY";
var UNIVER_SHEET_TABLE_FILTER_PANEL_ID = "UNIVER_SHEET_Table_FILTER_PANEL_ID";
var TABLE_TOOLBAR_BUTTON = "TABLE_TOOLBAR_BUTTON";
var TABLE_SELECTOR_DIALOG = "TABLE_SELECTOR_DIALOG";
var SHEET_TABLE_RENAME_DIALOG = "SHEET_TABLE_RENAME_DIALOG";
var SHEET_TABLE_RENAME_DIALOG_ID = "SHEET_TABLE_RENAME_DIALOG_ID";
var SHEET_TABLE_THEME_PANEL_ID = "SHEET_TABLE_THEME_PANEL_ID";
var SHEET_TABLE_THEME_PANEL = "SHEET_TABLE_THEME_PANEL";
var TABLE_CUSTOM_NAME_PREFIX = "table-custom-";
var TABLE_DEFAULT_NAME_PREFIX = "table-default-";
var TABLE_DEFAULT_BG_COLOR = "rgb(255, 255, 255)";
var TABLE_BORDER_NONE = "none";
var TABLE_BORDER_DEFAULT = "1px solid rgb(var(--grey-200))";
function _typeof2(o) {
  "@babel/helpers - typeof";
  return _typeof2 = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o2) {
    return typeof o2;
  } : function(o2) {
    return o2 && "function" == typeof Symbol && o2.constructor === Symbol && o2 !== Symbol.prototype ? "symbol" : typeof o2;
  }, _typeof2(o);
}
function toPrimitive2(t, r) {
  if ("object" != _typeof2(t) || !t) return t;
  var e = t[Symbol.toPrimitive];
  if (void 0 !== e) {
    var i = e.call(t, r || "default");
    if ("object" != _typeof2(i)) return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return ("string" === r ? String : Number)(t);
}
function toPropertyKey2(t) {
  var i = toPrimitive2(t, "string");
  return "symbol" == _typeof2(i) ? i : i + "";
}
function _defineProperty2(e, r, t) {
  return (r = toPropertyKey2(r)) in e ? Object.defineProperty(e, r, {
    value: t,
    enumerable: true,
    configurable: true,
    writable: true
  }) : e[r] = t, e;
}
function __decorateParam2(paramIndex, decorator) {
  return function(target, key) {
    decorator(target, key, paramIndex);
  };
}
function __decorate2(decorators, target, key, desc) {
  var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
  if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
  else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
  return c > 3 && r && Object.defineProperty(target, key, r), r;
}
var SheetsTableUiService = class SheetsTableUiService2 extends Disposable {
  constructor(_tableManager, _sheetTableService, _univerInstanceService, _commandService, _localeService) {
    super();
    this._tableManager = _tableManager;
    this._sheetTableService = _sheetTableService;
    this._univerInstanceService = _univerInstanceService;
    this._commandService = _commandService;
    this._localeService = _localeService;
    _defineProperty2(this, "_itemsCache", /* @__PURE__ */ new Map());
    this._registerTableFilterChangeEvent();
  }
  _registerTableFilterChangeEvent() {
    this._commandService.onCommandExecuted((command) => {
      if (command.id === SetRangeValuesMutation.id) {
        const { unitId, subUnitId, cellValue } = command.params;
        const tables = this._tableManager.getTablesBySubunitId(unitId, subUnitId);
        if (!tables.length) return;
        new ObjectMatrix(cellValue).forValue((row, col, _value) => {
          const cellRange = cellToRange(row, col);
          const overlapTable = tables.find((table) => {
            const tableRange = table.getTableFilterRange();
            return Rectangle.intersects(tableRange, cellRange);
          });
          if (overlapTable) {
            const colIndex = col - overlapTable.getRange().startColumn;
            this._itemsCache.delete(overlapTable.getId() + colIndex);
          }
        });
      } else if (command.id === SetSheetTableFilterCommand.id) {
        const { unitId, tableId } = command.params;
        const table = this._tableManager.getTable(unitId, tableId);
        if (!table) return;
        const subUnitId = table.getSubunitId();
        this._tableManager.getTablesBySubunitId(unitId, subUnitId).forEach((table2) => {
          const range = table2.getRange();
          for (let i = range.startColumn; i <= range.endColumn; i++) this._itemsCache.delete(table2.getId() + i);
        });
      }
    });
  }
  getTableFilterPanelInitProps(unitId, subUnitId, tableId, column) {
    const table = this._tableManager.getTable(unitId, tableId);
    const tableRange = table.getRange();
    const tableFilter = table.getTableFilterColumn(column - tableRange.startColumn);
    return {
      unitId,
      subUnitId,
      tableFilter,
      currentFilterBy: isConditionFilter(tableFilter) ? "condition" : "items",
      tableId,
      columnIndex: column - tableRange.startColumn
    };
  }
  getTableFilterCheckedItems(unitId, tableId, columnIndex) {
    const table = this._tableManager.getTable(unitId, tableId);
    const checkedItems = [];
    if (table) {
      const filter2 = table.getTableFilterColumn(columnIndex);
      if (filter2 && isManualTableFilter(filter2)) checkedItems.push(...filter2.values.map((value) => value === TABLE_FILTER_EMPTY_VALUE ? this._localeService.t("sheets-table-ui.condition.empty") : value));
    }
    return checkedItems;
  }
  setTableFilter(unitId, tableId, columnIndex, tableFilter) {
    if (!this._tableManager.getTable(unitId, tableId)) return;
    const setTableFilterParams = {
      unitId,
      tableId,
      column: columnIndex,
      tableFilter
    };
    this._commandService.executeCommand(SetSheetTableFilterCommand.id, setTableFilterParams);
  }
  getTableFilterItems(unitId, subUnitId, tableId, columnIndex) {
    var _this$_univerInstance;
    if (this._itemsCache.has(tableId + columnIndex)) return this._itemsCache.get(tableId + columnIndex) || {
      data: [],
      itemsCountMap: /* @__PURE__ */ new Map(),
      allItemsCount: 0
    };
    const table = this._tableManager.getTable(unitId, tableId);
    if (!table) return {
      data: [],
      itemsCountMap: /* @__PURE__ */ new Map(),
      allItemsCount: 0
    };
    const tableRange = table.getTableFilterRange();
    const { startRow, endRow, startColumn } = tableRange;
    const column = startColumn + columnIndex;
    const worksheet = (_this$_univerInstance = this._univerInstanceService.getUnit(unitId)) === null || _this$_univerInstance === void 0 ? void 0 : _this$_univerInstance.getSheetBySheetId(subUnitId);
    if (!worksheet) return {
      data: [],
      itemsCountMap: /* @__PURE__ */ new Map(),
      allItemsCount: 0
    };
    const data = [];
    const map2 = /* @__PURE__ */ new Map();
    const filteredRowsByOtherColumns = /* @__PURE__ */ new Set();
    const tableFilters = table.getTableFilters();
    for (let i = tableRange.startColumn; i <= tableRange.endColumn; i++) {
      const currentColumnIndex = i - tableRange.startColumn;
      if (currentColumnIndex !== columnIndex && table.getTableFilterColumn(currentColumnIndex)) tableFilters.doColumnFilter(worksheet, tableRange, currentColumnIndex, filteredRowsByOtherColumns);
    }
    let allItemsCount = 0;
    for (let row = startRow; row <= endRow; row++) {
      if (filteredRowsByOtherColumns.has(row)) continue;
      let stringItem = this._sheetTableService.getCellValueWithConditionType(worksheet, row, column);
      if (stringItem == null) stringItem = this._localeService.t("sheets-table-ui.condition.empty");
      if (!map2.has(stringItem)) data.push({
        title: stringItem,
        key: `${column}_${row}`,
        leaf: true
      });
      allItemsCount++;
      map2.set(stringItem, (map2.get(stringItem) || 0) + 1);
    }
    this._itemsCache.set(tableId + columnIndex, {
      data,
      itemsCountMap: map2,
      allItemsCount
    });
    return {
      data,
      itemsCountMap: map2,
      allItemsCount
    };
  }
};
SheetsTableUiService = __decorate2([
  __decorateParam2(0, Inject(TableManager)),
  __decorateParam2(1, Inject(SheetTableService)),
  __decorateParam2(2, Inject(IUniverInstanceService)),
  __decorateParam2(3, ICommandService),
  __decorateParam2(4, Inject(LocaleService))
], SheetsTableUiService);
function getCascaderListOptions(injector) {
  const t = injector.get(LocaleService).t;
  return [
    {
      value: TableConditionTypeEnum.String,
      label: t(`sheets-table-ui.condition.${TableConditionTypeEnum.String}`),
      children: [
        {
          value: TableStringCompareTypeEnum.Equal,
          label: t(`sheets-table-ui.string.compare.${TableStringCompareTypeEnum.Equal}`)
        },
        {
          value: TableStringCompareTypeEnum.NotEqual,
          label: t(`sheets-table-ui.string.compare.${TableStringCompareTypeEnum.NotEqual}`)
        },
        {
          value: TableStringCompareTypeEnum.Contains,
          label: t(`sheets-table-ui.string.compare.${TableStringCompareTypeEnum.Contains}`)
        },
        {
          value: TableStringCompareTypeEnum.NotContains,
          label: t(`sheets-table-ui.string.compare.${TableStringCompareTypeEnum.NotContains}`)
        },
        {
          value: TableStringCompareTypeEnum.StartsWith,
          label: t(`sheets-table-ui.string.compare.${TableStringCompareTypeEnum.StartsWith}`)
        },
        {
          value: TableStringCompareTypeEnum.EndsWith,
          label: t(`sheets-table-ui.string.compare.${TableStringCompareTypeEnum.EndsWith}`)
        }
      ]
    },
    {
      value: TableConditionTypeEnum.Number,
      label: t(`sheets-table-ui.condition.${TableConditionTypeEnum.Number}`),
      children: [
        {
          value: TableNumberCompareTypeEnum.Equal,
          label: t(`sheets-table-ui.number.compare.${TableNumberCompareTypeEnum.Equal}`)
        },
        {
          value: TableNumberCompareTypeEnum.NotEqual,
          label: t(`sheets-table-ui.number.compare.${TableNumberCompareTypeEnum.NotEqual}`)
        },
        {
          value: TableNumberCompareTypeEnum.GreaterThan,
          label: t(`sheets-table-ui.number.compare.${TableNumberCompareTypeEnum.GreaterThan}`)
        },
        {
          value: TableNumberCompareTypeEnum.GreaterThanOrEqual,
          label: t(`sheets-table-ui.number.compare.${TableNumberCompareTypeEnum.GreaterThanOrEqual}`)
        },
        {
          value: TableNumberCompareTypeEnum.LessThan,
          label: t(`sheets-table-ui.number.compare.${TableNumberCompareTypeEnum.LessThan}`)
        },
        {
          value: TableNumberCompareTypeEnum.LessThanOrEqual,
          label: t(`sheets-table-ui.number.compare.${TableNumberCompareTypeEnum.LessThanOrEqual}`)
        },
        {
          value: TableNumberCompareTypeEnum.Between,
          label: t(`sheets-table-ui.number.compare.${TableNumberCompareTypeEnum.Between}`)
        },
        {
          value: TableNumberCompareTypeEnum.NotBetween,
          label: t(`sheets-table-ui.number.compare.${TableNumberCompareTypeEnum.NotBetween}`)
        },
        {
          value: TableNumberCompareTypeEnum.Above,
          label: t(`sheets-table-ui.number.compare.${TableNumberCompareTypeEnum.Above}`)
        },
        {
          value: TableNumberCompareTypeEnum.Below,
          label: t(`sheets-table-ui.number.compare.${TableNumberCompareTypeEnum.Below}`)
        }
      ]
    },
    {
      value: TableConditionTypeEnum.Date,
      label: t(`sheets-table-ui.condition.${TableConditionTypeEnum.Date}`),
      children: [
        {
          value: TableDateCompareTypeEnum.Equal,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.Equal}`)
        },
        {
          value: TableDateCompareTypeEnum.NotEqual,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.NotEqual}`)
        },
        {
          value: TableDateCompareTypeEnum.After,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.After}`)
        },
        {
          value: TableDateCompareTypeEnum.AfterOrEqual,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.AfterOrEqual}`)
        },
        {
          value: TableDateCompareTypeEnum.Before,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.Before}`)
        },
        {
          value: TableDateCompareTypeEnum.BeforeOrEqual,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.BeforeOrEqual}`)
        },
        {
          value: TableDateCompareTypeEnum.Between,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.Between}`)
        },
        {
          value: TableDateCompareTypeEnum.NotBetween,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.NotBetween}`)
        },
        {
          value: TableDateCompareTypeEnum.Today,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.Today}`)
        },
        {
          value: TableDateCompareTypeEnum.Yesterday,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.Yesterday}`)
        },
        {
          value: TableDateCompareTypeEnum.Tomorrow,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.Tomorrow}`)
        },
        {
          value: TableDateCompareTypeEnum.ThisWeek,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.ThisWeek}`)
        },
        {
          value: TableDateCompareTypeEnum.LastWeek,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.LastWeek}`)
        },
        {
          value: TableDateCompareTypeEnum.NextWeek,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.NextWeek}`)
        },
        {
          value: TableDateCompareTypeEnum.ThisMonth,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.ThisMonth}`)
        },
        {
          value: TableDateCompareTypeEnum.LastMonth,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.LastMonth}`)
        },
        {
          value: TableDateCompareTypeEnum.NextMonth,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.NextMonth}`)
        },
        {
          value: TableDateCompareTypeEnum.ThisYear,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.ThisYear}`)
        },
        {
          value: TableDateCompareTypeEnum.LastYear,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.LastYear}`)
        },
        {
          value: TableDateCompareTypeEnum.NextYear,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.NextYear}`)
        },
        {
          value: TableDateCompareTypeEnum.Quarter,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.Quarter}`)
        },
        {
          value: TableDateCompareTypeEnum.Month,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.Month}`)
        }
      ]
    }
  ];
}
function getConditionDateSelect(injector, dateType) {
  if (!dateType) return [];
  const t = injector.get(LocaleService).t;
  switch (dateType) {
    case TableDateCompareTypeEnum.Quarter:
      return [
        {
          value: TableDateCompareTypeEnum.Q1,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.Q1}`)
        },
        {
          value: TableDateCompareTypeEnum.Q2,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.Q2}`)
        },
        {
          value: TableDateCompareTypeEnum.Q3,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.Q3}`)
        },
        {
          value: TableDateCompareTypeEnum.Q4,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.Q4}`)
        }
      ];
    case TableDateCompareTypeEnum.Month:
      return [
        {
          value: TableDateCompareTypeEnum.M1,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.M1}`)
        },
        {
          value: TableDateCompareTypeEnum.M2,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.M2}`)
        },
        {
          value: TableDateCompareTypeEnum.M3,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.M3}`)
        },
        {
          value: TableDateCompareTypeEnum.M4,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.M4}`)
        },
        {
          value: TableDateCompareTypeEnum.M5,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.M5}`)
        },
        {
          value: TableDateCompareTypeEnum.M6,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.M6}`)
        },
        {
          value: TableDateCompareTypeEnum.M7,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.M7}`)
        },
        {
          value: TableDateCompareTypeEnum.M8,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.M8}`)
        },
        {
          value: TableDateCompareTypeEnum.M9,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.M9}`)
        },
        {
          value: TableDateCompareTypeEnum.M10,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.M10}`)
        },
        {
          value: TableDateCompareTypeEnum.M11,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.M11}`)
        },
        {
          value: TableDateCompareTypeEnum.M12,
          label: t(`sheets-table-ui.date.compare.${TableDateCompareTypeEnum.M12}`)
        }
      ];
    default:
      return [];
  }
}
var datePickerSet = /* @__PURE__ */ new Set([
  TableDateCompareTypeEnum.Equal,
  TableDateCompareTypeEnum.NotEqual,
  TableDateCompareTypeEnum.After,
  TableDateCompareTypeEnum.AfterOrEqual,
  TableDateCompareTypeEnum.Before,
  TableDateCompareTypeEnum.BeforeOrEqual
]);
function getSubComponentType(type, compare) {
  if (!compare) return "None";
  if (type === TableConditionTypeEnum.String) return "Input";
  else if (type === TableConditionTypeEnum.Number) if (compare === TableNumberCompareTypeEnum.Between || compare === TableNumberCompareTypeEnum.NotBetween) return "Inputs";
  else return "Input";
  else if (type === TableConditionTypeEnum.Date) {
    if (compare === TableDateCompareTypeEnum.Between || compare === TableDateCompareTypeEnum.NotBetween) return "DateRange";
    else if (compare === TableDateCompareTypeEnum.Quarter || compare === TableDateCompareTypeEnum.Month) return "Select";
    else if (datePickerSet.has(compare)) return "DatePicker";
    return "None";
  }
  return "None";
}
function getInitConditionInfo(tableFilter) {
  if (!tableFilter || tableFilter.filterType !== "condition") return {
    type: TableConditionTypeEnum.String,
    compareType: TableStringCompareTypeEnum.Equal,
    info: {}
  };
  const filterInfo = tableFilter.filterInfo;
  const { conditionType, compareType } = filterInfo;
  if (conditionType === TableConditionTypeEnum.Date) if (compareType === TableDateCompareTypeEnum.Between || compareType === TableDateCompareTypeEnum.NotBetween) {
    let dateRange;
    if (Array.isArray(filterInfo.expectedValue)) dateRange = filterInfo.expectedValue.map((i) => typeof i === "string" ? new Date(i) : i);
    return {
      type: conditionType,
      compare: compareType,
      info: { dateRange }
    };
  } else if (compareType === TableDateCompareTypeEnum.Today || compareType === TableDateCompareTypeEnum.Yesterday || compareType === TableDateCompareTypeEnum.Tomorrow || compareType === TableDateCompareTypeEnum.ThisWeek || compareType === TableDateCompareTypeEnum.LastWeek || compareType === TableDateCompareTypeEnum.NextWeek || compareType === TableDateCompareTypeEnum.ThisMonth || compareType === TableDateCompareTypeEnum.LastMonth || compareType === TableDateCompareTypeEnum.NextMonth || compareType === TableDateCompareTypeEnum.ThisYear || compareType === TableDateCompareTypeEnum.LastYear || compareType === TableDateCompareTypeEnum.NextYear) return {
    type: conditionType,
    compare: compareType,
    info: {}
  };
  else if (datePickerSet.has(compareType)) {
    let date;
    if (typeof filterInfo.expectedValue === "string") date = new Date(filterInfo.expectedValue);
    else if (Array.isArray(filterInfo.expectedValue)) {
      for (let i = 0; i < filterInfo.expectedValue.length; i++) if (typeof filterInfo.expectedValue[i] === "string") filterInfo.expectedValue[i] = new Date(filterInfo.expectedValue[i]);
    }
    return {
      type: conditionType,
      compare: compareType,
      info: { date }
    };
  } else if ((/* @__PURE__ */ new Set([
    TableDateCompareTypeEnum.Q1,
    TableDateCompareTypeEnum.Q2,
    TableDateCompareTypeEnum.Q3,
    TableDateCompareTypeEnum.Q4
  ])).has(compareType)) return {
    type: conditionType,
    compare: TableDateCompareTypeEnum.Quarter,
    info: { dateSelect: filterInfo.compareType }
  };
  else return {
    type: conditionType,
    compare: TableDateCompareTypeEnum.Month,
    info: { dateSelect: filterInfo.compareType }
  };
  else if (conditionType === TableConditionTypeEnum.Number) if (compareType === TableNumberCompareTypeEnum.Between || compareType === TableNumberCompareTypeEnum.NotBetween) return {
    type: conditionType,
    compare: compareType,
    info: { numberRange: filterInfo.expectedValue }
  };
  else return {
    type: conditionType,
    compare: compareType,
    info: { number: filterInfo.expectedValue }
  };
  else if (conditionType === TableConditionTypeEnum.String) return {
    type: conditionType,
    compare: compareType,
    info: { string: filterInfo.expectedValue }
  };
  return {
    type: TableConditionTypeEnum.String,
    compare: TableStringCompareTypeEnum.Equal,
    info: {}
  };
}
var SheetTableConditionPanel = (props) => {
  var _conditionInfo$info$d, _conditionInfo$info$d2, _conditionInfo$info$d3, _conditionInfo$info$d4, _conditionInfo$info$d5, _conditionInfo$info$n, _conditionInfo$info$n3, _conditionInfo$info$d6;
  const { conditionInfo, onChange } = props;
  const localeService = useDependency(LocaleService);
  const [conditionVisible, setConditionVisible] = (0, import_react.useState)(false);
  const injector = useDependency(Injector);
  const cascaderOptions = getCascaderListOptions(injector);
  const handleConditionInfo = (info, type, compare) => {
    onChange({
      type: type !== null && type !== void 0 ? type : conditionInfo.type,
      compare: compare !== null && compare !== void 0 ? compare : conditionInfo.compare,
      info
    });
  };
  const handleChange = (value) => {
    var _ref;
    const type = value[0];
    const compare = value[1];
    if (compare) setConditionVisible(false);
    const info = {};
    if (type === TableConditionTypeEnum.Date) if (compare === TableDateCompareTypeEnum.Quarter) info.dateSelect = TableDateCompareTypeEnum.Q1;
    else if (compare === TableDateCompareTypeEnum.Month) info.dateSelect = TableDateCompareTypeEnum.M1;
    else if (datePickerSet.has(compare)) info.date = /* @__PURE__ */ new Date();
    else info.dateRange = [/* @__PURE__ */ new Date(), /* @__PURE__ */ new Date()];
    else if (type === TableConditionTypeEnum.Number) info.number = 0;
    else if (type === TableConditionTypeEnum.String) info.string = "";
    handleConditionInfo(info, value[0], (_ref = value[1]) !== null && _ref !== void 0 ? _ref : TableStringCompareTypeEnum.Equal);
  };
  const subComponentType = getSubComponentType(conditionInfo.type, conditionInfo.compare);
  let selectType = "";
  if (conditionInfo.compare) selectType = `${localeService.t(`sheets-table-ui.condition.${conditionInfo.type}`)} - ${localeService.t(`sheets-table-ui.${conditionInfo.type}.compare.${conditionInfo.compare}`)}`;
  else selectType = localeService.t(`sheets-table-ui.condition.${conditionInfo.type}`);
  const conditionDateOptions = getConditionDateSelect(injector, conditionInfo.compare);
  return (0, import_jsx_runtime.jsxs)("div", { children: [(0, import_jsx_runtime.jsx)(Dropdown, {
    align: "start",
    open: conditionVisible,
    onOpenChange: setConditionVisible,
    overlay: (0, import_jsx_runtime.jsx)(CascaderList, {
      value: [conditionInfo.type, conditionInfo.compare],
      options: cascaderOptions,
      onChange: handleChange,
      contentClassName: "univer-flex-1",
      wrapperClassName: "!univer-h-[150px]"
    }),
    children: (0, import_jsx_runtime.jsxs)("div", {
      className: clsx("univer-box-border univer-flex univer-h-8 univer-w-full univer-items-center univer-justify-between univer-rounded-md univer-bg-white univer-px-2 univer-text-sm univer-transition-colors univer-duration-200 hover:univer-border-primary-600 focus:univer-border-primary-600 focus:univer-outline-none focus:univer-ring-2 dark:!univer-bg-gray-700 dark:!univer-text-white", borderClassName),
      children: [(0, import_jsx_runtime.jsx)("span", { children: selectType }), (0, import_jsx_runtime.jsx)(MoreDownIcon, {})]
    })
  }), (0, import_jsx_runtime.jsxs)("div", {
    className: "univer-mt-3 univer-w-full",
    children: [
      subComponentType === "Input" && (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: conditionInfo.type === TableConditionTypeEnum.String ? (0, import_jsx_runtime.jsx)(Input, {
        className: "univer-w-full",
        placeholder: "请输入",
        value: conditionInfo.info.string,
        onChange: (v) => handleConditionInfo({ string: v })
      }) : (0, import_jsx_runtime.jsx)(InputNumber, {
        className: "univer-h-7 univer-w-full",
        value: conditionInfo.info.number,
        controls: false,
        onChange: (v) => {
          if (v !== null) handleConditionInfo({ number: v });
        }
      }) }),
      !!(subComponentType === "DatePicker") && (0, import_jsx_runtime.jsx)("div", {
        id: "univer-table-date-picker-wrapper",
        children: (0, import_jsx_runtime.jsx)(DatePicker, {
          className: "univer-w-full",
          value: (_conditionInfo$info$d = conditionInfo.info.date) !== null && _conditionInfo$info$d !== void 0 ? _conditionInfo$info$d : /* @__PURE__ */ new Date(),
          onValueChange: (v) => handleConditionInfo({ date: v })
        })
      }),
      !!(subComponentType === "DateRange") && (0, import_jsx_runtime.jsx)("div", {
        id: "univer-table-date-range-wrapper",
        children: (0, import_jsx_runtime.jsx)(DateRangePicker, {
          className: "univer-w-full",
          value: [(_conditionInfo$info$d2 = (_conditionInfo$info$d3 = conditionInfo.info.dateRange) === null || _conditionInfo$info$d3 === void 0 ? void 0 : _conditionInfo$info$d3[0]) !== null && _conditionInfo$info$d2 !== void 0 ? _conditionInfo$info$d2 : /* @__PURE__ */ new Date(), (_conditionInfo$info$d4 = (_conditionInfo$info$d5 = conditionInfo.info.dateRange) === null || _conditionInfo$info$d5 === void 0 ? void 0 : _conditionInfo$info$d5[1]) !== null && _conditionInfo$info$d4 !== void 0 ? _conditionInfo$info$d4 : /* @__PURE__ */ new Date()],
          onValueChange: (v) => {
            if (v) handleConditionInfo({ dateRange: v });
            else handleConditionInfo({});
          }
        })
      }),
      subComponentType === "Inputs" && (0, import_jsx_runtime.jsxs)("div", {
        className: "univer-flex univer-items-center univer-gap-2",
        children: [
          (0, import_jsx_runtime.jsx)(InputNumber, {
            className: "univer-w-full",
            value: (_conditionInfo$info$n = conditionInfo.info.numberRange) === null || _conditionInfo$info$n === void 0 ? void 0 : _conditionInfo$info$n[0],
            onChange: (v) => {
              if (v !== null) {
                var _conditionInfo$info$n2;
                handleConditionInfo({ numberRange: [v, (_conditionInfo$info$n2 = conditionInfo.info.numberRange) === null || _conditionInfo$info$n2 === void 0 ? void 0 : _conditionInfo$info$n2[1]] });
              }
            },
            controls: false
          }),
          (0, import_jsx_runtime.jsx)("span", { children: " - " }),
          (0, import_jsx_runtime.jsx)(InputNumber, {
            className: "univer-w-full",
            value: (_conditionInfo$info$n3 = conditionInfo.info.numberRange) === null || _conditionInfo$info$n3 === void 0 ? void 0 : _conditionInfo$info$n3[1],
            controls: false,
            onChange: (v) => {
              if (v !== null) {
                var _conditionInfo$info$n4;
                handleConditionInfo({ numberRange: [(_conditionInfo$info$n4 = conditionInfo.info.numberRange) === null || _conditionInfo$info$n4 === void 0 ? void 0 : _conditionInfo$info$n4[0], v] });
              }
            }
          })
        ]
      }),
      !!(subComponentType === "Select") && (0, import_jsx_runtime.jsx)(Select, {
        className: "univer-w-full",
        value: (_conditionInfo$info$d6 = conditionInfo.info.dateSelect) !== null && _conditionInfo$info$d6 !== void 0 ? _conditionInfo$info$d6 : conditionDateOptions[0].value,
        options: conditionDateOptions,
        onChange: (v) => handleConditionInfo({ dateSelect: v })
      })
    ]
  })] });
};
var getCheckedCount = (map2) => {
  let count = 0;
  map2.forEach((value) => {
    count += value;
  });
  return count;
};
function SheetTableItemsFilterPanel(props) {
  const { unitId, tableId, subUnitId, columnIndex, checkedItemSet, setCheckedItemSet, tableFilter } = props;
  const localeService = useDependency(LocaleService);
  const { data: items, itemsCountMap, allItemsCount } = useDependency(SheetsTableUiService).getTableFilterItems(unitId, subUnitId, tableId, columnIndex);
  const [allChecked, setAllChecked] = (0, import_react.useState)(tableFilter === void 0 ? true : checkedItemSet.size === itemsCountMap.size);
  const [checkedCount, setCheckedCount] = (0, import_react.useState)(allChecked ? allItemsCount : getCheckedCount(itemsCountMap));
  const indeterminate = !allChecked && checkedItemSet.size > 0;
  const [searchText, setSearchText] = (0, import_react.useState)("");
  const displayItems = (0, import_react.useMemo)(() => {
    return searchText ? items.filter((item) => {
      return String(item.title).toLowerCase().includes(searchText.toLowerCase());
    }) : items;
  }, [searchText, items]);
  const onCheckAllToggled = (0, import_react.useCallback)(() => {
    if (allChecked) {
      checkedItemSet.clear();
      setCheckedItemSet(new Set(checkedItemSet));
      setAllChecked(false);
    } else {
      displayItems.forEach((item) => {
        checkedItemSet.add(item.title);
      });
      setCheckedItemSet(new Set(checkedItemSet));
      setAllChecked(true);
    }
  }, [allChecked]);
  const onSearchValueChange = (0, import_react.useCallback)((str) => {
    if (str === "") {
      setAllChecked(true);
      items.forEach((item) => {
        checkedItemSet.add(item.title);
      });
      setCheckedCount(allItemsCount);
    } else {
      checkedItemSet.clear();
      setAllChecked(false);
      setCheckedCount(0);
    }
    setSearchText(str);
  }, []);
  const onCheckItemToggled = (key) => {
    if (allChecked) {
      setAllChecked(false);
      const newSet = /* @__PURE__ */ new Set();
      for (const { title } of items) if (key !== title) newSet.add(title);
      setCheckedCount(allItemsCount - itemsCountMap.get(key));
      setCheckedItemSet(newSet);
    } else {
      if (checkedItemSet.has(key)) {
        checkedItemSet.delete(key);
        setCheckedCount(checkedCount - itemsCountMap.get(key));
      } else {
        checkedItemSet.add(key);
        setCheckedCount(checkedCount + itemsCountMap.get(key));
      }
      setCheckedItemSet(new Set(checkedItemSet));
    }
  };
  return (0, import_jsx_runtime.jsxs)("div", {
    className: "univer-flex univer-h-full univer-flex-col",
    children: [(0, import_jsx_runtime.jsx)(Input, {
      autoFocus: true,
      value: searchText,
      placeholder: localeService.t("sheets-table-ui.filter.search-placeholder"),
      onChange: onSearchValueChange
    }), (0, import_jsx_runtime.jsx)("div", {
      className: clsx("univer-mt-2 univer-box-border univer-flex univer-h-[180px] univer-max-h-[180px] univer-flex-grow univer-flex-col univer-overflow-hidden univer-rounded-md univer-py-1.5 univer-pl-2", borderClassName),
      children: (0, import_jsx_runtime.jsx)("div", {
        className: clsx("univer-h-40 univer-min-w-0 univer-overflow-y-auto univer-py-1 univer-pl-2", scrollbarClassName),
        children: (0, import_jsx_runtime.jsxs)("div", {
          className: "univer-h-full",
          children: [(0, import_jsx_runtime.jsx)("div", {
            className: "univer-flex univer-items-center univer-px-2 univer-py-1",
            children: (0, import_jsx_runtime.jsx)(Checkbox, {
              className: "univer-min-w-0 univer-flex-1",
              contentClassName: "univer-flex-1 univer-min-w-0",
              indeterminate,
              disabled: items.length === 0,
              checked: allChecked,
              onChange: onCheckAllToggled,
              children: (0, import_jsx_runtime.jsxs)("div", {
                className: "univer-flex univer-h-5 univer-flex-1 univer-items-center univer-text-sm",
                children: [(0, import_jsx_runtime.jsx)("span", {
                  className: "univer-flex-1 univer-truncate",
                  children: `${localeService.t("sheets-table-ui.filter.select-all")}`
                }), (0, import_jsx_runtime.jsx)("span", {
                  className: "univer-ml univer-text-gray-400",
                  children: `(${checkedCount}/${searchText ? displayItems.length : allItemsCount})`
                })]
              })
            })
          }), displayItems.map((item) => {
            return (0, import_jsx_runtime.jsx)("div", {
              className: "univer-flex univer-items-center univer-px-2 univer-py-1",
              children: (0, import_jsx_runtime.jsx)(Checkbox, {
                className: "univer-min-w-0 univer-flex-1",
                contentClassName: "univer-flex-1 univer-min-w-0",
                checked: allChecked || checkedItemSet.has(item.title),
                onChange: () => {
                  onCheckItemToggled(item.title);
                },
                children: (0, import_jsx_runtime.jsxs)("span", {
                  className: "univer-flex univer-h-5 univer-flex-1 univer-items-center univer-text-sm",
                  children: [(0, import_jsx_runtime.jsx)("span", {
                    className: "univer-flex-1 univer-truncate",
                    children: item.title
                  }), (0, import_jsx_runtime.jsx)("span", {
                    className: "univer-ml-1 univer-inline-flex univer-h-full univer-items-center univer-text-gray-400",
                    children: `(${itemsCountMap.get(item.title) || 0})`
                  })]
                })
              })
            }, item.key);
          })]
        })
      })
    })]
  });
}
function SheetTableFilterPanel() {
  var _permissionService$ge;
  const localeService = useDependency(LocaleService);
  const filterByItems = useFilterByOptions(localeService);
  const tableUiService = useDependency(SheetsTableUiService);
  const tableManager = useDependency(TableManager);
  const commandService = useDependency(ICommandService);
  const permissionService = useDependency(IPermissionService);
  const sheetsTableComponentController = useDependency(SheetsTableComponentController);
  const tableFilterPanelInfo = sheetsTableComponentController.getCurrentTableFilterInfo();
  const props = tableUiService.getTableFilterPanelInitProps(tableFilterPanelInfo.unitId, tableFilterPanelInfo.subUnitId, tableFilterPanelInfo.tableId, tableFilterPanelInfo.column);
  const { unitId, subUnitId, tableId, tableFilter, currentFilterBy, columnIndex } = props;
  const { data } = tableUiService.getTableFilterItems(unitId, subUnitId, tableId, columnIndex);
  const checkedItems = tableUiService.getTableFilterCheckedItems(unitId, tableId, columnIndex);
  const [checkedItemSet, setCheckedItemSet] = (0, import_react.useState)(new Set(checkedItems));
  const [filterBy, setFilterBy] = (0, import_react.useState)(currentFilterBy || "items");
  const [conditionInfo, setConditionInfo] = (0, import_react.useState)(() => {
    const tableFilter2 = props.tableFilter;
    return getInitConditionInfo(tableFilter2);
  });
  const table = tableManager.getTable(unitId, tableId);
  if (!table) return null;
  const tableFilters = table.getTableFilters();
  const tableRange = table.getRange();
  const sortState = tableFilters.getSortState();
  sortState.columnIndex === columnIndex && (sortState.sortState, SheetsTableSortStateEnum.Asc);
  sortState.columnIndex === columnIndex && (sortState.sortState, SheetsTableSortStateEnum.Desc);
  const absoluteColumn = tableFilterPanelInfo.column;
  const canDeleteColumn = tableRange.endColumn > tableRange.startColumn;
  const closeDialog = () => {
    sheetsTableComponentController.closeFilterPanel();
  };
  const onCancel = () => {
    closeDialog();
  };
  const applySort = (asc) => {
    const range = table.getTableFilterRange();
    commandService.executeCommand(SortRangeCommand.id, {
      unitId,
      subUnitId,
      range,
      orderRules: [{
        colIndex: columnIndex + range.startColumn,
        type: asc ? SortType.ASC : SortType.DESC
      }],
      hasTitle: false
    });
    tableFilters.setSortState(columnIndex, asc ? SheetsTableSortStateEnum.Asc : SheetsTableSortStateEnum.Desc);
    closeDialog();
  };
  const insertColumn = (side) => {
    commandService.executeCommand(SheetTableInsertColumnAtCommand.id, {
      unitId,
      subUnitId,
      tableId,
      index: side === "left" ? absoluteColumn : absoluteColumn + 1,
      count: 1
    });
    closeDialog();
  };
  const deleteColumn = () => {
    if (!canDeleteColumn) return;
    commandService.executeCommand(SheetTableRemoveColumnAtCommand.id, {
      unitId,
      subUnitId,
      tableId,
      index: absoluteColumn,
      count: 1
    });
    closeDialog();
  };
  const onApply = () => {
    if (filterBy === "items") {
      const filteredItems = [];
      const emptyLabel = localeService.t("sheets-table-ui.condition.empty");
      for (const itemInfo of data) if (checkedItemSet.has(itemInfo.title)) filteredItems.push(itemInfo.title === emptyLabel ? TABLE_FILTER_EMPTY_VALUE : itemInfo.title);
      const originFilter = table.getTableFilterColumn(columnIndex);
      if (originFilter) {
        if (originFilter.values.join(",") === filteredItems.join(",")) {
          closeDialog();
          return;
        }
      } else if (filteredItems.length === 0) {
        closeDialog();
        return;
      }
      const tableFilter2 = {
        filterType: TableColumnFilterTypeEnum.manual,
        values: filteredItems
      };
      tableUiService.setTableFilter(unitId, tableId, columnIndex, tableFilter2);
    } else {
      let filterInfo;
      if (conditionInfo.compare === TableDateCompareTypeEnum.Quarter || conditionInfo.compare === TableDateCompareTypeEnum.Month) filterInfo = {
        conditionType: conditionInfo.type,
        compareType: Object.values(conditionInfo.info)[0]
      };
      else filterInfo = {
        conditionType: conditionInfo.type,
        compareType: conditionInfo.compare,
        expectedValue: Object.values(conditionInfo.info)[0]
      };
      const tableFilter2 = {
        filterType: TableColumnFilterTypeEnum.condition,
        filterInfo
      };
      tableUiService.setTableFilter(unitId, tableId, columnIndex, tableFilter2);
    }
    closeDialog();
  };
  const onClearFilter = () => {
    tableUiService.setTableFilter(unitId, tableId, columnIndex, void 0);
    closeDialog();
  };
  const workbookEditableId = new WorkbookEditablePermission(unitId).id;
  return (0, import_jsx_runtime.jsxs)("div", {
    className: "univer-box-border univer-flex univer-w-[400px] univer-flex-col univer-rounded-[10px] univer-bg-white univer-p-4 univer-shadow-lg dark:!univer-border-gray-600 dark:!univer-bg-gray-700",
    children: [
      ((_permissionService$ge = permissionService.getPermissionPoint(workbookEditableId)) === null || _permissionService$ge === void 0 ? void 0 : _permissionService$ge.value) && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [(0, import_jsx_runtime.jsxs)("div", {
        className: "-univer-mx-4 -univer-mt-2 univer-mb-3 univer-border-0 univer-border-b univer-border-solid univer-border-gray-200 univer-py-1",
        children: [
          (0, import_jsx_runtime.jsxs)("button", {
            type: "button",
            className: "univer-box-border univer-flex univer-h-10 univer-w-full univer-cursor-pointer univer-items-center univer-gap-3 univer-border-none univer-bg-transparent univer-px-4 univer-text-left univer-text-sm univer-text-gray-900 hover:univer-bg-gray-100 disabled:univer-cursor-not-allowed disabled:univer-text-gray-400 dark:!univer-text-white dark:hover:!univer-bg-gray-600",
            onClick: () => insertColumn("left"),
            children: [(0, import_jsx_runtime.jsx)(LeftInsertColumnDoubleIcon, {
              className: "univer-size-5",
              extend: { colorChannel1: "var(--univer-primary-600)" }
            }), (0, import_jsx_runtime.jsx)("span", { children: localeService.t("sheets-table-ui.columnMenu.insert-left") })]
          }),
          (0, import_jsx_runtime.jsxs)("button", {
            type: "button",
            className: "univer-box-border univer-flex univer-h-10 univer-w-full univer-cursor-pointer univer-items-center univer-gap-3 univer-border-none univer-bg-transparent univer-px-4 univer-text-left univer-text-sm univer-text-gray-900 hover:univer-bg-gray-100 disabled:univer-cursor-not-allowed disabled:univer-text-gray-400 dark:!univer-text-white dark:hover:!univer-bg-gray-600",
            onClick: () => insertColumn("right"),
            children: [(0, import_jsx_runtime.jsx)(RightInsertColumnDoubleIcon, {
              className: "univer-size-5",
              extend: { colorChannel1: "var(--univer-primary-600)" }
            }), (0, import_jsx_runtime.jsx)("span", { children: localeService.t("sheets-table-ui.columnMenu.insert-right") })]
          }),
          (0, import_jsx_runtime.jsxs)("button", {
            type: "button",
            className: "univer-box-border univer-flex univer-h-10 univer-w-full univer-cursor-pointer univer-items-center univer-gap-3 univer-border-none univer-bg-transparent univer-px-4 univer-text-left univer-text-sm univer-text-gray-900 hover:univer-bg-gray-100 disabled:univer-cursor-not-allowed disabled:univer-text-gray-400 dark:!univer-text-white dark:hover:!univer-bg-gray-600",
            disabled: !canDeleteColumn,
            onClick: deleteColumn,
            children: [(0, import_jsx_runtime.jsx)(DeleteColumnDoubleIcon, {
              className: "univer-size-5",
              extend: { colorChannel1: "var(--univer-primary-600)" }
            }), (0, import_jsx_runtime.jsx)("span", { children: localeService.t("sheets-table-ui.columnMenu.delete") })]
          })
        ]
      }), (0, import_jsx_runtime.jsx)("div", {
        className: "univer-mb-3 univer-flex",
        children: (0, import_jsx_runtime.jsxs)(ButtonGroup, {
          className: "univer-mb-3 !univer-flex univer-w-full",
          children: [(0, import_jsx_runtime.jsxs)(Button, {
            className: "univer-w-1/2",
            onClick: () => applySort(true),
            children: [(0, import_jsx_runtime.jsx)(AscendingIcon, { className: "univer-mr-1" }), localeService.t("sheets-table-ui.sort.sort-asc")]
          }), (0, import_jsx_runtime.jsxs)(Button, {
            className: "univer-w-1/2",
            onClick: () => applySort(false),
            children: [(0, import_jsx_runtime.jsx)(DescendingIcon, { className: "univer-mr-1" }), localeService.t("sheets-table-ui.sort.sort-desc")]
          })]
        })
      })] }),
      (0, import_jsx_runtime.jsx)("div", {
        className: "univer-w-full",
        children: (0, import_jsx_runtime.jsx)(Segmented, {
          value: filterBy,
          items: filterByItems,
          onChange: (value) => setFilterBy(value)
        })
      }),
      (0, import_jsx_runtime.jsx)("div", {
        className: "univer-z-10 univer-h-60",
        children: (0, import_jsx_runtime.jsx)("div", {
          className: "univer-mt-3 univer-size-full",
          children: filterBy === "items" ? (0, import_jsx_runtime.jsx)(SheetTableItemsFilterPanel, {
            tableFilter,
            unitId,
            subUnitId,
            tableId,
            columnIndex,
            checkedItemSet,
            setCheckedItemSet
          }) : (0, import_jsx_runtime.jsx)(SheetTableConditionPanel, {
            tableFilter,
            unitId,
            subUnitId,
            tableId,
            columnIndex,
            conditionInfo,
            onChange: setConditionInfo
          })
        })
      }),
      (0, import_jsx_runtime.jsxs)("div", {
        className: "univer-flex-wrap-nowrap univer-mt-4 univer-inline-flex univer-flex-shrink-0 univer-flex-grow-0 univer-justify-between univer-gap-6 univer-overflow-hidden",
        children: [(0, import_jsx_runtime.jsx)(Button, {
          disabled: tableFilter === void 0,
          onClick: onClearFilter,
          children: localeService.t("sheets-table-ui.filter.clear-filter")
        }), (0, import_jsx_runtime.jsxs)("div", { children: [(0, import_jsx_runtime.jsx)(Button, {
          className: "univer-mr-2",
          onClick: onCancel,
          children: localeService.t("sheets-table-ui.filter.cancel")
        }), (0, import_jsx_runtime.jsx)(Button, {
          variant: "primary",
          onClick: onApply,
          children: localeService.t("sheets-table-ui.filter.confirm")
        })] })]
      })
    ]
  });
}
function useFilterByOptions(localeService) {
  return (0, import_react.useMemo)(() => [{
    label: localeService.t("sheets-table-ui.filter.by-values"),
    value: "items"
  }, {
    label: localeService.t("sheets-table-ui.filter.by-conditions"),
    value: "condition"
  }], [localeService.getCurrentLocale(), localeService]);
}
function SheetTableRenameDialog(props) {
  var _table$getDisplayName;
  const { unitId, tableId, onClose } = props;
  const localeService = useDependency(LocaleService);
  const commandService = useDependency(ICommandService);
  const tableManager = useDependency(TableManager);
  const univerInstanceService = useDependency(IUniverInstanceService);
  const definedNamesService = useDependency(IDefinedNamesService);
  const table = tableManager.getTableById(unitId, tableId);
  const [value, setValue] = (0, import_react.useState)((_table$getDisplayName = table === null || table === void 0 ? void 0 : table.getDisplayName()) !== null && _table$getDisplayName !== void 0 ? _table$getDisplayName : "");
  const [error, setError] = (0, import_react.useState)("");
  const existingNames = (0, import_react.useMemo)(() => {
    const names = getExistingNamesSet(unitId, {
      univerInstanceService,
      tableManager,
      definedNamesService
    });
    const currentName = table === null || table === void 0 ? void 0 : table.getDisplayName().toLowerCase();
    if (currentName) names.delete(currentName);
    return names;
  }, [
    definedNamesService,
    table,
    tableManager,
    unitId,
    univerInstanceService
  ]);
  const handleConfirm = () => {
    const nextName = value.trim();
    if (!table || nextName === table.getDisplayName()) {
      onClose();
      return;
    }
    if (!validateSheetTableName(nextName, existingNames).valid) {
      setError(localeService.t("sheets-table-ui.tableNameError"));
      return;
    }
    commandService.executeCommand(SetSheetTableCommand.id, {
      unitId,
      tableId,
      name: nextName
    });
    onClose();
  };
  return (0, import_jsx_runtime.jsxs)("div", {
    className: "univer-box-border univer-flex univer-w-full univer-flex-col univer-gap-4 univer-pb-3 univer-pt-2",
    children: [
      (0, import_jsx_runtime.jsx)(Input, {
        size: "middle",
        value,
        placeholder: localeService.t("sheets-table-ui.renamePlaceholder"),
        onChange: (nextValue) => {
          setValue(nextValue);
          setError("");
        },
        onKeyDown: (event) => {
          if (event.key === "Enter") handleConfirm();
        },
        autoFocus: true
      }),
      error ? (0, import_jsx_runtime.jsx)("div", {
        className: "-univer-mt-2 univer-text-sm univer-text-red-500",
        children: error
      }) : null,
      (0, import_jsx_runtime.jsxs)("div", {
        className: "univer-flex univer-w-full univer-items-center univer-justify-end univer-gap-2",
        children: [(0, import_jsx_runtime.jsx)(Button, {
          className: "univer-min-w-16",
          onClick: onClose,
          children: localeService.t("sheets-table-ui.cancel")
        }), (0, import_jsx_runtime.jsx)(Button, {
          className: "univer-min-w-16",
          variant: "primary",
          onClick: handleConfirm,
          children: localeService.t("sheets-table-ui.confirm")
        })]
      })
    ]
  });
}
var SheetsTableComponentController = class SheetsTableComponentController2 extends Disposable {
  constructor(_componentManager, _contextService, _sheetCanvasPopupService, _dialogService) {
    super();
    this._componentManager = _componentManager;
    this._contextService = _contextService;
    this._sheetCanvasPopupService = _sheetCanvasPopupService;
    this._dialogService = _dialogService;
    _defineProperty2(this, "_popupDisposable", void 0);
    _defineProperty2(this, "_currentTableFilterInfo", null);
    this._initComponents();
    this._initUIPopup();
  }
  setCurrentTableFilterInfo(info) {
    this._currentTableFilterInfo = info;
  }
  openOrToggleFilterPanel(info) {
    const opened = this._contextService.getContextValue(SHEETS_TABLE_FILTER_PANEL_OPENED_KEY);
    if (opened && this._isSameFilterPanelInfo(this._currentTableFilterInfo, info)) {
      this.closeFilterPanel();
      return;
    }
    this.setCurrentTableFilterInfo(info);
    if (opened) {
      var _this$_popupDisposabl;
      (_this$_popupDisposabl = this._popupDisposable) === null || _this$_popupDisposabl === void 0 || _this$_popupDisposabl.dispose();
      this._popupDisposable = null;
      this._openFilterPopup();
      return;
    }
    this._contextService.setContextValue(SHEETS_TABLE_FILTER_PANEL_OPENED_KEY, true);
  }
  clearCurrentTableFilterInfo() {
    this._currentTableFilterInfo = null;
  }
  getCurrentTableFilterInfo() {
    return this._currentTableFilterInfo;
  }
  _initComponents() {
    [[SHEETS_TABLE_FILTER_PANEL_OPENED_KEY, SheetTableFilterPanel], [SHEET_TABLE_RENAME_DIALOG, SheetTableRenameDialog]].forEach(([key, comp]) => {
      this.disposeWithMe(this._componentManager.register(key, comp));
    });
  }
  _initUIPopup() {
    this.disposeWithMe(this._contextService.subscribeContextValue$(SHEETS_TABLE_FILTER_PANEL_OPENED_KEY).pipe(startWith(void 0), distinctUntilChanged()).subscribe((open) => {
      if (open) this._openFilterPopup();
      else if (open === false) this._closeFilterPopup();
    }));
  }
  closeFilterPanel() {
    this._contextService.setContextValue(SHEETS_TABLE_FILTER_PANEL_OPENED_KEY, false);
  }
  _openFilterPopup() {
    const currentFilterModel = this._currentTableFilterInfo;
    if (!currentFilterModel) throw new Error("[SheetsFilterUIController]: no filter model when opening filter popup!");
    const { row: startRow, column: col } = currentFilterModel;
    this._popupDisposable = this._sheetCanvasPopupService.attachPopupToCell(startRow, col, {
      componentKey: SHEETS_TABLE_FILTER_PANEL_OPENED_KEY,
      direction: "horizontal",
      onClickOutside: () => {
        this._dialogService.close(UNIVER_SHEET_TABLE_FILTER_PANEL_ID);
        this._contextService.setContextValue(SHEETS_TABLE_FILTER_PANEL_OPENED_KEY, false);
      },
      offset: [5, 0],
      portal: true
    });
  }
  _closeFilterPopup() {
    var _this$_popupDisposabl2;
    (_this$_popupDisposabl2 = this._popupDisposable) === null || _this$_popupDisposabl2 === void 0 || _this$_popupDisposabl2.dispose();
    this._popupDisposable = null;
    this.clearCurrentTableFilterInfo();
  }
  _isSameFilterPanelInfo(a, b) {
    return Boolean(a && a.unitId === b.unitId && a.subUnitId === b.subUnitId && a.tableId === b.tableId && a.column === b.column && a.row === b.row);
  }
};
SheetsTableComponentController = __decorate2([
  __decorateParam2(0, Inject(ComponentManager)),
  __decorateParam2(1, IContextService),
  __decorateParam2(2, Inject(SheetCanvasPopManagerService)),
  __decorateParam2(3, Inject(IDialogService))
], SheetsTableComponentController);
var OpenTableFilterPanelOperation = {
  type: CommandType.OPERATION,
  id: "sheet.operation.open-table-filter-panel",
  async handler(accessor, params) {
    if (!params) return false;
    const { row, col, unitId, subUnitId, tableId } = params;
    const tableManager = accessor.get(TableManager);
    const sheetsTableComponentController = accessor.get(SheetsTableComponentController);
    if (!tableManager.getTable(unitId, tableId)) return false;
    sheetsTableComponentController.openOrToggleFilterPanel({
      unitId,
      subUnitId,
      row,
      tableId,
      column: col
    });
    return true;
  }
};
var OpenTableSelectorOperation = {
  type: CommandType.OPERATION,
  id: "sheet.operation.open-table-selector",
  async handler(accessor) {
    var _lastSelection$range;
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const target = getSheetCommandTarget(univerInstanceService);
    if (!target) return false;
    const { unitId, subUnitId, worksheet } = target;
    const lastSelection = accessor.get(SheetsSelectionsService).getCurrentLastSelection();
    const range = (_lastSelection$range = lastSelection === null || lastSelection === void 0 ? void 0 : lastSelection.range) !== null && _lastSelection$range !== void 0 ? _lastSelection$range : {
      startRow: 0,
      endRow: 0,
      startColumn: 0,
      endColumn: 0
    };
    const rangeInfo = await openRangeSelector(accessor, unitId, subUnitId, isSingleCellSelection(lastSelection) ? expandToContinuousRange(range, {
      up: true,
      left: true,
      right: true,
      down: true
    }, worksheet) : range);
    if (!rangeInfo) return false;
    commandService.executeCommand(AddSheetTableCommand.id, { ...rangeInfo });
    return true;
  }
};
async function openRangeSelector(accessor, unitId, subUnitId, range, tableId) {
  const dialogService = accessor.get(IDialogService);
  const localeService = accessor.get(LocaleService);
  return new Promise((resolve) => {
    const dialogProps = {
      unitId,
      subUnitId,
      range,
      tableId,
      onConfirm: (info) => {
        resolve(info);
        dialogService.close(TABLE_SELECTOR_DIALOG);
      },
      onCancel: () => {
        resolve(null);
        dialogService.close(TABLE_SELECTOR_DIALOG);
      }
    };
    dialogService.open({
      id: TABLE_SELECTOR_DIALOG,
      title: { title: localeService.t("sheets-table-ui.selectRange") },
      draggable: true,
      destroyOnClose: true,
      mask: false,
      maskClosable: false,
      children: { label: {
        name: TABLE_SELECTOR_DIALOG,
        props: dialogProps
      } },
      width: 300,
      onClose: () => {
        resolve(null);
        dialogService.close(TABLE_SELECTOR_DIALOG);
      }
    });
  });
}
var SHEETS_TABLE_UI_PLUGIN_CONFIG_KEY = "sheets-table-ui.config";
var configSymbol2 = Symbol(SHEETS_TABLE_UI_PLUGIN_CONFIG_KEY);
var defaultPluginConfig2 = {
  anchorHeight: 24,
  anchorBackgroundColor: "rgb(134,139,156)"
};
var TABLE_CONTROL_MENU_ACTIONS = [
  "rename",
  "update-range",
  "set-theme",
  "delete"
];
function isPointInTableControlRegion(region, x, y) {
  return x >= region.left && x <= region.left + region.width && y >= region.top && y <= region.top + region.height;
}
function hitTestTableControl(regions, x, y) {
  for (let i = regions.length - 1; i >= 0; i--) if (isPointInTableControlRegion(regions[i], x, y)) return regions[i];
  return null;
}
function buildTableMenuRegions(tableId, left, top) {
  return TABLE_CONTROL_MENU_ACTIONS.map((action, index) => ({
    type: "menu-item",
    tableId,
    action,
    left,
    top: top + index * 32,
    width: 168,
    height: 32
  }));
}
function buildCenteredPlusSegments(centerX, centerY, size) {
  const halfSize = size / 2;
  return [{
    fromX: centerX - halfSize,
    fromY: centerY,
    toX: centerX + halfSize,
    toY: centerY
  }, {
    fromX: centerX,
    fromY: centerY - halfSize,
    toX: centerX,
    toY: centerY + halfSize
  }];
}
var ANCHOR_MIN_WIDTH = 122;
var ANCHOR_MAX_WIDTH = 240;
var ANCHOR_PADDING_X = 12;
var ANCHOR_TOGGLE_WIDTH = 30;
var ANCHOR_OFFSET_Y = 0;
var ANCHOR_BORDER = "rgba(0, 0, 0, 0.22)";
var ANCHOR_DIVIDER = "rgba(0, 0, 0, 0.20)";
var ANCHOR_TOGGLE_BG_ACTIVE = "rgba(0, 0, 0, 0.12)";
var MENU_RADIUS = 8;
var MENU_BORDER = "#d9dee7";
var MENU_HOVER_BG = "#f1f3f4";
var INSERT_BUTTON_VISUAL_SIZE = 18;
var INSERT_BUTTON_PLUS_SIZE = 8;
var SheetTableControlsShape = class extends Shape {
  constructor(key, _getSkeleton) {
    super(key, {
      evented: true,
      fill: "rgba(0, 0, 0, 0)",
      zIndex: 5001
    });
    this._getSkeleton = _getSkeleton;
    _defineProperty2(this, "_items", []);
    _defineProperty2(this, "_regions", []);
    _defineProperty2(this, "_openedMenuTableId", null);
    _defineProperty2(this, "_hoveredRegion", null);
    _defineProperty2(this, "_hoveredInsertRegion", null);
    _defineProperty2(this, "_menuLabels", {
      rename: "Rename table",
      "update-range": "Update range",
      "set-theme": "Set theme",
      delete: "Remove table"
    });
  }
  setItems(items) {
    this._items = items;
    this.makeDirty(true);
  }
  setMenuLabels(labels) {
    this._menuLabels = labels;
    this.makeDirty(true);
  }
  setOpenedMenuTableId(tableId) {
    if (this._openedMenuTableId === tableId) return;
    this._openedMenuTableId = tableId;
    this.makeDirty(true);
  }
  getOpenedMenuTableId() {
    return this._openedMenuTableId;
  }
  setHoveredRegion(region) {
    if (this._hoveredRegion === region) return;
    this._hoveredRegion = region;
    this.makeDirty(true);
  }
  setHoveredInsertRegion(region) {
    if (this._hoveredInsertRegion === region) return;
    this._hoveredInsertRegion = region;
    this.makeDirty(true);
  }
  hitTest(x, y) {
    return hitTestTableControl(this._regions, x, y);
  }
  isHit(coord) {
    return this.hitTest(coord.x, coord.y) != null;
  }
  refreshBounds() {
    const skeleton = this._getSkeleton();
    if (!skeleton) {
      this.hide();
      return;
    }
    this.show();
    this.transformByState({
      left: 0,
      top: 0,
      width: skeleton.rowHeaderWidth + skeleton.columnTotalWidth,
      height: skeleton.columnHeaderHeight + skeleton.rowTotalHeight
    });
  }
  _draw(ctx) {
    this._regions = [];
    const skeleton = this._getSkeleton();
    if (!skeleton) return;
    ctx.save();
    ctx.textBaseline = "middle";
    for (const item of this._items) this._drawAnchor(ctx, skeleton, item);
    if (this._hoveredInsertRegion) {
      var _item$fill;
      const item = this._items.find((renderItem) => {
        var _this$_hoveredInsertR;
        return renderItem.tableId === ((_this$_hoveredInsertR = this._hoveredInsertRegion) === null || _this$_hoveredInsertR === void 0 ? void 0 : _this$_hoveredInsertR.tableId);
      });
      this._drawInsertButton(ctx, this._hoveredInsertRegion, (_item$fill = item === null || item === void 0 ? void 0 : item.fill) !== null && _item$fill !== void 0 ? _item$fill : "#355bb7");
      this._regions.push(this._hoveredInsertRegion);
    }
    ctx.restore();
  }
  _drawAnchor(ctx, skeleton, item) {
    const position = skeleton.getNoMergeCellWithCoordByIndex(item.range.startRow, item.range.startColumn);
    const left = position.startX;
    const rawTop = position.startY - 28 - ANCHOR_OFFSET_Y;
    const top = Math.max(0, rawTop);
    const width = Math.max(ANCHOR_MIN_WIDTH, Math.min(ANCHOR_MAX_WIDTH, item.tableName.length * 8.5 + ANCHOR_PADDING_X * 2 + ANCHOR_TOGGLE_WIDTH));
    const toggleRegion = {
      type: "anchor-menu-toggle",
      tableId: item.tableId,
      left: left + width - ANCHOR_TOGGLE_WIDTH,
      top,
      width: ANCHOR_TOGGLE_WIDTH,
      height: 28
    };
    ctx.save();
    ctx.translateWithPrecision(left, top);
    this._drawTopRoundedRect(ctx, width, 28, 14, item.fill, ANCHOR_BORDER);
    this._drawAnchorToggle(ctx, width, item.text, this._openedMenuTableId === item.tableId || this._isSameRegion(this._hoveredRegion, toggleRegion));
    ctx.font = `600 13px ${DEFAULT_FONTFACE_PLANE}`;
    ctx.fillStyle = item.text;
    ctx.textAlign = "left";
    ctx.fillText(item.tableName, ANCHOR_PADDING_X, 28 / 2);
    ctx.restore();
    this._regions.push({
      type: "anchor-main",
      tableId: item.tableId,
      left,
      top,
      width,
      height: 28
    });
    this._regions.push(toggleRegion);
    if (this._openedMenuTableId === item.tableId) this._drawMenu(ctx, item.tableId, left, top + 28);
  }
  _drawAnchorToggle(ctx, anchorWidth, color, active) {
    const toggleLeft = anchorWidth - ANCHOR_TOGGLE_WIDTH;
    if (active) this._drawRightTopRoundedRect(ctx, toggleLeft, anchorWidth, 28, 14, ANCHOR_TOGGLE_BG_ACTIVE);
    ctx.save();
    ctx.beginPath();
    ctx.strokeStyle = ANCHOR_DIVIDER;
    ctx.lineWidth = 1;
    ctx.moveTo(toggleLeft + 0.5, 5);
    ctx.lineTo(toggleLeft + 0.5, 28 - 5);
    ctx.stroke();
    ctx.restore();
    const centerX = anchorWidth - ANCHOR_TOGGLE_WIDTH / 2;
    const centerY = 28 / 2;
    ctx.save();
    ctx.beginPath();
    ctx.strokeStyle = color;
    ctx.lineWidth = 1.6;
    ctx.lineCap = "round";
    ctx.moveTo(centerX - 5, centerY - 4);
    ctx.lineTo(centerX + 5, centerY - 4);
    ctx.moveTo(centerX - 5, centerY);
    ctx.lineTo(centerX + 5, centerY);
    ctx.moveTo(centerX - 5, centerY + 4);
    ctx.lineTo(centerX + 5, centerY + 4);
    ctx.stroke();
    ctx.restore();
  }
  _drawTopRoundedRect(ctx, width, height, radius, fill, stroke) {
    const r = Math.min(radius, width / 2, height);
    ctx.beginPath();
    ctx.moveTo(0, height);
    ctx.lineTo(0, r);
    ctx.arcTo(0, 0, r, 0, r);
    ctx.lineTo(width - r, 0);
    ctx.arcTo(width, 0, width, r, r);
    ctx.lineTo(width, height);
    ctx.closePath();
    ctx.fillStyle = fill;
    ctx.fill();
    if (stroke) {
      ctx.strokeStyle = stroke;
      ctx.lineWidth = 1;
      ctx.stroke();
    }
  }
  _drawRightTopRoundedRect(ctx, left, width, height, radius, fill) {
    const r = Math.min(radius, width - left, height);
    ctx.save();
    ctx.beginPath();
    ctx.moveTo(left, height);
    ctx.lineTo(left, 0);
    ctx.lineTo(width - r, 0);
    ctx.arcTo(width, 0, width, r, r);
    ctx.lineTo(width, height);
    ctx.closePath();
    ctx.fillStyle = fill;
    ctx.fill();
    ctx.restore();
  }
  _drawMenu(ctx, tableId, left, top) {
    const regions = buildTableMenuRegions(tableId, left, top);
    ctx.save();
    ctx.translateWithPrecision(left, top);
    Rect.drawWith(ctx, {
      width: 168,
      height: regions.length * 32,
      radius: MENU_RADIUS,
      fill: "#fff",
      stroke: MENU_BORDER
    });
    ctx.restore();
    for (const region of regions) {
      if (this._isSameRegion(this._hoveredRegion, region)) {
        ctx.save();
        ctx.fillStyle = MENU_HOVER_BG;
        ctx.fillRectByPrecision(region.left, region.top, region.width, region.height);
        ctx.restore();
      }
      ctx.save();
      ctx.font = `12px ${DEFAULT_FONTFACE_PLANE}`;
      ctx.fillStyle = region.action === "delete" ? "#d92d20" : "#344054";
      ctx.textAlign = "left";
      ctx.fillText(this._menuLabels[region.action], region.left + 12, region.top + region.height / 2);
      ctx.restore();
    }
    this._regions.push(...regions);
  }
  _drawInsertButton(ctx, region, fill) {
    const centerX = region.left + region.width / 2;
    const centerY = region.top + region.height / 2;
    const radius = INSERT_BUTTON_VISUAL_SIZE / 2;
    ctx.save();
    ctx.beginPath();
    ctx.arc(centerX, centerY, radius, 0, Math.PI * 2);
    ctx.fillStyle = "#fff";
    ctx.fill();
    ctx.strokeStyle = fill;
    ctx.stroke();
    ctx.beginPath();
    ctx.strokeStyle = fill;
    ctx.lineWidth = 1;
    ctx.lineCap = "round";
    for (const segment of buildCenteredPlusSegments(centerX, centerY, INSERT_BUTTON_PLUS_SIZE)) {
      ctx.moveTo(segment.fromX, segment.fromY);
      ctx.lineTo(segment.toX, segment.toY);
    }
    ctx.stroke();
    ctx.restore();
  }
  _isSameRegion(a, b) {
    return Boolean(a && a.type === b.type && a.tableId === b.tableId && a.action === b.action && a.index === b.index);
  }
};
var SheetTableThemeUIController = class SheetTableThemeUIController2 extends Disposable {
  constructor(_commandService) {
    super();
    this._commandService = _commandService;
    _defineProperty2(this, "_refreshTable", new Subject());
    _defineProperty2(this, "refreshTable$", this._refreshTable.asObservable());
    this._initListener();
  }
  _initListener() {
    this.disposeWithMe(this._commandService.onCommandExecuted((command) => {
      if (command.id === SetRangeThemeMutation.id) {
        const { styleName } = command.params;
        if (styleName.startsWith(SHEET_TABLE_CUSTOM_THEME_PREFIX)) this._refreshTable.next(Math.random());
      }
    }));
  }
};
SheetTableThemeUIController = __decorate2([__decorateParam2(0, Inject(ICommandService))], SheetTableThemeUIController);
var TABLE_CONTROLS_LAYER_INDEX = 5002;
var TABLE_CONTROL_GAP_ROW = 0;
var TABLE_RENDER_REFRESH_COMMANDS = /* @__PURE__ */ new Set([SetScrollOperation.id, SetZoomRatioOperation.id]);
function isSameTopGap(left, right) {
  if (left === null || right === null) return left === right;
  return left.size === right.size && left.color === right.color && left.stripeColor === right.stripeColor;
}
var SheetTableControlsRenderController = class SheetTableControlsRenderController2 extends Disposable {
  constructor(_context, _injector, _sheetSkeletonManagerService, _commandService, _tableManager, _rangeThemeModel, _workbookPermissionService, _permissionService, _sheetsSelectionsService, _selectionRenderService, _sheetTableThemeUIController, _localeService, _dialogService, _sidebarService) {
    super();
    this._context = _context;
    this._injector = _injector;
    this._sheetSkeletonManagerService = _sheetSkeletonManagerService;
    this._commandService = _commandService;
    this._tableManager = _tableManager;
    this._rangeThemeModel = _rangeThemeModel;
    this._workbookPermissionService = _workbookPermissionService;
    this._permissionService = _permissionService;
    this._sheetsSelectionsService = _sheetsSelectionsService;
    this._selectionRenderService = _selectionRenderService;
    this._sheetTableThemeUIController = _sheetTableThemeUIController;
    this._localeService = _localeService;
    this._dialogService = _dialogService;
    this._sidebarService = _sidebarService;
    _defineProperty2(this, "_shape", void 0);
    _defineProperty2(this, "_topGapBaseBySkeleton", /* @__PURE__ */ new WeakMap());
    this._shape = new SheetTableControlsShape("SheetTableControlsShape", () => this._sheetSkeletonManagerService.getCurrentSkeleton() || null);
    this._initShape();
    this._initRefresh();
    this._refresh();
  }
  _initShape() {
    var _this$_context$compon, _this$_context$compon2;
    this._context.scene.addObjects([this._shape], TABLE_CONTROLS_LAYER_INDEX);
    this.disposeWithMe(toDisposable(() => {
      this._context.scene.removeObjects([this._shape]);
    }));
    this.disposeWithMe(this._shape.onPointerMove$.subscribeEvent((evt, state) => {
      this._handlePointerMove(evt, state);
    }));
    this.disposeWithMe(this._shape.onPointerLeave$.subscribeEvent((_evt, state) => {
      this._handlePointerLeave(state);
    }));
    this.disposeWithMe(this._shape.onPointerDown$.subscribeEvent((evt, state) => {
      this._handlePointerDown(evt, state);
    }));
    this.disposeWithMe((_this$_context$compon = (_this$_context$compon2 = this._context.components.get(SHEET_VIEW_KEY.MAIN)) === null || _this$_context$compon2 === void 0 ? void 0 : _this$_context$compon2.onPointerMove$.subscribeEvent((evt) => {
      const point = this._getLocalPoint(evt);
      const insertRegion = this._getInsertRegionFromPoint(point.x, point.y);
      this._shape.setHoveredInsertRegion(insertRegion);
    })) !== null && _this$_context$compon !== void 0 ? _this$_context$compon : toDisposable(() => {
    }));
  }
  _initRefresh() {
    const commandExecuted$ = fromCallback(this._commandService.onCommandExecuted.bind(this._commandService)).pipe(filter(([command]) => {
      if (command.type === CommandType.OPERATION && TABLE_RENDER_REFRESH_COMMANDS.has(command.id)) {
        this._closeFloatingControls();
        return true;
      }
      return command.type === CommandType.MUTATION || command.type === CommandType.COMMAND;
    }));
    this.disposeWithMe(merge(this._context.unit.activeSheet$, this._sheetSkeletonManagerService.currentSkeleton$, this._tableManager.tableAdd$, this._tableManager.tableDelete$, this._tableManager.tableNameChanged$, this._tableManager.tableRangeChanged$, this._tableManager.tableThemeChanged$, this._sheetTableThemeUIController.refreshTable$, this._workbookPermissionService.unitPermissionInitStateChange$, this._permissionService.permissionPointUpdate$, this._sheetsSelectionsService.selectionChanged$, commandExecuted$).subscribe(() => {
      this._closeFloatingControls();
      this._refresh();
    }));
  }
  _refresh() {
    const skeleton = this._sheetSkeletonManagerService.getCurrentSkeleton();
    const worksheet = this._context.unit.getActiveSheet();
    if (!skeleton || !worksheet || !this._canEditWorkbook()) {
      this._shape.setItems([]);
      this._shape.refreshBounds();
      this._context.scene.makeDirty();
      return;
    }
    this._syncTopTableGap(skeleton);
    this._shape.setMenuLabels({
      rename: this._localeService.t("sheets-table-ui.rename"),
      "update-range": this._localeService.t("sheets-table-ui.updateRange"),
      "set-theme": this._localeService.t("sheets-table-ui.setTheme"),
      delete: this._localeService.t("sheets-table-ui.removeTable")
    });
    const unitId = this._context.unit.getUnitId();
    const subUnitId = worksheet.getSheetId();
    const items = this._tableManager.getTablesBySubunitId(unitId, subUnitId).map((table) => {
      var _rangeTheme$getHeader, _rangeTheme$getHeader2, _rangeTheme$getHeader3, _rangeTheme$getHeader4;
      const rangeTheme = this._rangeThemeModel.getRangeThemeStyle(unitId, table.getTableStyleId());
      return {
        tableId: table.getId(),
        tableName: table.getDisplayName(),
        range: table.getRange(),
        fill: (_rangeTheme$getHeader = rangeTheme === null || rangeTheme === void 0 || (_rangeTheme$getHeader2 = rangeTheme.getHeaderRowStyle()) === null || _rangeTheme$getHeader2 === void 0 || (_rangeTheme$getHeader2 = _rangeTheme$getHeader2.bg) === null || _rangeTheme$getHeader2 === void 0 ? void 0 : _rangeTheme$getHeader2.rgb) !== null && _rangeTheme$getHeader !== void 0 ? _rangeTheme$getHeader : "rgb(53,91,183)",
        text: (_rangeTheme$getHeader3 = rangeTheme === null || rangeTheme === void 0 || (_rangeTheme$getHeader4 = rangeTheme.getHeaderRowStyle()) === null || _rangeTheme$getHeader4 === void 0 || (_rangeTheme$getHeader4 = _rangeTheme$getHeader4.cl) === null || _rangeTheme$getHeader4 === void 0 ? void 0 : _rangeTheme$getHeader4.rgb) !== null && _rangeTheme$getHeader3 !== void 0 ? _rangeTheme$getHeader3 : "rgb(255,255,255)"
      };
    });
    this._shape.setItems(items);
    this._shape.refreshBounds();
    this._shape.makeDirty(true);
    this._context.scene.makeDirty();
  }
  _canEditWorkbook() {
    var _this$_permissionServ;
    const unitId = this._context.unit.getUnitId();
    return ((_this$_permissionServ = this._permissionService.getPermissionPoint(new WorkbookEditablePermission(unitId).id)) === null || _this$_permissionServ === void 0 ? void 0 : _this$_permissionServ.value) !== false;
  }
  _handlePointerMove(evt, state) {
    const point = this._getLocalPoint(evt);
    const hit = this._shape.hitTest(point.x, point.y);
    const insertRegion = this._isInsertHit(hit) ? hit : hit ? null : this._getInsertRegionFromPoint(point.x, point.y);
    const activeHit = hit !== null && hit !== void 0 ? hit : insertRegion;
    this._shape.setHoveredRegion(this._isInsertHit(hit) ? null : hit);
    this._shape.setHoveredInsertRegion(insertRegion);
    if (activeHit) {
      state.stopPropagation();
      this._context.scene.setCursor(CURSOR_TYPE.POINTER);
    } else this._context.scene.resetCursor();
  }
  _isInsertHit(hit) {
    return (hit === null || hit === void 0 ? void 0 : hit.type) === "insert-row" || (hit === null || hit === void 0 ? void 0 : hit.type) === "insert-column";
  }
  _handlePointerLeave(state) {
    state.stopPropagation();
    this._shape.setHoveredRegion(null);
    this._shape.setHoveredInsertRegion(null);
    this._context.scene.resetCursor();
  }
  _handlePointerDown(evt, state) {
    var _this$_shape$hitTest;
    if (evt.button === 2) return;
    const point = this._getLocalPoint(evt);
    const hit = (_this$_shape$hitTest = this._shape.hitTest(point.x, point.y)) !== null && _this$_shape$hitTest !== void 0 ? _this$_shape$hitTest : this._getInsertRegionFromPoint(point.x, point.y);
    if (!hit) {
      this._closeFloatingControls();
      return;
    }
    state.stopPropagation();
    evt.stopPropagation();
    evt.preventDefault();
    this._handleHit(hit);
  }
  _handleHit(hit) {
    const worksheet = this._context.unit.getActiveSheet();
    if (!worksheet) return;
    const unitId = this._context.unit.getUnitId();
    const subUnitId = worksheet.getSheetId();
    if (hit.type === "anchor-menu-toggle" || hit.type === "anchor-main") {
      this._shape.setOpenedMenuTableId(this._shape.getOpenedMenuTableId() === hit.tableId ? null : hit.tableId);
      return;
    }
    if (hit.type === "insert-row") {
      this._commandService.executeCommand(SheetTableInsertRowAtCommand.id, {
        unitId,
        subUnitId,
        tableId: hit.tableId,
        index: hit.index,
        count: 1
      });
      this._closeFloatingControls();
      return;
    }
    if (hit.type === "insert-column") {
      this._commandService.executeCommand(SheetTableInsertColumnAtCommand.id, {
        unitId,
        subUnitId,
        tableId: hit.tableId,
        index: hit.index,
        count: 1
      });
      this._closeFloatingControls();
      return;
    }
    if (hit.type !== "menu-item") return;
    switch (hit.action) {
      case "rename":
        this._openRenameDialog(unitId, hit.tableId);
        break;
      case "update-range":
        this._openRangeSelector(unitId, subUnitId, hit.tableId);
        break;
      case "set-theme":
        this._openThemePanel(unitId, subUnitId, hit.tableId);
        break;
      case "delete":
        this._commandService.executeCommand(DeleteSheetTableCommand.id, {
          tableId: hit.tableId,
          subUnitId,
          unitId
        });
        break;
    }
    this._closeFloatingControls();
  }
  _openRenameDialog(unitId, tableId) {
    this._dialogService.open({
      id: SHEET_TABLE_RENAME_DIALOG_ID,
      title: { title: this._localeService.t("sheets-table-ui.rename") },
      draggable: true,
      destroyOnClose: true,
      mask: true,
      children: { label: {
        name: SHEET_TABLE_RENAME_DIALOG,
        props: {
          unitId,
          tableId,
          onClose: () => this._dialogService.close(SHEET_TABLE_RENAME_DIALOG_ID)
        }
      } },
      width: 360,
      onClose: () => this._dialogService.close(SHEET_TABLE_RENAME_DIALOG_ID)
    });
  }
  async _openRangeSelector(unitId, subUnitId, tableId) {
    const table = this._tableManager.getTableById(unitId, tableId);
    if (!table) return;
    const selection = await openRangeSelector(this._injector, unitId, subUnitId, table.getRange(), tableId);
    if (!selection) return;
    this._commandService.executeCommand(SetSheetTableCommand.id, {
      tableId,
      unitId,
      updateRange: { newRange: selection.range }
    });
  }
  _openThemePanel(unitId, subUnitId, tableId) {
    const table = this._tableManager.getTableById(unitId, tableId);
    if (!table) return;
    this._sidebarService.open({
      id: SHEET_TABLE_THEME_PANEL_ID,
      header: { title: this._localeService.t("sheets-table-ui.tableStyle") },
      children: {
        label: SHEET_TABLE_THEME_PANEL,
        oldConfig: table.getTableConfig(),
        unitId,
        subUnitId,
        tableId
      },
      width: 330
    });
  }
  _getInsertRegionFromPoint(x, y) {
    const skeleton = this._sheetSkeletonManagerService.getCurrentSkeleton();
    const worksheet = this._context.unit.getActiveSheet();
    if (!skeleton || !worksheet) return null;
    const unitId = this._context.unit.getUnitId();
    const subUnitId = worksheet.getSheetId();
    const tables = this._tableManager.getTablesBySubunitId(unitId, subUnitId);
    for (const table of tables) {
      const range = table.getRange();
      const tableBounds = this._getRangeBounds(skeleton, range);
      if (x < tableBounds.left || x > tableBounds.right || y < tableBounds.top || y > tableBounds.bottom) continue;
      if (y > this._getRangeBounds(skeleton, {
        ...range,
        endRow: range.startRow
      }).bottom) for (let row = range.startRow + 1; row <= range.endRow; row++) {
        const cell = skeleton.getNoMergeCellWithCoordByIndex(row, range.startColumn);
        if (y >= cell.startY && y <= cell.endY) return {
          type: "insert-row",
          tableId: table.getId(),
          index: row + 1,
          left: tableBounds.left - 22 / 2,
          top: cell.endY - 22 / 2,
          width: 22,
          height: 22
        };
      }
    }
    return null;
  }
  _getRangeBounds(skeleton, range) {
    const startCell = skeleton.getNoMergeCellWithCoordByIndex(range.startRow, range.startColumn);
    const endCell = skeleton.getNoMergeCellWithCoordByIndex(range.endRow, range.endColumn);
    return {
      left: startCell.startX,
      top: startCell.startY,
      right: endCell.endX,
      bottom: endCell.endY
    };
  }
  _syncTopTableGap(skeleton) {
    const worksheet = this._context.unit.getActiveSheet();
    if (!worksheet) return;
    const unitId = this._context.unit.getUnitId();
    const subUnitId = worksheet.getSheetId();
    const hasTopTable = this._tableManager.getTablesBySubunitId(unitId, subUnitId).some((table) => table.getRange().startRow === 0);
    const current = skeleton.gapConfig;
    const rowGaps = { ...current.rowGaps };
    const previousTopGap = rowGaps[TABLE_CONTROL_GAP_ROW] ? { ...rowGaps[TABLE_CONTROL_GAP_ROW] } : null;
    let shouldSync = false;
    if (hasTopTable) {
      var _baseGap$size;
      if (!this._topGapBaseBySkeleton.has(skeleton)) this._topGapBaseBySkeleton.set(skeleton, rowGaps[TABLE_CONTROL_GAP_ROW] ? { ...rowGaps[TABLE_CONTROL_GAP_ROW] } : null);
      const baseGap = this._topGapBaseBySkeleton.get(skeleton);
      rowGaps[TABLE_CONTROL_GAP_ROW] = {
        ...baseGap !== null && baseGap !== void 0 ? baseGap : rowGaps[TABLE_CONTROL_GAP_ROW],
        size: ((_baseGap$size = baseGap === null || baseGap === void 0 ? void 0 : baseGap.size) !== null && _baseGap$size !== void 0 ? _baseGap$size : 0) + 32
      };
      shouldSync = true;
    } else if (this._topGapBaseBySkeleton.has(skeleton)) {
      const baseGap = this._topGapBaseBySkeleton.get(skeleton);
      if (baseGap) rowGaps[TABLE_CONTROL_GAP_ROW] = { ...baseGap };
      else delete rowGaps[TABLE_CONTROL_GAP_ROW];
      this._topGapBaseBySkeleton.delete(skeleton);
      shouldSync = true;
    }
    if (!shouldSync) return;
    if (isSameTopGap(previousTopGap, rowGaps[TABLE_CONTROL_GAP_ROW] ? { ...rowGaps[TABLE_CONTROL_GAP_ROW] } : null)) return;
    skeleton.setGapConfig({
      ...current,
      rowGaps
    });
    this._refreshSelections();
  }
  _refreshSelections() {
    this._selectionRenderService.resetSelectionsByModelData(this._sheetsSelectionsService.getCurrentSelections());
  }
  _closeFloatingControls() {
    this._shape.setOpenedMenuTableId(null);
    this._shape.setHoveredInsertRegion(null);
    this._shape.setHoveredRegion(null);
  }
  _getLocalPoint(evt) {
    const skeleton = this._sheetSkeletonManagerService.getCurrentSkeleton();
    if (skeleton) return getTransformCoord(evt.offsetX, evt.offsetY, this._context.scene, skeleton);
    return {
      x: evt.offsetX,
      y: evt.offsetY
    };
  }
};
SheetTableControlsRenderController = __decorate2([
  __decorateParam2(1, Inject(Injector)),
  __decorateParam2(2, Inject(SheetSkeletonManagerService)),
  __decorateParam2(3, ICommandService),
  __decorateParam2(4, Inject(TableManager)),
  __decorateParam2(5, Inject(SheetRangeThemeModel)),
  __decorateParam2(6, Inject(WorkbookPermissionService)),
  __decorateParam2(7, Inject(IPermissionService)),
  __decorateParam2(8, Inject(SheetsSelectionsService)),
  __decorateParam2(9, ISheetSelectionRenderService),
  __decorateParam2(10, Inject(SheetTableThemeUIController)),
  __decorateParam2(11, Inject(LocaleService)),
  __decorateParam2(12, IDialogService),
  __decorateParam2(13, ISidebarService)
], SheetTableControlsRenderController);
var FILTER_TRIGGER_HOVER_RADIUS = 4;
var SheetsTableFilterButtonShape = class SheetsTableFilterButtonShape2 extends Shape {
  constructor(key, props, _commandService) {
    super(key, props);
    this._commandService = _commandService;
    _defineProperty2(this, "_cellWidth", 0);
    _defineProperty2(this, "_cellHeight", 0);
    _defineProperty2(this, "_filterParams", void 0);
    _defineProperty2(this, "_iconColor", "#fff");
    _defineProperty2(this, "_hoverBackground", "rgba(255, 255, 255, 0.92)");
    _defineProperty2(this, "_hoverIconColor", "#202124");
    _defineProperty2(this, "_hovered", false);
    this.setShapeProps(props);
    this.onPointerDown$.subscribeEvent((evt) => this.onPointerDown(evt));
    this.onPointerEnter$.subscribeEvent(() => this.onPointerEnter());
    this.onPointerLeave$.subscribeEvent(() => this.onPointerLeave());
  }
  setShapeProps(props) {
    if (typeof props.cellHeight !== "undefined") this._cellHeight = props.cellHeight;
    if (typeof props.cellWidth !== "undefined") this._cellWidth = props.cellWidth;
    if (typeof props.filterParams !== "undefined") this._filterParams = props.filterParams;
    if (typeof props.iconColor !== "undefined") this._iconColor = props.iconColor;
    if (typeof props.hoverBackground !== "undefined") this._hoverBackground = props.hoverBackground;
    if (typeof props.hoverIconColor !== "undefined") this._hoverIconColor = props.hoverIconColor;
    this.transformByState({
      width: props.width,
      height: props.height
    });
  }
  _draw(ctx) {
    const cellHeight = this._cellHeight;
    const cellWidth = this._cellWidth;
    const left = 16 - cellWidth;
    const top = 16 - cellHeight;
    ctx.save();
    const cellRegion = new Path2D();
    cellRegion.rect(left, top, cellWidth, cellHeight);
    ctx.clip(cellRegion);
    if (this._hovered) {
      var _ctx$roundRect;
      ctx.save();
      ctx.fillStyle = this._hoverBackground;
      ctx.beginPath();
      (_ctx$roundRect = ctx.roundRect) === null || _ctx$roundRect === void 0 || _ctx$roundRect.call(ctx, 0, 0, 16, 16, FILTER_TRIGGER_HOVER_RADIUS);
      if (!ctx.roundRect) ctx.rect(0, 0, 16, 16);
      ctx.fill();
      ctx.restore();
    }
    this._drawChevron(ctx, this._hovered ? this._hoverIconColor : this._iconColor);
    ctx.restore();
  }
  _drawChevron(ctx, color) {
    const centerX = 16 / 2;
    const centerY = 16 / 2;
    ctx.save();
    ctx.beginPath();
    ctx.strokeStyle = color;
    ctx.lineWidth = 1.8;
    ctx.lineCap = "round";
    ctx.lineJoin = "round";
    ctx.moveTo(centerX - 4.5, centerY - 2.5);
    ctx.lineTo(centerX, 10);
    ctx.lineTo(12.5, centerY - 2.5);
    ctx.stroke();
    ctx.restore();
  }
  onPointerDown(evt) {
    if (evt.button === 2) return;
    const { row, col, unitId, subUnitId, tableId } = this._filterParams;
    if (!this._commandService.hasCommand(OpenTableFilterPanelOperation.id)) return;
    setTimeout(() => {
      const cmdParams = {
        row,
        col,
        unitId,
        subUnitId,
        tableId
      };
      this._commandService.executeCommand(OpenTableFilterPanelOperation.id, cmdParams);
    }, 200);
  }
  onPointerEnter() {
    this._hovered = true;
    this.makeDirty(true);
  }
  onPointerLeave() {
    this._hovered = false;
    this.makeDirty(true);
  }
};
SheetsTableFilterButtonShape = __decorate2([__decorateParam2(2, ICommandService)], SheetsTableFilterButtonShape);
var SHEETS_FILTER_BUTTON_Z_INDEX = 5e3;
var computeIconTop = (startY, endY, cellHeight, verticalAlign) => {
  switch (verticalAlign) {
    case VerticalAlign.TOP:
      return startY + 1;
    case VerticalAlign.MIDDLE:
      return startY + Math.max(0, (cellHeight - 16) / 2);
    case VerticalAlign.BOTTOM:
    default:
      return endY - 16 - 1;
  }
};
var SheetsTableFilterButtonRenderController = class SheetsTableFilterButtonRenderController2 extends RxDisposable {
  constructor(_context, _injector, _sheetSkeletonManagerService, _sheetInterceptorService, _tableManager, _rangeThemeModel, _commandService) {
    super();
    this._context = _context;
    this._injector = _injector;
    this._sheetSkeletonManagerService = _sheetSkeletonManagerService;
    this._sheetInterceptorService = _sheetInterceptorService;
    this._tableManager = _tableManager;
    this._rangeThemeModel = _rangeThemeModel;
    this._commandService = _commandService;
    _defineProperty2(this, "_buttonRenderDisposable", null);
    _defineProperty2(this, "_tableFilterButtonShapes", []);
    this._initRenderer();
    this._initCommandExecuted();
  }
  dispose() {
    super.dispose();
    this._disposeRendering();
  }
  _initRenderer() {
    const tableManager = this._tableManager;
    this._sheetSkeletonManagerService.currentSkeleton$.pipe(switchMap((skeletonParams) => {
      var _workbook$getActiveSh;
      if (!skeletonParams) return of(null);
      const { unit: workbook, unitId } = this._context;
      const worksheetId = ((_workbook$getActiveSh = workbook.getActiveSheet()) === null || _workbook$getActiveSh === void 0 ? void 0 : _workbook$getActiveSh.getSheetId()) || "";
      const getParams = () => ({
        unitId,
        worksheetId,
        tableFilterRanges: this._tableManager.getSheetFilterRangeWithState(workbook.getUnitId(), worksheetId),
        skeleton: skeletonParams.skeleton
      });
      return merge(tableManager.tableAdd$, tableManager.tableNameChanged$, tableManager.tableRangeChanged$, tableManager.tableThemeChanged$, tableManager.tableDelete$, tableManager.tableFilterChanged$).pipe(map(() => getParams()), startWith(getParams()));
    }), takeUntil(this.dispose$)).subscribe((renderParams) => {
      this._disposeRendering();
      if (!renderParams || !renderParams.tableFilterRanges) return;
      this._renderButtons(renderParams);
    });
  }
  _initCommandExecuted() {
    this.disposeWithMe(this._commandService.onCommandExecuted((command) => {
      var _workbook$getActiveSh2;
      if (command.id !== SetVerticalTextAlignCommand.id) return;
      const { unit: workbook, unitId } = this._context;
      const worksheetId = ((_workbook$getActiveSh2 = workbook.getActiveSheet()) === null || _workbook$getActiveSh2 === void 0 ? void 0 : _workbook$getActiveSh2.getSheetId()) || "";
      const skeleton = this._sheetSkeletonManagerService.getCurrentSkeleton();
      if (!skeleton) return;
      const renderParams = {
        unitId,
        worksheetId,
        tableFilterRanges: this._tableManager.getSheetFilterRangeWithState(workbook.getUnitId(), worksheetId),
        skeleton
      };
      this._disposeRendering();
      if (!renderParams || !renderParams.tableFilterRanges) return;
      this._renderButtons(renderParams);
    }));
  }
  _renderButtons(params) {
    const { tableFilterRanges, unitId, skeleton, worksheetId } = params;
    const { unit: workbook, scene } = this._context;
    const worksheet = workbook.getSheetBySheetId(worksheetId);
    if (!worksheet) return;
    for (const { range, states, tableId } of tableFilterRanges) {
      var _this$_rangeThemeMode, _headerStyle$cl$rgb, _headerStyle$cl, _headerStyle$bg$rgb, _headerStyle$bg;
      const { startRow, startColumn, endColumn } = range;
      const table = this._tableManager.getTableById(unitId, tableId);
      const headerStyle = table ? (_this$_rangeThemeMode = this._rangeThemeModel.getRangeThemeStyle(unitId, table.getTableStyleId())) === null || _this$_rangeThemeMode === void 0 ? void 0 : _this$_rangeThemeMode.getHeaderRowStyle() : null;
      const iconColor = (_headerStyle$cl$rgb = headerStyle === null || headerStyle === void 0 || (_headerStyle$cl = headerStyle.cl) === null || _headerStyle$cl === void 0 ? void 0 : _headerStyle$cl.rgb) !== null && _headerStyle$cl$rgb !== void 0 ? _headerStyle$cl$rgb : "#fff";
      const hoverIconColor = (_headerStyle$bg$rgb = headerStyle === null || headerStyle === void 0 || (_headerStyle$bg = headerStyle.bg) === null || _headerStyle$bg === void 0 ? void 0 : _headerStyle$bg.rgb) !== null && _headerStyle$bg$rgb !== void 0 ? _headerStyle$bg$rgb : "#202124";
      this._interceptCellContent(unitId, worksheetId, range);
      for (let col = startColumn; col <= endColumn; col++) {
        const key = `sheets-table-filter-button-${startRow}-${col}`;
        const startPosition = getCoordByCell(startRow, col, scene, skeleton);
        const cellStyle = worksheet.getCellStyle(startRow, col);
        const verticalAlign = (cellStyle === null || cellStyle === void 0 ? void 0 : cellStyle.vt) || VerticalAlign.BOTTOM;
        const { startX, startY, endX, endY } = startPosition;
        const cellWidth = endX - startX;
        const cellHeight = endY - startY;
        if (cellHeight <= 1 || cellWidth <= 1) continue;
        const state = states[col - startColumn];
        const props = {
          left: endX - 16 - 1,
          top: computeIconTop(startY, endY, cellHeight, verticalAlign),
          height: 16,
          width: 16,
          zIndex: SHEETS_FILTER_BUTTON_Z_INDEX,
          iconColor,
          hoverBackground: iconColor,
          hoverIconColor,
          cellHeight,
          cellWidth,
          filterParams: {
            unitId,
            subUnitId: worksheetId,
            row: startRow,
            col,
            buttonState: state,
            tableId
          }
        };
        const buttonShape = this._injector.createInstance(SheetsTableFilterButtonShape, key, props);
        this._tableFilterButtonShapes.push(buttonShape);
      }
    }
    scene.addObjects(this._tableFilterButtonShapes);
    scene.makeDirty();
  }
  _interceptCellContent(workbookId, worksheetId, range) {
    const { startRow, startColumn, endColumn } = range;
    this._buttonRenderDisposable = this._sheetInterceptorService.intercept(INTERCEPTOR_POINT.CELL_CONTENT, {
      effect: InterceptorEffectEnum.Style,
      handler: (cell, pos, next) => {
        const { row, col, unitId, subUnitId } = pos;
        if (unitId !== workbookId || subUnitId !== worksheetId || row !== startRow || col < startColumn || col > endColumn) return next(cell);
        if (!cell || cell === pos.rawData) cell = { ...pos.rawData };
        cell.fontRenderExtension = {
          ...cell === null || cell === void 0 ? void 0 : cell.fontRenderExtension,
          rightOffset: 16 + 1 + 2
        };
        return next(cell);
      },
      priority: 10
    });
  }
  _disposeRendering() {
    var _this$_buttonRenderDi;
    this._tableFilterButtonShapes.forEach((s) => s.dispose());
    (_this$_buttonRenderDi = this._buttonRenderDisposable) === null || _this$_buttonRenderDi === void 0 || _this$_buttonRenderDi.dispose();
    this._buttonRenderDisposable = null;
    this._tableFilterButtonShapes = [];
  }
};
SheetsTableFilterButtonRenderController = __decorate2([
  __decorateParam2(1, Inject(Injector)),
  __decorateParam2(2, Inject(SheetSkeletonManagerService)),
  __decorateParam2(3, Inject(SheetInterceptorService)),
  __decorateParam2(4, Inject(TableManager)),
  __decorateParam2(5, Inject(SheetRangeThemeModel)),
  __decorateParam2(6, ICommandService)
], SheetsTableFilterButtonRenderController);
var SheetsTableRenderController = class SheetsTableRenderController2 extends RxDisposable {
  constructor(_context, _injector, _sheetSkeletonManagerService, _tableManager, _sheetTableThemeUIController) {
    super();
    this._context = _context;
    this._injector = _injector;
    this._sheetSkeletonManagerService = _sheetSkeletonManagerService;
    this._tableManager = _tableManager;
    this._sheetTableThemeUIController = _sheetTableThemeUIController;
    this._initListener();
  }
  _dirtySkeleton() {
    var _this$_context$mainCo;
    (_this$_context$mainCo = this._context.mainComponent) === null || _this$_context$mainCo === void 0 || _this$_context$mainCo.makeDirty();
    const currentParam = this._sheetSkeletonManagerService.getCurrentParam();
    if (currentParam) {
      const param = {
        ...currentParam,
        dirty: true
      };
      this._sheetSkeletonManagerService.reCalculate(param);
    }
  }
  _initListener() {
    const tableManager = this._tableManager;
    const dirtySkeleton = this._dirtySkeleton.bind(this);
    this.disposeWithMe(merge(tableManager.tableAdd$, tableManager.tableDelete$, tableManager.tableNameChanged$, tableManager.tableRangeChanged$, tableManager.tableThemeChanged$, tableManager.tableFilterChanged$, tableManager.tableInitStatus$, this._sheetTableThemeUIController.refreshTable$).subscribe(dirtySkeleton));
  }
};
SheetsTableRenderController = __decorate2([
  __decorateParam2(1, Inject(Injector)),
  __decorateParam2(2, Inject(SheetSkeletonManagerService)),
  __decorateParam2(3, Inject(TableManager)),
  __decorateParam2(4, Inject(SheetTableThemeUIController))
], SheetsTableRenderController);
var SheetTableSelectionController = class SheetTableSelectionController2 extends Disposable {
  constructor(_sheetInterceptorService, _univerInstanceService, _tableManager) {
    super();
    this._sheetInterceptorService = _sheetInterceptorService;
    this._univerInstanceService = _univerInstanceService;
    this._tableManager = _tableManager;
    this._initSelectionChange();
  }
  _initSelectionChange() {
    this.disposeWithMe(this._sheetInterceptorService.interceptCommand({ getMutations: (command) => {
      if (command.id === SelectAllCommand.id) {
        const target = getSheetCommandTarget(this._univerInstanceService);
        if (!target) return {
          redos: [],
          undos: []
        };
        const { range } = command.params;
        const { unitId, subUnitId, worksheet } = target;
        const overlapTable = this._tableManager.getTablesBySubunitId(unitId, subUnitId).find((table) => {
          const tableRange = table.getRange();
          return Rectangle.contains(tableRange, range);
        });
        if (overlapTable) {
          const tableRange = overlapTable.getRange();
          const tableRangeWithoutHeader = {
            ...tableRange,
            startRow: tableRange.startRow + 1
          };
          if (Rectangle.equals(tableRange, range)) return {
            undos: [],
            redos: []
          };
          else if (Rectangle.equals(tableRangeWithoutHeader, range)) return {
            undos: [],
            redos: [{
              id: SetSelectionsOperation.id,
              params: {
                unitId,
                subUnitId,
                selections: [{
                  range: tableRange,
                  primary: getPrimaryForRange(tableRange, worksheet)
                }]
              }
            }]
          };
          else return {
            undos: [],
            redos: [{
              id: SetSelectionsOperation.id,
              params: {
                unitId,
                subUnitId,
                selections: [{
                  range: tableRangeWithoutHeader,
                  primary: getPrimaryForRange(tableRangeWithoutHeader, worksheet)
                }]
              }
            }]
          };
        }
      }
      return {
        redos: [],
        undos: []
      };
    } }));
  }
};
SheetTableSelectionController = __decorate2([
  __decorateParam2(0, Inject(SheetInterceptorService)),
  __decorateParam2(1, Inject(IUniverInstanceService)),
  __decorateParam2(2, Inject(TableManager))
], SheetTableSelectionController);
var SheetTableSelector = (props) => {
  const { unitId, subUnitId, range, onCancel, onConfirm, tableId } = props;
  const tableManager = useDependency(TableManager);
  const [selectedRange, setSelectedRange] = (0, import_react.useState)(range);
  const [rangeError, setRangeError] = (0, import_react.useState)("");
  const localeService = useDependency(LocaleService);
  const univerInstanceService = useDependency(IUniverInstanceService);
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(RangeSelector, {
      maxRangeCount: 1,
      unitId,
      subUnitId,
      initialValue: serializeRange(range),
      onChange: (_, text) => {
        const originValue = serializeRange(range);
        const newRange = deserializeRangeWithSheet(text).range;
        const target = getSheetCommandTarget(univerInstanceService, {
          unitId,
          subUnitId
        });
        if (!target) return;
        if (target.worksheet.getMergeData().some((merge2) => {
          return Rectangle.intersects(newRange, merge2);
        })) {
          setRangeError(localeService.t("sheets-table-ui.tableRangeWithMergeError"));
          return;
        }
        if (tableManager.getTablesBySubunitId(unitId, subUnitId).some((table) => {
          if (table.getId() === tableId) return false;
          const tableRange = table.getRange();
          return Rectangle.intersects(newRange, tableRange);
        })) {
          setRangeError(localeService.t("sheets-table-ui.tableRangeWithOtherTableError"));
          return;
        }
        const { startRow, endRow } = newRange;
        if (startRow === endRow) {
          setRangeError(localeService.t("sheets-table-ui.tableRangeSingleRowError"));
          return;
        }
        if (originValue === text) return;
        if (tableId) {
          const table = tableManager.getTableById(unitId, tableId);
          if (table) {
            const oldRange = table.getRange();
            if (Rectangle.intersects(newRange, oldRange) && oldRange.startRow === newRange.startRow) {
              setSelectedRange(newRange);
              setRangeError("");
              onConfirm({
                unitId,
                subUnitId,
                range: newRange
              });
              return;
            } else {
              setRangeError(localeService.t("sheets-table-ui.updateError"));
              return;
            }
          }
        }
        setSelectedRange(newRange);
        setRangeError("");
      },
      supportAcrossSheet: false
    }),
    rangeError && (0, import_jsx_runtime.jsx)("div", {
      className: "univer-mt-1 univer-text-xs univer-text-red-500",
      children: rangeError
    }),
    (0, import_jsx_runtime.jsxs)("div", {
      className: "univer-mt-4 univer-flex univer-justify-end",
      children: [(0, import_jsx_runtime.jsx)(Button, {
        onClick: onCancel,
        children: localeService.t("sheets-table-ui.cancel")
      }), (0, import_jsx_runtime.jsx)(Button, {
        variant: "primary",
        onClick: () => {
          if (rangeError) return;
          onConfirm({
            unitId,
            subUnitId,
            range: selectedRange
          });
        },
        className: "univer-ml-2",
        children: localeService.t("sheets-table-ui.confirm")
      })]
    })
  ] });
};
var SheetTableThemePanel = (props) => {
  var _customStyle$getHeade, _customStyle$getHeade2, _customStyle$getFirst, _customStyle$getFirst2, _customStyle$getSecon, _customStyle$getSecon2, _customStyle$getLastR, _customStyle$getLastR2;
  const { unitId, subUnitId, tableId } = props;
  const commandService = useDependency(ICommandService);
  const localeService = useDependency(LocaleService);
  const tableManager = useDependency(TableManager);
  const table = tableManager.getTableById(unitId, tableId);
  const rangeThemeModel = useDependency(SheetRangeThemeModel);
  const sheetTableThemeUIController = useDependency(SheetTableThemeUIController);
  const rangeThemeMapChanged = useObservable(rangeThemeModel.rangeThemeMapChange$);
  const tableRefresh = useObservable(sheetTableThemeUIController.refreshTable$);
  const errorService = useDependency(ErrorService);
  const [, setRefresh] = (0, import_react.useState)(Math.random());
  const themeConfig = useObservable(tableManager.tableThemeChanged$, {
    theme: table === null || table === void 0 ? void 0 : table.getTableStyleId(),
    oldTheme: table === null || table === void 0 ? void 0 : table.getTableStyleId(),
    unitId,
    subUnitId,
    tableId
  });
  const defaultRangeThemes = rangeThemeModel.getRegisteredRangeThemes().filter((item) => item === null || item === void 0 ? void 0 : item.startsWith(TABLE_DEFAULT_NAME_PREFIX));
  const customRangeThemes = rangeThemeModel.getALLRegisteredTheme(unitId).filter((item) => item === null || item === void 0 ? void 0 : item.startsWith(TABLE_CUSTOM_NAME_PREFIX));
  const selectedTheme = table === null || table === void 0 ? void 0 : table.getTableStyleId();
  const customSelected = customRangeThemes.find((item) => item === selectedTheme);
  const customStyleName = customSelected || customRangeThemes[0];
  const customStyle = rangeThemeModel.getCustomRangeThemeStyle(unitId, customStyleName);
  const headerBg = (_customStyle$getHeade = customStyle === null || customStyle === void 0 || (_customStyle$getHeade2 = customStyle.getHeaderRowStyle()) === null || _customStyle$getHeade2 === void 0 || (_customStyle$getHeade2 = _customStyle$getHeade2.bg) === null || _customStyle$getHeade2 === void 0 ? void 0 : _customStyle$getHeade2.rgb) !== null && _customStyle$getHeade !== void 0 ? _customStyle$getHeade : TABLE_DEFAULT_BG_COLOR;
  const firstRowBg = (_customStyle$getFirst = customStyle === null || customStyle === void 0 || (_customStyle$getFirst2 = customStyle.getFirstRowStyle()) === null || _customStyle$getFirst2 === void 0 || (_customStyle$getFirst2 = _customStyle$getFirst2.bg) === null || _customStyle$getFirst2 === void 0 ? void 0 : _customStyle$getFirst2.rgb) !== null && _customStyle$getFirst !== void 0 ? _customStyle$getFirst : TABLE_DEFAULT_BG_COLOR;
  const secondRowBg = (_customStyle$getSecon = customStyle === null || customStyle === void 0 || (_customStyle$getSecon2 = customStyle.getSecondRowStyle()) === null || _customStyle$getSecon2 === void 0 || (_customStyle$getSecon2 = _customStyle$getSecon2.bg) === null || _customStyle$getSecon2 === void 0 ? void 0 : _customStyle$getSecon2.rgb) !== null && _customStyle$getSecon !== void 0 ? _customStyle$getSecon : TABLE_DEFAULT_BG_COLOR;
  const lastRowBg = (_customStyle$getLastR = customStyle === null || customStyle === void 0 || (_customStyle$getLastR2 = customStyle.getLastRowStyle()) === null || _customStyle$getLastR2 === void 0 || (_customStyle$getLastR2 = _customStyle$getLastR2.bg) === null || _customStyle$getLastR2 === void 0 ? void 0 : _customStyle$getLastR2.rgb) !== null && _customStyle$getLastR !== void 0 ? _customStyle$getLastR : TABLE_DEFAULT_BG_COLOR;
  const [hoverCustomId, setHoverCustomId] = (0, import_react.useState)(null);
  const handleThemeChange = (theme) => {
    commandService.executeCommand(SetSheetTableCommand.id, {
      unitId,
      tableId,
      theme
    });
  };
  const handleAddCustomTheme = () => {
    if (customRangeThemes.length >= 11) {
      errorService.emit(localeService.t("sheets-table-ui.customTooMore"));
      return;
    }
    const lastCustomTheme = customRangeThemes[customRangeThemes.length - 1];
    let newThemeName = `${TABLE_CUSTOM_NAME_PREFIX}1`;
    if (lastCustomTheme) newThemeName = `${TABLE_CUSTOM_NAME_PREFIX}${Number(lastCustomTheme.split("-")[2]) + 1}`;
    const newTheme = new RangeThemeStyle(newThemeName, { ...customEmptyThemeWithBorderStyle });
    commandService.executeCommand(AddTableThemeCommand.id, {
      unitId,
      tableId,
      themeStyle: newTheme
    });
  };
  const setCustomTheme = (themeName, tableThemeStyle) => {
    commandService.executeCommand(SetRangeThemeMutation.id, {
      unitId,
      subUnitId,
      styleName: themeName,
      style: tableThemeStyle
    });
  };
  const removeCustomTheme = (themeName) => {
    commandService.executeCommand(RemoveTableThemeCommand.id, {
      unitId,
      tableId,
      themeName
    });
  };
  (0, import_react.useEffect)(() => {
    setRefresh(Math.random());
  }, [rangeThemeMapChanged, tableRefresh]);
  if (!table) return null;
  const headerBgIsDark = new ColorKit(headerBg).isDark();
  const firstRowBgIsDark = new ColorKit(firstRowBg).isDark();
  const secondRowBgIsDark = new ColorKit(secondRowBg).isDark();
  const lastRowBgIsDark = new ColorKit(lastRowBg).isDark();
  return (0, import_jsx_runtime.jsxs)("div", { children: [
    (0, import_jsx_runtime.jsx)("h5", { children: localeService.t("sheets-table-ui.defaultStyle") }),
    (0, import_jsx_runtime.jsx)("div", {
      className: "univer-flex univer-gap-2",
      children: defaultRangeThemes.map((item) => {
        var _rangeThemeItem$getHe, _rangeThemeItem$getFi, _rangeThemeItem$getSe, _rangeThemeItem$getLa;
        const rangeThemeItem = rangeThemeModel.getDefaultRangeThemeStyle(item);
        const headerRowBg = (rangeThemeItem === null || rangeThemeItem === void 0 || (_rangeThemeItem$getHe = rangeThemeItem.getHeaderRowStyle()) === null || _rangeThemeItem$getHe === void 0 || (_rangeThemeItem$getHe = _rangeThemeItem$getHe.bg) === null || _rangeThemeItem$getHe === void 0 ? void 0 : _rangeThemeItem$getHe.rgb) || "rgb(255, 255, 255)";
        const firstRowBg2 = (rangeThemeItem === null || rangeThemeItem === void 0 || (_rangeThemeItem$getFi = rangeThemeItem.getFirstRowStyle()) === null || _rangeThemeItem$getFi === void 0 || (_rangeThemeItem$getFi = _rangeThemeItem$getFi.bg) === null || _rangeThemeItem$getFi === void 0 ? void 0 : _rangeThemeItem$getFi.rgb) || "rgb(255, 255, 255)";
        const secondRowBg2 = (rangeThemeItem === null || rangeThemeItem === void 0 || (_rangeThemeItem$getSe = rangeThemeItem.getSecondRowStyle()) === null || _rangeThemeItem$getSe === void 0 || (_rangeThemeItem$getSe = _rangeThemeItem$getSe.bg) === null || _rangeThemeItem$getSe === void 0 ? void 0 : _rangeThemeItem$getSe.rgb) || "rgb(255, 255, 255)";
        const lastRowBg2 = (rangeThemeItem === null || rangeThemeItem === void 0 || (_rangeThemeItem$getLa = rangeThemeItem.getLastRowStyle()) === null || _rangeThemeItem$getLa === void 0 || (_rangeThemeItem$getLa = _rangeThemeItem$getLa.bg) === null || _rangeThemeItem$getLa === void 0 ? void 0 : _rangeThemeItem$getLa.rgb) || "rgb(255, 255, 255)";
        return (0, import_jsx_runtime.jsxs)("div", {
          className: clsx("univer-h-10 univer-w-8 univer-cursor-pointer univer-border univer-border-solid univer-border-gray-200 univer-p-px [&>div]:univer-box-border [&>div]:univer-h-2.5", { "univer-border-blue-500": item === themeConfig.theme }),
          onClick: () => handleThemeChange(item),
          children: [
            (0, import_jsx_runtime.jsx)("div", { style: {
              background: headerRowBg,
              border: `${headerRowBg ? TABLE_BORDER_NONE : TABLE_BORDER_DEFAULT}`
            } }),
            (0, import_jsx_runtime.jsx)("div", { style: {
              background: firstRowBg2,
              border: `${firstRowBg2 ? TABLE_BORDER_NONE : TABLE_BORDER_DEFAULT}`
            } }),
            (0, import_jsx_runtime.jsx)("div", { style: {
              background: secondRowBg2,
              border: `${secondRowBg2 ? TABLE_BORDER_NONE : TABLE_BORDER_DEFAULT}`
            } }),
            (0, import_jsx_runtime.jsx)("div", { style: {
              background: lastRowBg2,
              border: `${lastRowBg2 ? TABLE_BORDER_NONE : TABLE_BORDER_DEFAULT}`
            } })
          ]
        }, item);
      })
    }),
    (0, import_jsx_runtime.jsx)("h5", { children: localeService.t("sheets-table-ui.customStyle") }),
    (0, import_jsx_runtime.jsxs)("div", {
      className: clsx("univer-w-full univer-rounded-sm", borderClassName),
      children: [(0, import_jsx_runtime.jsxs)("div", {
        className: "univer-flex univer-flex-wrap univer-gap-2 univer-p-2",
        children: [(0, import_jsx_runtime.jsx)("div", {
          className: clsx("univer-h-10 univer-w-8 univer-cursor-pointer univer-p-px univer-text-center univer-leading-10", borderClassName),
          onClick: handleAddCustomTheme,
          children: "+"
        }), customRangeThemes.map((item) => {
          var _rangeThemeItem$getHe2, _rangeThemeItem$getFi2, _rangeThemeItem$getSe2, _rangeThemeItem$getLa2;
          const rangeThemeItem = rangeThemeModel.getCustomRangeThemeStyle(unitId, item);
          const headerRowBg = rangeThemeItem === null || rangeThemeItem === void 0 || (_rangeThemeItem$getHe2 = rangeThemeItem.getHeaderRowStyle()) === null || _rangeThemeItem$getHe2 === void 0 || (_rangeThemeItem$getHe2 = _rangeThemeItem$getHe2.bg) === null || _rangeThemeItem$getHe2 === void 0 ? void 0 : _rangeThemeItem$getHe2.rgb;
          const firstRowBg2 = rangeThemeItem === null || rangeThemeItem === void 0 || (_rangeThemeItem$getFi2 = rangeThemeItem.getFirstRowStyle()) === null || _rangeThemeItem$getFi2 === void 0 || (_rangeThemeItem$getFi2 = _rangeThemeItem$getFi2.bg) === null || _rangeThemeItem$getFi2 === void 0 ? void 0 : _rangeThemeItem$getFi2.rgb;
          const secondRowBg2 = rangeThemeItem === null || rangeThemeItem === void 0 || (_rangeThemeItem$getSe2 = rangeThemeItem.getSecondRowStyle()) === null || _rangeThemeItem$getSe2 === void 0 || (_rangeThemeItem$getSe2 = _rangeThemeItem$getSe2.bg) === null || _rangeThemeItem$getSe2 === void 0 ? void 0 : _rangeThemeItem$getSe2.rgb;
          const lastRowBg2 = rangeThemeItem === null || rangeThemeItem === void 0 || (_rangeThemeItem$getLa2 = rangeThemeItem.getLastRowStyle()) === null || _rangeThemeItem$getLa2 === void 0 || (_rangeThemeItem$getLa2 = _rangeThemeItem$getLa2.bg) === null || _rangeThemeItem$getLa2 === void 0 ? void 0 : _rangeThemeItem$getLa2.rgb;
          return (0, import_jsx_runtime.jsxs)("div", {
            className: clsx("univer-relative univer-h-10 univer-w-8 univer-cursor-pointer univer-border univer-border-solid univer-border-gray-200 univer-p-px", { "univer-border-blue-500": item === themeConfig.theme }),
            onClick: () => handleThemeChange(item),
            onMouseEnter: () => setHoverCustomId(item),
            onMouseLeave: () => setHoverCustomId(null),
            children: [
              (0, import_jsx_runtime.jsx)("div", {
                className: "univer-box-border univer-h-2.5",
                style: {
                  background: headerRowBg !== null && headerRowBg !== void 0 ? headerRowBg : TABLE_BORDER_NONE,
                  border: `${headerRowBg ? TABLE_BORDER_NONE : TABLE_BORDER_DEFAULT}`
                }
              }),
              (0, import_jsx_runtime.jsx)("div", {
                className: "univer-box-border univer-h-2.5",
                style: {
                  background: firstRowBg2 !== null && firstRowBg2 !== void 0 ? firstRowBg2 : TABLE_BORDER_NONE,
                  border: `${firstRowBg2 ? TABLE_BORDER_NONE : TABLE_BORDER_DEFAULT}`
                }
              }),
              (0, import_jsx_runtime.jsx)("div", {
                className: "univer-box-border univer-h-2.5",
                style: {
                  background: secondRowBg2 !== null && secondRowBg2 !== void 0 ? secondRowBg2 : TABLE_BORDER_NONE,
                  border: `${secondRowBg2 ? TABLE_BORDER_NONE : TABLE_BORDER_DEFAULT}`
                }
              }),
              (0, import_jsx_runtime.jsx)("div", {
                className: "univer-box-border univer-h-2.5",
                style: {
                  background: lastRowBg2 !== null && lastRowBg2 !== void 0 ? lastRowBg2 : TABLE_BORDER_NONE,
                  border: `${lastRowBg2 ? TABLE_BORDER_NONE : TABLE_BORDER_DEFAULT}`
                }
              }),
              (0, import_jsx_runtime.jsx)("div", {
                className: "univer-absolute univer-right-[-3px] univer-top-[-3px] univer-size-3 univer-rounded-md univer-bg-gray-200 univer-text-center univer-text-xs univer-leading-[10px]",
                style: { display: hoverCustomId === item ? "block" : "none" },
                onClick: (e) => {
                  e.stopPropagation();
                  removeCustomTheme(item);
                },
                children: "x"
              })
            ]
          }, item);
        })]
      }), customSelected && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [(0, import_jsx_runtime.jsx)("div", { className: "univer-h-px univer-w-full univer-bg-gray-200" }), (0, import_jsx_runtime.jsxs)("div", {
        className: "univer-flex univer-flex-col univer-gap-2 univer-p-2",
        children: [
          (0, import_jsx_runtime.jsxs)("div", {
            className: "univer-flex univer-h-9 univer-gap-2",
            children: [(0, import_jsx_runtime.jsx)("div", {
              className: clsx("univer-box-border univer-h-full univer-w-52 univer-rounded-sm univer-text-center univer-leading-9", borderClassName, {
                "univer-text-white": headerBgIsDark,
                "univer-text-gray-900": !headerBgIsDark
              }),
              style: { background: headerBg },
              children: localeService.t("sheets-table-ui.header")
            }), (0, import_jsx_runtime.jsx)(Dropdown, {
              overlay: (0, import_jsx_runtime.jsx)("div", {
                className: "univer-p-2",
                children: (0, import_jsx_runtime.jsx)(ColorPicker, {
                  value: headerBg,
                  onChange: (val) => {
                    const headerRowStyle = processStyleWithBorderStyle("headerRowStyle", {
                      bg: { rgb: val },
                      cl: { rgb: new ColorKit(val).isDark() ? "#fff" : "#000" }
                    });
                    setCustomTheme(table.getTableStyleId(), { headerRowStyle });
                  }
                })
              }),
              children: (0, import_jsx_runtime.jsxs)("div", {
                className: clsx("univer-flex univer-cursor-pointer univer-items-center univer-gap-2 univer-rounded-sm univer-bg-white univer-p-1", borderClassName),
                children: [(0, import_jsx_runtime.jsx)("div", {
                  className: clsx("univer-size-4 univer-rounded-lg univer-bg-gray-400", borderClassName, {
                    "univer-text-white": headerBgIsDark,
                    "univer-text-gray-900": !headerBgIsDark
                  }),
                  style: { background: headerBg }
                }), (0, import_jsx_runtime.jsx)(DropdownIcon, { className: "univer-size-2" })]
              })
            })]
          }),
          (0, import_jsx_runtime.jsxs)("div", {
            className: "univer-flex univer-h-9 univer-gap-2",
            children: [(0, import_jsx_runtime.jsx)("div", {
              className: clsx("univer-box-border univer-h-full univer-w-52 univer-rounded-sm univer-text-center univer-leading-9", borderClassName, {
                "univer-text-white": firstRowBgIsDark,
                "univer-text-gray-900": !firstRowBgIsDark
              }),
              style: { background: firstRowBg },
              children: localeService.t("sheets-table-ui.firstLine")
            }), (0, import_jsx_runtime.jsx)(Dropdown, {
              overlay: (0, import_jsx_runtime.jsx)("div", {
                className: "univer-p-2",
                children: (0, import_jsx_runtime.jsx)(ColorPicker, {
                  value: firstRowBg,
                  onChange: (val) => {
                    setCustomTheme(table.getTableStyleId(), { firstRowStyle: {
                      bg: { rgb: val },
                      cl: { rgb: new ColorKit(val).isDark() ? "#fff" : "#000" }
                    } });
                  }
                })
              }),
              children: (0, import_jsx_runtime.jsxs)("div", {
                className: clsx("univer-flex univer-cursor-pointer univer-items-center univer-gap-2 univer-rounded-sm univer-bg-white univer-p-1", borderClassName),
                children: [(0, import_jsx_runtime.jsx)("div", {
                  className: clsx("univer-size-4 univer-rounded-lg univer-bg-gray-400", borderClassName),
                  style: { background: firstRowBg }
                }), (0, import_jsx_runtime.jsx)(DropdownIcon, { className: "univer-size-2" })]
              })
            })]
          }),
          (0, import_jsx_runtime.jsxs)("div", {
            className: "univer-flex univer-h-9 univer-gap-2",
            children: [(0, import_jsx_runtime.jsx)("div", {
              className: clsx("univer-box-border univer-h-full univer-w-52 univer-rounded-sm univer-text-center univer-leading-9", borderClassName, {
                "univer-text-white": secondRowBgIsDark,
                "univer-text-gray-900": !secondRowBgIsDark
              }),
              style: { background: secondRowBg },
              children: localeService.t("sheets-table-ui.secondLine")
            }), (0, import_jsx_runtime.jsx)(Dropdown, {
              overlay: (0, import_jsx_runtime.jsx)("div", {
                className: "univer-p-2",
                children: (0, import_jsx_runtime.jsx)(ColorPicker, {
                  value: secondRowBg,
                  onChange: (val) => setCustomTheme(table.getTableStyleId(), { secondRowStyle: {
                    bg: { rgb: val },
                    cl: { rgb: new ColorKit(val).isDark() ? "#fff" : "#000" }
                  } })
                })
              }),
              children: (0, import_jsx_runtime.jsxs)("div", {
                className: clsx("univer-flex univer-cursor-pointer univer-items-center univer-gap-2 univer-rounded-sm univer-bg-white univer-p-1", borderClassName),
                children: [(0, import_jsx_runtime.jsx)("div", {
                  className: clsx("univer-size-4 univer-rounded-lg univer-bg-gray-400", borderClassName),
                  style: { background: secondRowBg }
                }), (0, import_jsx_runtime.jsx)(DropdownIcon, { className: "univer-size-2" })]
              })
            })]
          }),
          (0, import_jsx_runtime.jsxs)("div", {
            className: "univer-flex univer-h-9 univer-gap-2",
            children: [(0, import_jsx_runtime.jsx)("div", {
              className: clsx("univer-box-border univer-h-full univer-w-52 univer-rounded-sm univer-text-center univer-leading-9", borderClassName, {
                "univer-text-white": lastRowBgIsDark,
                "univer-text-gray-900": !lastRowBgIsDark
              }),
              style: { background: lastRowBg },
              children: localeService.t("sheets-table-ui.footer")
            }), (0, import_jsx_runtime.jsx)(Dropdown, {
              overlay: (0, import_jsx_runtime.jsx)("div", {
                className: "univer-p-2",
                children: (0, import_jsx_runtime.jsx)(ColorPicker, {
                  value: lastRowBg,
                  onChange: (val) => {
                    const lastRowStyle = processStyleWithBorderStyle("lastRowStyle", {
                      bg: { rgb: val },
                      cl: { rgb: new ColorKit(val).isDark() ? "#fff" : "#000" }
                    });
                    setCustomTheme(table.getTableStyleId(), { lastRowStyle });
                  }
                })
              }),
              children: (0, import_jsx_runtime.jsxs)("div", {
                className: clsx("univer-flex univer-cursor-pointer univer-items-center univer-gap-2 univer-rounded-sm univer-bg-white univer-p-1", borderClassName),
                children: [(0, import_jsx_runtime.jsx)("div", {
                  className: clsx("univer-size-4 univer-rounded-lg univer-bg-gray-400", borderClassName),
                  style: { background: lastRowBg }
                }), (0, import_jsx_runtime.jsx)(DropdownIcon, { className: "univer-size-2" })]
              })
            })]
          })
        ]
      })] })]
    })
  ] });
};
var SHEET_TABLE_CONTEXT_INSERT_MENU_ID = "sheet.table.context-insert_menu-id";
var SHEET_TABLE_CONTEXT_REMOVE_MENU_ID = "sheet.table.context-remove_menu-id";
function sheetTableToolbarInsertMenuFactory(accessor) {
  return {
    id: OpenTableSelectorOperation.id,
    type: MenuItemType.BUTTON,
    icon: TABLE_TOOLBAR_BUTTON,
    tooltip: "sheets-table-ui.title",
    title: "sheets-table-ui.title",
    hidden$: getMenuHiddenObservable(accessor, UniverType.UNIVER_SHEET),
    disabled$: getCurrentRangeDisable$(accessor, {}, true)
  };
}
function SheetTableInsertContextMenuFactory(accessor) {
  return {
    id: SHEET_TABLE_CONTEXT_INSERT_MENU_ID,
    type: MenuItemType.SUBITEMS,
    icon: "InsertDoubleIcon",
    title: "sheets-table-ui.insert.main",
    hidden$: getSheetTableRowColOperationHidden$(accessor)
  };
}
function SheetTableRemoveContextMenuFactory(accessor) {
  return {
    id: SHEET_TABLE_CONTEXT_REMOVE_MENU_ID,
    type: MenuItemType.SUBITEMS,
    icon: "ReduceDoubleIcon",
    title: "sheets-table-ui.remove.main",
    hidden$: getSheetTableRowColOperationHidden$(accessor)
  };
}
function SheetTableInsertRowMenuFactory(accessor) {
  return {
    id: SheetTableInsertRowCommand.id,
    type: MenuItemType.BUTTON,
    title: "sheets-table-ui.insert.row",
    hidden$: getSheetTableHeaderOperationHidden$(accessor)
  };
}
function SheetTableInsertColMenuFactory(accessor) {
  return {
    id: SheetTableInsertColCommand.id,
    title: "sheets-table-ui.insert.col",
    type: MenuItemType.BUTTON
  };
}
function SheetTableRemoveRowMenuFactory(accessor) {
  return {
    id: SheetTableRemoveRowCommand.id,
    type: MenuItemType.BUTTON,
    title: "sheets-table-ui.remove.row",
    hidden$: getSheetTableHeaderOperationHidden$(accessor)
  };
}
function SheetTableRemoveColMenuFactory(accessor) {
  return {
    id: SheetTableRemoveColCommand.id,
    title: "sheets-table-ui.remove.col",
    type: MenuItemType.BUTTON
  };
}
function getSheetTableRowColOperationHidden$(accessor) {
  const sheetsSelectionsService = accessor.get(SheetsSelectionsService);
  return accessor.get(IUniverInstanceService).getCurrentTypeOfUnit$(UniverType.UNIVER_SHEET).pipe(switchMap((workbook) => {
    if (!workbook) return of(true);
    return workbook.activeSheet$.pipe(switchMap((sheet) => {
      if (!sheet) return of(true);
      return sheetsSelectionsService.selectionMoveEnd$.pipe(switchMap((selections) => {
        if (!selections.length || selections.length > 1) return of(true);
        const range = selections[0].range;
        return of(!accessor.get(SheetsTableController).getContainerTableWithRange(workbook.getUnitId(), sheet.getSheetId(), range));
      }));
    }));
  }));
}
function getSheetTableHeaderOperationHidden$(accessor) {
  const sheetsSelectionsService = accessor.get(SheetsSelectionsService);
  return accessor.get(IUniverInstanceService).getCurrentTypeOfUnit$(UniverType.UNIVER_SHEET).pipe(switchMap((workbook) => {
    if (!workbook) return of(true);
    return workbook.activeSheet$.pipe(switchMap((sheet) => {
      if (!sheet) return of(true);
      return sheetsSelectionsService.selectionMoveEnd$.pipe(switchMap((selections) => {
        if (!selections.length || selections.length > 1) return of(true);
        const range = selections[0].range;
        const isInTable = accessor.get(SheetsTableController).getContainerTableWithRange(workbook.getUnitId(), sheet.getSheetId(), range);
        if (!isInTable) return of(true);
        const tableRange = isInTable.getRange();
        if (range.startRow === tableRange.startRow) return of(true);
        return of(false);
      }));
    }));
  }));
}
var menuSchema = {
  [RibbonDataGroup.ORGANIZATION]: { [OpenTableSelectorOperation.id]: {
    order: 0,
    menuItemFactory: sheetTableToolbarInsertMenuFactory
  } },
  [ContextMenuPosition.MAIN_AREA]: { [ContextMenuGroup.LAYOUT]: {
    [SHEET_TABLE_CONTEXT_INSERT_MENU_ID]: {
      order: 5,
      menuItemFactory: SheetTableInsertContextMenuFactory,
      [SheetTableInsertRowCommand.id]: {
        order: 1,
        menuItemFactory: SheetTableInsertRowMenuFactory
      },
      [SheetTableInsertColCommand.id]: {
        order: 2,
        menuItemFactory: SheetTableInsertColMenuFactory
      }
    },
    [SHEET_TABLE_CONTEXT_REMOVE_MENU_ID]: {
      order: 6,
      menuItemFactory: SheetTableRemoveContextMenuFactory,
      [SheetTableRemoveRowCommand.id]: {
        order: 1,
        menuItemFactory: SheetTableRemoveRowMenuFactory
      },
      [SheetTableRemoveColCommand.id]: {
        order: 2,
        menuItemFactory: SheetTableRemoveColMenuFactory
      }
    }
  } }
};
var SheetTableMenuController = class SheetTableMenuController2 extends Disposable {
  constructor(_componentManager, _menuManagerService) {
    super();
    this._componentManager = _componentManager;
    this._menuManagerService = _menuManagerService;
    this._initComponents();
    this._initMenu();
  }
  _initComponents() {
    [
      [TABLE_TOOLBAR_BUTTON, TableIcon],
      [TABLE_SELECTOR_DIALOG, SheetTableSelector],
      [SHEET_TABLE_THEME_PANEL, SheetTableThemePanel]
    ].forEach(([key, comp]) => {
      this.disposeWithMe(this._componentManager.register(key, comp));
    });
  }
  _initMenu() {
    this._menuManagerService.mergeMenu(menuSchema);
  }
};
SheetTableMenuController = __decorate2([__decorateParam2(0, Inject(ComponentManager)), __decorateParam2(1, Inject(IMenuManagerService))], SheetTableMenuController);
var UniverSheetsTableUIPlugin = class UniverSheetsTableUIPlugin2 extends Plugin {
  constructor(_config = defaultPluginConfig2, _injector, _commandService, _configService, _renderManagerService) {
    super();
    this._config = _config;
    this._injector = _injector;
    this._commandService = _commandService;
    this._configService = _configService;
    this._renderManagerService = _renderManagerService;
    const { menu, ...rest } = merge_default({}, defaultPluginConfig2, this._config);
    if (menu) this._configService.setConfig("menu", menu, { merge: true });
    this._configService.setConfig(SHEETS_TABLE_UI_PLUGIN_CONFIG_KEY, rest);
    this._initRegisterCommand();
  }
  onStarting() {
    registerDependencies(this._injector, [
      [SheetsTableComponentController],
      [SheetsTableUiService],
      [SheetTableMenuController],
      [SheetTableThemeUIController],
      [SheetTableSelectionController]
    ]);
  }
  onReady() {
    touchDependencies(this._injector, [
      [SheetsTableComponentController],
      [SheetsTableUiService],
      [SheetTableMenuController],
      [SheetTableThemeUIController],
      [SheetTableSelectionController]
    ]);
  }
  onRendered() {
    this._registerRenderModules();
  }
  _registerRenderModules() {
    const renderDependencies = [];
    if (this._config.hideAnchor !== true) renderDependencies.push([SheetTableControlsRenderController]);
    renderDependencies.push([SheetsTableFilterButtonRenderController], [SheetsTableRenderController]);
    renderDependencies.forEach((m) => {
      this.disposeWithMe(this._renderManagerService.registerRenderModule(UniverType.UNIVER_SHEET, m));
    });
  }
  _initRegisterCommand() {
    [OpenTableFilterPanelOperation, OpenTableSelectorOperation].forEach((m) => this._commandService.registerCommand(m));
  }
};
_defineProperty2(UniverSheetsTableUIPlugin, "pluginName", PLUGIN_NAME2);
_defineProperty2(UniverSheetsTableUIPlugin, "packageName", name2);
_defineProperty2(UniverSheetsTableUIPlugin, "version", version2);
_defineProperty2(UniverSheetsTableUIPlugin, "type", UniverType.UNIVER_SHEET);
UniverSheetsTableUIPlugin = __decorate2([
  DependentOn(UniverSheetsTablePlugin),
  __decorateParam2(1, Inject(Injector)),
  __decorateParam2(2, Inject(ICommandService)),
  __decorateParam2(3, IConfigService),
  __decorateParam2(4, IRenderManagerService)
], UniverSheetsTableUIPlugin);

// node_modules/@univerjs/sheets-table/lib/es/facade.js
var FWorkbookSheetsTableMixin = class extends FWorkbook {
  getTableInfo(tableId) {
    const unitId = this.getId();
    return this._injector.get(SheetTableService).getTableInfo(unitId, tableId);
  }
  getTableList() {
    const unitId = this.getId();
    return this._injector.get(SheetTableService).getTableList(unitId);
  }
  async addTable(subUnitId, tableName, rangeInfo, tableId, options) {
    const sheetTableService = this._injector.get(SheetTableService);
    const localeService = this._injector.get(LocaleService);
    const workbook = this._injector.get(IUniverInstanceService).getCurrentUnitOfType(UniverType.UNIVER_SHEET);
    const sheetNameSet = /* @__PURE__ */ new Set();
    if (workbook) workbook.getSheets().forEach((sheet) => {
      sheetNameSet.add(sheet.getName());
    });
    if (!customNameCharacterCheck(tableName, sheetNameSet)) {
      this._injector.get(ILogService).warn(localeService.t("sheets-table.tableNameError"));
      return;
    }
    const addTableParams = {
      unitId: this.getId(),
      name: tableName,
      subUnitId,
      range: rangeInfo,
      options,
      id: tableId
    };
    if (await this._commandService.executeCommand(AddSheetTableCommand.id, addTableParams)) {
      var _sheetTableService$ge;
      return (_sheetTableService$ge = sheetTableService.getTableList(this.getId()).find((table) => table.name === tableName)) === null || _sheetTableService$ge === void 0 ? void 0 : _sheetTableService$ge.id;
    }
  }
  async removeTable(tableId) {
    var _this$getTableInfo;
    const subUnitId = (_this$getTableInfo = this.getTableInfo(tableId)) === null || _this$getTableInfo === void 0 ? void 0 : _this$getTableInfo.subUnitId;
    if (!subUnitId) return false;
    const removedTableParams = {
      unitId: this.getId(),
      subUnitId,
      tableId
    };
    return this._commandService.executeCommand(DeleteSheetTableCommand.id, removedTableParams);
  }
  getTableInfoByName(tableName) {
    return this.getTableList().find((table) => table.name === tableName);
  }
  setTableFilter(tableId, column, filter2) {
    const setTableFilterParams = {
      unitId: this.getId(),
      tableId,
      column,
      tableFilter: filter2
    };
    return this._commandService.executeCommand(SetSheetTableFilterCommand.id, setTableFilterParams);
  }
};
FWorkbook.extend(FWorkbookSheetsTableMixin);
var FWorksheetTableMixin = class extends FWorksheet {
  addTable(tableName, rangeInfo, tableId, options) {
    const subUnitId = this.getSheetId();
    const workbook = this.getWorkbook();
    const unitId = workbook.getUnitId();
    const localeService = this._injector.get(LocaleService);
    const sheetNameSet = /* @__PURE__ */ new Set();
    if (workbook) workbook.getSheets().forEach((sheet) => {
      sheetNameSet.add(sheet.getName());
    });
    if (!customNameCharacterCheck(tableName, sheetNameSet)) {
      this._injector.get(ILogService).warn(localeService.t("sheets-table.tableNameError"));
      return false;
    }
    const addTableParams = {
      unitId,
      subUnitId,
      name: tableName,
      range: rangeInfo,
      id: tableId,
      options
    };
    return this._commandService.executeCommand(AddSheetTableCommand.id, addTableParams);
  }
  setTableFilter(tableId, column, filter2) {
    const setTableFilterParams = {
      unitId: this.getWorkbook().getUnitId(),
      tableId,
      column,
      tableFilter: filter2
    };
    return this._commandService.executeCommand(SetSheetTableFilterCommand.id, setTableFilterParams);
  }
  removeTable(tableId) {
    const removedTableParams = {
      unitId: this._fWorkbook.getId(),
      subUnitId: this.getSheetId(),
      tableId
    };
    return this._commandService.executeCommand(DeleteSheetTableCommand.id, removedTableParams);
  }
  setTableRange(tableId, rangeInfo) {
    const tableSetConfig = {
      unitId: this.getWorkbook().getUnitId(),
      tableId,
      updateRange: { newRange: rangeInfo }
    };
    return this._commandService.executeCommand(SetSheetTableCommand.id, tableSetConfig);
  }
  setTableName(tableId, tableName) {
    const workbook = this.getWorkbook();
    const localeService = this._injector.get(LocaleService);
    const sheetNameSet = /* @__PURE__ */ new Set();
    if (workbook) workbook.getSheets().forEach((sheet) => {
      sheetNameSet.add(sheet.getName());
    });
    if (!customNameCharacterCheck(tableName, sheetNameSet)) {
      this._injector.get(ILogService).warn(localeService.t("sheets-table.tableNameError"));
      return false;
    }
    const tableSetConfig = {
      unitId: this.getWorkbook().getUnitId(),
      tableId,
      name: tableName
    };
    return this._commandService.executeCommand(SetSheetTableCommand.id, tableSetConfig);
  }
  getSubTableInfos() {
    const unitId = this._fWorkbook.getId();
    return this._injector.get(SheetTableService).getTableList(unitId).filter((table) => table.subUnitId === this.getSheetId());
  }
  resetFilter(tableId, column) {
    const setTableFilterParams = {
      unitId: this._fWorkbook.getId(),
      tableId,
      column,
      tableFilter: void 0
    };
    return this._commandService.executeCommand(SetSheetTableFilterCommand.id, setTableFilterParams);
  }
  getTableByCell(row, column) {
    const unitId = this._fWorkbook.getId();
    const allSubTableInfos = this._injector.get(SheetTableService).getTableList(unitId).filter((table) => table.subUnitId === this.getSheetId());
    const cellRange = cellToRange(row, column);
    return allSubTableInfos.find((table) => {
      const tableRange = table.range;
      return Rectangle.intersects(tableRange, cellRange);
    });
  }
  addTableTheme(tableId, themeStyleJSON) {
    const themeStyle = new RangeThemeStyle("table-style");
    themeStyle.fromJson(themeStyleJSON);
    return this._commandService.executeCommand(AddTableThemeCommand.id, {
      unitId: this._fWorkbook.getId(),
      tableId,
      themeStyle
    });
  }
};
FWorksheet.extend(FWorksheetTableMixin);
var FSheetsTableEnumMixin = class extends FEnum {
  get TableColumnFilterTypeEnum() {
    return TableColumnFilterTypeEnum;
  }
  get TableConditionTypeEnum() {
    return TableConditionTypeEnum;
  }
  get TableNumberCompareTypeEnum() {
    return TableNumberCompareTypeEnum;
  }
  get TableStringCompareTypeEnum() {
    return TableStringCompareTypeEnum;
  }
  get TableDateCompareTypeEnum() {
    return TableDateCompareTypeEnum;
  }
};
FEnum.extend(FSheetsTableEnumMixin);

// node_modules/@univerjs/preset-sheets-table/lib/es/index.js
function l() {
  return {
    plugins: [
      UniverSheetsTablePlugin,
      UniverSheetsTableUIPlugin
    ].filter((e) => !!e)
  };
}
export {
  AddSheetTableCommand,
  AddSheetTableMutation,
  AddTableThemeCommand,
  DeleteSheetTableCommand,
  DeleteSheetTableMutation,
  RemoveTableThemeCommand,
  SHEET_TABLE_CUSTOM_THEME_PREFIX,
  SetSheetTableCommand,
  SetSheetTableFilterCommand,
  SetSheetTableFilterMutation,
  SetSheetTableMutation,
  SheetTableInsertColCommand,
  SheetTableInsertColumnAtCommand,
  SheetTableInsertRowAtCommand,
  SheetTableInsertRowCommand,
  SheetTableRemoveColCommand,
  SheetTableRemoveColumnAtCommand,
  SheetTableRemoveRowCommand,
  SheetTableService,
  SheetsTableButtonStateEnum,
  SheetsTableController,
  SheetsTableSortStateEnum,
  TABLE_FILTER_EMPTY_VALUE,
  TableColumnDataTypeEnum,
  TableColumnFilterTypeEnum,
  TableConditionTypeEnum,
  TableDateCompareTypeEnum,
  TableManager,
  TableNumberCompareTypeEnum,
  TableStringCompareTypeEnum,
  UniverSheetsTablePlugin,
  l as UniverSheetsTablePreset,
  UniverSheetsTableUIPlugin,
  customEmptyThemeWithBorderStyle,
  getExistingNamesSet,
  isConditionFilter,
  isManualTableFilter,
  processStyleWithBorderStyle,
  validateSheetTableName
};
//# sourceMappingURL=@univerjs_preset-sheets-table.js.map
