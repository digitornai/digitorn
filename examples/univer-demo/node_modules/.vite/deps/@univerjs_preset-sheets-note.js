import {
  AddNoteIcon,
  CellPopupManagerService,
  ComponentManager,
  ContextMenuGroup,
  ContextMenuPosition,
  CopySheetCommand,
  DeleteNoteIcon,
  FRange,
  FWorksheet,
  HideNoteIcon,
  HoverManagerService,
  IEditorBridgeService,
  IMenuManagerService,
  INTERCEPTOR_POINT,
  IRenderManagerService,
  IZenZoneService,
  MenuItemType,
  RefRangeService,
  RemoveSheetCommand,
  SheetInterceptorService,
  SheetSkeletonManagerService,
  SheetsSelectionsService,
  Textarea,
  UniverSheetsPlugin,
  WorkbookEditablePermission,
  WorksheetEditPermission,
  clsx,
  getCurrentRangeDisable$,
  getMenuHiddenObservable,
  getSheetCommandTarget,
  handleCommonRangeChangeWithEffectRefCommandsSkipNoInterests,
  useConfigValue,
  useDebounceFn,
  useDependency
} from "./chunk-FWGY4U6S.js";
import {
  require_jsx_runtime
} from "./chunk-YG5AX2YU.js";
import {
  FEventName,
  FUniver
} from "./chunk-YVJOJ6TY.js";
import "./chunk-MCFHSGOC.js";
import "./chunk-UVXKGGW7.js";
import {
  require_react
} from "./chunk-73THXJN7.js";
import {
  BehaviorSubject,
  CanceledError,
  CommandType,
  DependentOn,
  Disposable,
  DisposableCollection,
  ICommandService,
  IConfigService,
  IResourceManagerService,
  IUndoRedoService,
  IUniverInstanceService,
  Inject,
  Injector,
  InterceptorEffectEnum,
  LocaleService,
  ObjectMatrix,
  Plugin,
  RANGE_TYPE,
  Rectangle,
  Subject,
  UniverType,
  combineLatest,
  debounceTime,
  filter,
  generateRandomId,
  map,
  merge_default,
  of,
  sequenceExecuteAsync,
  switchMap,
  touchDependencies
} from "./chunk-X3WWXCVS.js";
import {
  __toESM
} from "./chunk-2TUXWMP5.js";

// node_modules/@univerjs/sheets-note/lib/es/index.js
function _typeof(o) {
  "@babel/helpers - typeof";
  return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o2) {
    return typeof o2;
  } : function(o2) {
    return o2 && "function" == typeof Symbol && o2.constructor === Symbol && o2 !== Symbol.prototype ? "symbol" : typeof o2;
  }, _typeof(o);
}
function toPrimitive(t, r) {
  if ("object" != _typeof(t) || !t) return t;
  var e = t[Symbol.toPrimitive];
  if (void 0 !== e) {
    var i = e.call(t, r || "default");
    if ("object" != _typeof(i)) return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return ("string" === r ? String : Number)(t);
}
function toPropertyKey(t) {
  var i = toPrimitive(t, "string");
  return "symbol" == _typeof(i) ? i : i + "";
}
function _defineProperty(e, r, t) {
  return (r = toPropertyKey(r)) in e ? Object.defineProperty(e, r, {
    value: t,
    enumerable: true,
    configurable: true,
    writable: true
  }) : e[r] = t, e;
}
var SheetsNoteModel = class extends Disposable {
  constructor(..._args) {
    super(..._args);
    _defineProperty(this, "_notesMap", /* @__PURE__ */ new Map());
    _defineProperty(this, "_change$", new Subject());
    _defineProperty(this, "change$", this._change$.asObservable());
  }
  _ensureNotesMap(unitId, subUnitId) {
    let unitMap = this._notesMap.get(unitId);
    if (!unitMap) {
      unitMap = /* @__PURE__ */ new Map();
      this._notesMap.set(unitId, unitMap);
    }
    let subUnitMap = unitMap.get(subUnitId);
    if (!subUnitMap) {
      subUnitMap = /* @__PURE__ */ new Map();
      unitMap.set(subUnitId, subUnitMap);
    }
    return subUnitMap;
  }
  _getNoteByPosition(unitId, subUnitId, row, col) {
    const subUnitMap = this._ensureNotesMap(unitId, subUnitId);
    for (const [_, note] of subUnitMap) if (note.row === row && note.col === col) return note;
  }
  _getNoteById(unitId, subUnitId, id) {
    return this._ensureNotesMap(unitId, subUnitId).get(id);
  }
  _getNoteByParams(unitId, subUnitId, params) {
    const { noteId, row, col } = params;
    if (noteId) return this._getNoteById(unitId, subUnitId, noteId);
    if (row !== void 0 && col !== void 0) return this._getNoteByPosition(unitId, subUnitId, row, col);
    return null;
  }
  getSheetShowNotes$(unitId, subUnitId) {
    return this._change$.pipe(filter(({ unitId: u, subUnitId: s }) => u === unitId && s === subUnitId), map(() => {
      const subUnitMap = this._ensureNotesMap(unitId, subUnitId);
      const notes = [];
      for (const [_, note] of subUnitMap) if (note.show) notes.push({
        loc: {
          row: note.row,
          col: note.col,
          unitId,
          subUnitId
        },
        note
      });
      return notes;
    }));
  }
  getCellNoteChange$(unitId, subUnitId, row, col) {
    return this._change$.pipe(filter(({ unitId: u, subUnitId: s, oldNote }) => {
      if (u !== unitId || s !== subUnitId || !oldNote) return false;
      return oldNote.row === row && oldNote.col === col;
    }), map((newNote) => newNote));
  }
  updateNote(unitId, subUnitId, row, col, note, silent) {
    const oldNote = this._getNoteByParams(unitId, subUnitId, {
      noteId: note === null || note === void 0 ? void 0 : note.id,
      row,
      col
    });
    const subUnitMap = this._ensureNotesMap(unitId, subUnitId);
    const newNote = {
      ...note,
      id: (oldNote === null || oldNote === void 0 ? void 0 : oldNote.id) || note.id || generateRandomId(6),
      row,
      col
    };
    subUnitMap.set(newNote.id, newNote);
    this._change$.next({
      unitId,
      subUnitId,
      oldNote,
      type: "update",
      newNote,
      silent
    });
  }
  removeNote(unitId, subUnitId, params) {
    const { noteId, row, col, silent } = params;
    const oldNote = this._getNoteByParams(unitId, subUnitId, {
      noteId,
      row,
      col
    });
    if (!oldNote) return;
    this._ensureNotesMap(unitId, subUnitId).delete(oldNote.id);
    this._change$.next({
      unitId,
      subUnitId,
      oldNote,
      type: "update",
      newNote: null,
      silent
    });
  }
  toggleNotePopup(unitId, subUnitId, params) {
    const { noteId, row, col, silent } = params;
    const oldNote = this._getNoteByParams(unitId, subUnitId, {
      noteId,
      row,
      col
    });
    if (!oldNote) return;
    const subUnitMap = this._ensureNotesMap(unitId, subUnitId);
    const newNote = {
      ...oldNote,
      show: !oldNote.show
    };
    subUnitMap.set(newNote.id, newNote);
    this._change$.next({
      unitId,
      subUnitId,
      oldNote,
      type: "update",
      newNote,
      silent
    });
  }
  updateNotePosition(unitId, subUnitId, params) {
    const { noteId, row, col, newRow, newCol, silent } = params;
    const oldNote = this._getNoteByParams(unitId, subUnitId, {
      noteId,
      row,
      col
    });
    if (!oldNote) return;
    const subUnitMap = this._ensureNotesMap(unitId, subUnitId);
    const newNote = {
      ...oldNote,
      row: newRow,
      col: newCol
    };
    subUnitMap.set(newNote.id, newNote);
    this._change$.next({
      unitId,
      subUnitId,
      oldNote,
      type: "ref",
      newNote,
      silent
    });
  }
  getNote(unitId, subUnitId, params) {
    return this._getNoteByParams(unitId, subUnitId, params);
  }
  getNotes() {
    return this._notesMap;
  }
  getUnitNotes(unitId) {
    return this._notesMap.get(unitId);
  }
  getSheetNotes(unitId, subUnitId) {
    const unitMap = this._notesMap.get(unitId);
    if (!unitMap) return;
    return unitMap.get(subUnitId);
  }
  deleteUnitNotes(unitId) {
    this._notesMap.delete(unitId);
  }
};
var UpdateNoteMutation = {
  id: "sheet.mutation.update-note",
  type: CommandType.MUTATION,
  handler: (accessor, params) => {
    const { unitId, sheetId, row, col, note, silent } = params;
    accessor.get(SheetsNoteModel).updateNote(unitId, sheetId, row, col, note, silent);
    return true;
  }
};
var RemoveNoteMutation = {
  id: "sheet.mutation.remove-note",
  type: CommandType.MUTATION,
  handler: (accessor, params) => {
    const { unitId, sheetId, noteId, row, col, silent } = params;
    accessor.get(SheetsNoteModel).removeNote(unitId, sheetId, {
      noteId,
      row,
      col,
      silent
    });
    return true;
  }
};
var ToggleNotePopupMutation = {
  id: "sheet.mutation.toggle-note-popup",
  type: CommandType.MUTATION,
  handler: (accessor, params) => {
    const { unitId, sheetId, noteId, row, col, silent } = params;
    accessor.get(SheetsNoteModel).toggleNotePopup(unitId, sheetId, {
      noteId,
      row,
      col,
      silent
    });
    return true;
  }
};
var UpdateNotePositionMutation = {
  id: "sheet.mutation.update-note-position",
  type: CommandType.MUTATION,
  handler: (accessor, params) => {
    const { unitId, sheetId, noteId, row, col, newPosition, silent } = params;
    accessor.get(SheetsNoteModel).updateNotePosition(unitId, sheetId, {
      noteId,
      row,
      col,
      newRow: newPosition.row,
      newCol: newPosition.col,
      silent
    });
    return true;
  }
};
var SheetDeleteNoteCommand = {
  id: "sheet.command.delete-note",
  type: CommandType.COMMAND,
  handler: (accessor) => {
    const target = getSheetCommandTarget(accessor.get(IUniverInstanceService));
    if (!target) return false;
    const selection = accessor.get(SheetsSelectionsService).getCurrentLastSelection();
    if (!(selection === null || selection === void 0 ? void 0 : selection.primary)) return false;
    const sheetsNoteModel = accessor.get(SheetsNoteModel);
    const { unitId, subUnitId } = target;
    const { actualColumn, actualRow } = selection.primary;
    const note = sheetsNoteModel.getNote(unitId, subUnitId, {
      row: actualRow,
      col: actualColumn
    });
    if (!note) return false;
    const commandService = accessor.get(ICommandService);
    const undoRedoService = accessor.get(IUndoRedoService);
    const redoMutation = {
      id: RemoveNoteMutation.id,
      params: {
        unitId,
        sheetId: subUnitId,
        noteId: note.id
      }
    };
    const undoMutation = {
      id: UpdateNoteMutation.id,
      params: {
        unitId,
        sheetId: subUnitId,
        row: actualRow,
        col: actualColumn,
        note: { ...note }
      }
    };
    if (commandService.syncExecuteCommand(redoMutation.id, redoMutation.params)) {
      undoRedoService.pushUndoRedo({
        unitID: unitId,
        redoMutations: [redoMutation],
        undoMutations: [undoMutation]
      });
      return true;
    }
    return false;
  }
};
var SheetToggleNotePopupCommand = {
  id: "sheet.command.toggle-note-popup",
  type: CommandType.COMMAND,
  handler: (accessor) => {
    const target = getSheetCommandTarget(accessor.get(IUniverInstanceService));
    if (!target) return false;
    const selection = accessor.get(SheetsSelectionsService).getCurrentLastSelection();
    if (!(selection === null || selection === void 0 ? void 0 : selection.primary)) return false;
    const sheetsNoteModel = accessor.get(SheetsNoteModel);
    const { unitId, subUnitId } = target;
    const { actualColumn, actualRow } = selection.primary;
    const note = sheetsNoteModel.getNote(unitId, subUnitId, {
      row: actualRow,
      col: actualColumn
    });
    if (!note) return false;
    const commandService = accessor.get(ICommandService);
    const undoRedoService = accessor.get(IUndoRedoService);
    const redoMutation = {
      id: ToggleNotePopupMutation.id,
      params: {
        unitId,
        sheetId: subUnitId,
        noteId: note.id
      }
    };
    const undoMutation = {
      id: ToggleNotePopupMutation.id,
      params: {
        unitId,
        sheetId: subUnitId,
        noteId: note.id
      }
    };
    if (commandService.syncExecuteCommand(redoMutation.id, redoMutation.params)) {
      undoRedoService.pushUndoRedo({
        unitID: unitId,
        redoMutations: [redoMutation],
        undoMutations: [undoMutation]
      });
      return true;
    }
    return false;
  }
};
var SheetUpdateNoteCommand = {
  id: "sheet.command.update-note",
  type: CommandType.COMMAND,
  handler: (accessor, params) => {
    const target = getSheetCommandTarget(accessor.get(IUniverInstanceService), params);
    if (!target) return false;
    const commandService = accessor.get(ICommandService);
    const undoRedoService = accessor.get(IUndoRedoService);
    const sheetsNoteModel = accessor.get(SheetsNoteModel);
    const { unitId, subUnitId } = target;
    const { row, col, note } = params;
    const oldNote = sheetsNoteModel.getNote(unitId, subUnitId, {
      noteId: note.id,
      row,
      col
    });
    const redoMutation = {
      id: UpdateNoteMutation.id,
      params: {
        unitId,
        sheetId: subUnitId,
        row,
        col,
        note
      }
    };
    const undoMutations = [];
    if (oldNote) {
      const undoMutation = {
        id: UpdateNoteMutation.id,
        params: {
          unitId,
          sheetId: subUnitId,
          row,
          col,
          note: { ...oldNote }
        }
      };
      undoMutations.push(undoMutation);
    } else {
      const undoMutation = {
        id: RemoveNoteMutation.id,
        params: {
          unitId,
          sheetId: subUnitId,
          row,
          col
        }
      };
      undoMutations.push(undoMutation);
    }
    if (commandService.syncExecuteCommand(redoMutation.id, redoMutation.params)) {
      undoRedoService.pushUndoRedo({
        unitID: unitId,
        redoMutations: [redoMutation],
        undoMutations
      });
      return true;
    }
    return false;
  }
};
var PLUGIN_NAME = "SHEET_NOTE_PLUGIN";
function __decorateParam(paramIndex, decorator) {
  return function(target, key) {
    decorator(target, key, paramIndex);
  };
}
function __decorate(decorators, target, key, desc) {
  var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
  if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
  else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
  return c > 3 && r && Object.defineProperty(target, key, r), r;
}
var SheetsNoteResourceController = class SheetsNoteResourceController2 extends Disposable {
  constructor(_resourceManagerService, _univerInstanceService, _sheetInterceptorService, _sheetsNoteModel) {
    super();
    this._resourceManagerService = _resourceManagerService;
    this._univerInstanceService = _univerInstanceService;
    this._sheetInterceptorService = _sheetInterceptorService;
    this._sheetsNoteModel = _sheetsNoteModel;
    this._initSnapshot();
    this._initSheetChange();
  }
  _initSnapshot() {
    const toJson = (unitId) => {
      const unitMap = this._sheetsNoteModel.getUnitNotes(unitId);
      if (!unitMap) return "";
      const result = {};
      unitMap.forEach((subUnitMap, subUnitId) => {
        const sheetNotes = {};
        subUnitMap.forEach((note) => {
          const { row, col } = note;
          if (!sheetNotes[row]) sheetNotes[row] = {};
          sheetNotes[row][col] = note;
        });
        if (Object.keys(sheetNotes).length > 0) result[subUnitId] = sheetNotes;
      });
      return JSON.stringify(result);
    };
    const parseJson = (json) => {
      if (!json) return {};
      try {
        return JSON.parse(json);
      } catch {
        return {};
      }
    };
    this.disposeWithMe(this._resourceManagerService.registerPluginResource({
      pluginName: PLUGIN_NAME,
      businesses: [UniverType.UNIVER_SHEET],
      toJson: (unitId) => toJson(unitId),
      parseJson: (json) => parseJson(json),
      onUnLoad: (unitId) => {
        this._sheetsNoteModel.deleteUnitNotes(unitId);
      },
      onLoad: (unitId, value) => {
        Object.entries(value).forEach(([sheetId, sheetNotes]) => {
          Object.entries(sheetNotes).forEach(([row, colNotes]) => {
            Object.entries(colNotes).forEach(([col, note]) => {
              this._sheetsNoteModel.updateNote(unitId, sheetId, Number(row), Number(col), note);
            });
          });
        });
      }
    }));
  }
  _initSheetChange() {
    this.disposeWithMe(this._sheetInterceptorService.interceptCommand({ getMutations: (commandInfo) => {
      if (commandInfo.id === RemoveSheetCommand.id) {
        var _getActiveSheet;
        const params = commandInfo.params;
        const unitId = params.unitId || this._univerInstanceService.getCurrentUnitOfType(UniverType.UNIVER_SHEET).getUnitId();
        const subUnitId = params.subUnitId || ((_getActiveSheet = this._univerInstanceService.getCurrentUnitOfType(UniverType.UNIVER_SHEET).getActiveSheet()) === null || _getActiveSheet === void 0 ? void 0 : _getActiveSheet.getSheetId());
        if (!unitId || !subUnitId) return {
          redos: [],
          undos: []
        };
        const sheetNotes = this._sheetsNoteModel.getSheetNotes(unitId, subUnitId);
        if (!sheetNotes) return {
          redos: [],
          undos: []
        };
        const redos = [];
        const undos = [];
        sheetNotes.forEach((note) => {
          redos.push({
            id: RemoveNoteMutation.id,
            params: {
              unitId,
              sheetId: subUnitId,
              noteId: note.id,
              row: note.row,
              col: note.col
            }
          });
          undos.push({
            id: UpdateNoteMutation.id,
            params: {
              unitId,
              sheetId: subUnitId,
              row: note.row,
              col: note.col,
              note
            }
          });
        });
        return {
          redos,
          undos
        };
      } else if (commandInfo.id === CopySheetCommand.id) {
        const { unitId, subUnitId, targetSubUnitId } = commandInfo.params;
        if (!unitId || !subUnitId || !targetSubUnitId) return {
          redos: [],
          undos: []
        };
        const sheetNotes = this._sheetsNoteModel.getSheetNotes(unitId, subUnitId);
        if (!sheetNotes) return {
          redos: [],
          undos: []
        };
        const redos = [];
        const undos = [];
        sheetNotes.forEach((note) => {
          const newNote = {
            ...note,
            id: generateRandomId(6)
          };
          redos.push({
            id: UpdateNoteMutation.id,
            params: {
              unitId,
              sheetId: targetSubUnitId,
              row: newNote.row,
              col: newNote.col,
              note: newNote
            }
          });
          undos.push({
            id: RemoveNoteMutation.id,
            params: {
              unitId,
              sheetId: targetSubUnitId,
              noteId: newNote.id,
              row: newNote.row,
              col: newNote.col
            }
          });
        });
        return {
          redos,
          undos
        };
      }
      return {
        redos: [],
        undos: []
      };
    } }));
  }
};
SheetsNoteResourceController = __decorate([
  __decorateParam(0, IResourceManagerService),
  __decorateParam(1, IUniverInstanceService),
  __decorateParam(2, Inject(SheetInterceptorService)),
  __decorateParam(3, Inject(SheetsNoteModel))
], SheetsNoteResourceController);
var name = "@univerjs/sheets-note";
var version = "0.25.1";
var SHEETS_NOTE_PLUGIN_CONFIG_KEY = "sheets-note.config";
var configSymbol = Symbol(SHEETS_NOTE_PLUGIN_CONFIG_KEY);
var defaultPluginConfig = {};
var SheetsNoteRefRangeController = class SheetsNoteRefRangeController2 extends Disposable {
  constructor(_refRangeService, _sheetsNoteModel, _selectionManagerService, _commandService) {
    super();
    this._refRangeService = _refRangeService;
    this._sheetsNoteModel = _sheetsNoteModel;
    this._selectionManagerService = _selectionManagerService;
    this._commandService = _commandService;
    _defineProperty(this, "_disposableMap", /* @__PURE__ */ new Map());
    _defineProperty(this, "_watcherMap", /* @__PURE__ */ new Map());
    _defineProperty(this, "_handleRangeChange", (unitId, subUnitId, note, row, col, resultRange, silent) => {
      if (!resultRange) return {
        redos: [{
          id: RemoveNoteMutation.id,
          params: {
            unitId,
            sheetId: subUnitId,
            noteId: note.id
          }
        }],
        undos: [{
          id: UpdateNoteMutation.id,
          params: {
            unitId,
            sheetId: subUnitId,
            row,
            col,
            note
          }
        }]
      };
      return {
        redos: [{
          id: UpdateNotePositionMutation.id,
          params: {
            unitId,
            sheetId: subUnitId,
            noteId: note.id,
            newPosition: {
              row: resultRange.startRow,
              col: resultRange.startColumn
            },
            silent
          }
        }],
        undos: [{
          id: UpdateNotePositionMutation.id,
          params: {
            unitId,
            sheetId: subUnitId,
            noteId: note.id,
            newPosition: {
              row,
              col
            },
            note,
            silent
          }
        }]
      };
    });
    this._initData();
    this._initRefRange();
  }
  _getIdWithUnitId(unitId, subUnitId, row, col) {
    return `${unitId}-${subUnitId}-${row}-${col}`;
  }
  _register(unitId, subUnitId, note, row, col) {
    const oldRange = {
      startColumn: col,
      endColumn: col,
      startRow: row,
      endRow: row
    };
    this._disposableMap.set(this._getIdWithUnitId(unitId, subUnitId, row, col), this._refRangeService.registerRefRange(oldRange, (commandInfo) => {
      const resultRanges = handleCommonRangeChangeWithEffectRefCommandsSkipNoInterests(oldRange, commandInfo, { selectionManagerService: this._selectionManagerService });
      const resultRange = Array.isArray(resultRanges) ? resultRanges[0] : resultRanges;
      if (resultRange && resultRange.startColumn === oldRange.startColumn && resultRange.startRow === oldRange.startRow) return {
        undos: [],
        redos: []
      };
      return this._handleRangeChange(unitId, subUnitId, note, row, col, resultRange, false);
    }, unitId, subUnitId));
  }
  _watch(unitId, subUnitId, note, row, col) {
    const oldRange = {
      startColumn: col,
      endColumn: col,
      startRow: row,
      endRow: row
    };
    this._watcherMap.set(this._getIdWithUnitId(unitId, subUnitId, row, col), this._refRangeService.watchRange(unitId, subUnitId, oldRange, (before, after) => {
      const { redos } = this._handleRangeChange(unitId, subUnitId, note, before.startRow, before.startColumn, after, true);
      sequenceExecuteAsync(redos, this._commandService, { onlyLocal: true });
    }, true));
  }
  _unwatch(unitId, subUnitId, row, col) {
    var _this$_watcherMap$get;
    const id = this._getIdWithUnitId(unitId, subUnitId, row, col);
    (_this$_watcherMap$get = this._watcherMap.get(id)) === null || _this$_watcherMap$get === void 0 || _this$_watcherMap$get.dispose();
    this._watcherMap.delete(id);
  }
  _unregister(unitId, subUnitId, row, col) {
    var _this$_disposableMap$;
    const id = this._getIdWithUnitId(unitId, subUnitId, row, col);
    (_this$_disposableMap$ = this._disposableMap.get(id)) === null || _this$_disposableMap$ === void 0 || _this$_disposableMap$.dispose();
    this._disposableMap.delete(id);
  }
  _initData() {
    const unitNotes = this._sheetsNoteModel.getNotes();
    for (const [unitId, unitNote] of unitNotes) for (const [subUnitId, notes] of unitNote) notes.forEach((note) => {
      this._register(unitId, subUnitId, note, note.row, note.col);
      this._watch(unitId, subUnitId, note, note.row, note.col);
    });
  }
  _initRefRange() {
    this.disposeWithMe(this._sheetsNoteModel.change$.subscribe((option) => {
      switch (option.type) {
        case "update": {
          const { unitId, subUnitId, oldNote, newNote } = option;
          const row = newNote ? newNote.row : oldNote.row;
          const col = newNote ? newNote.col : oldNote.col;
          const id = this._getIdWithUnitId(unitId, subUnitId, row, col);
          if (newNote) {
            if (!this._disposableMap.has(id)) {
              this._register(unitId, subUnitId, newNote, row, col);
              this._watch(unitId, subUnitId, newNote, row, col);
            }
          } else {
            this._unregister(unitId, subUnitId, row, col);
            this._unwatch(unitId, subUnitId, row, col);
          }
          break;
        }
        case "ref": {
          const { unitId, subUnitId, oldNote, newNote, silent } = option;
          const { row: oldRow, col: oldCol } = oldNote;
          const { row: newRow, col: newCol } = newNote;
          this._unregister(unitId, subUnitId, oldRow, oldCol);
          if (!silent) {
            this._unwatch(unitId, subUnitId, oldRow, oldCol);
            this._watch(unitId, subUnitId, newNote, newRow, newCol);
          }
          this._register(unitId, subUnitId, newNote, newRow, newCol);
          break;
        }
      }
    }));
  }
};
SheetsNoteRefRangeController = __decorate([
  __decorateParam(0, Inject(RefRangeService)),
  __decorateParam(1, Inject(SheetsNoteModel)),
  __decorateParam(2, Inject(SheetsSelectionsService)),
  __decorateParam(3, ICommandService)
], SheetsNoteRefRangeController);
var SheetsNoteController = class SheetsNoteController2 extends Disposable {
  constructor(_commandService) {
    super();
    this._commandService = _commandService;
    this._initialize();
  }
  _initialize() {
    [
      UpdateNotePositionMutation,
      ToggleNotePopupMutation,
      UpdateNoteMutation,
      RemoveNoteMutation,
      SheetDeleteNoteCommand,
      SheetToggleNotePopupCommand,
      SheetUpdateNoteCommand
    ].forEach((command) => {
      this.disposeWithMe(this._commandService.registerCommand(command));
    });
  }
};
SheetsNoteController = __decorate([__decorateParam(0, ICommandService)], SheetsNoteController);
var UniverSheetsNotePlugin = class UniverSheetsNotePlugin2 extends Plugin {
  constructor(_config = defaultPluginConfig, _configService, _injector) {
    super();
    this._config = _config;
    this._configService = _configService;
    this._injector = _injector;
    const { ...rest } = merge_default({}, defaultPluginConfig, this._config);
    this._configService.setConfig(SHEETS_NOTE_PLUGIN_CONFIG_KEY, rest);
  }
  onStarting() {
    [
      [SheetsNoteModel],
      [SheetsNoteController],
      [SheetsNoteResourceController],
      [SheetsNoteRefRangeController]
    ].forEach((dependency) => {
      this._injector.add(dependency);
    });
    touchDependencies(this._injector, [
      [SheetsNoteModel],
      [SheetsNoteController],
      [SheetsNoteResourceController]
    ]);
  }
  onReady() {
    touchDependencies(this._injector, [[SheetsNoteRefRangeController]]);
  }
};
_defineProperty(UniverSheetsNotePlugin, "pluginName", PLUGIN_NAME);
_defineProperty(UniverSheetsNotePlugin, "packageName", name);
_defineProperty(UniverSheetsNotePlugin, "version", version);
_defineProperty(UniverSheetsNotePlugin, "type", UniverType.UNIVER_SHEET);
UniverSheetsNotePlugin = __decorate([
  DependentOn(UniverSheetsPlugin),
  __decorateParam(1, IConfigService),
  __decorateParam(2, Inject(Injector))
], UniverSheetsNotePlugin);

// node_modules/@univerjs/sheets-note-ui/lib/es/index.js
var import_react = __toESM(require_react());
var import_jsx_runtime = __toESM(require_jsx_runtime());
function __decorateParam2(paramIndex, decorator) {
  return function(target, key) {
    decorator(target, key, paramIndex);
  };
}
function __decorate2(decorators, target, key, desc) {
  var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
  if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
  else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
  return c > 3 && r && Object.defineProperty(target, key, r), r;
}
var SheetsCellContentController = class SheetsCellContentController2 extends Disposable {
  constructor(_sheetInterceptorService, _sheetsNoteModel, _renderManagerService, _univerInstanceService) {
    super();
    this._sheetInterceptorService = _sheetInterceptorService;
    this._sheetsNoteModel = _sheetsNoteModel;
    this._renderManagerService = _renderManagerService;
    this._univerInstanceService = _univerInstanceService;
    this._initViewModelIntercept();
    this._initSkeletonChange();
  }
  _initViewModelIntercept() {
    this.disposeWithMe(this._sheetInterceptorService.intercept(INTERCEPTOR_POINT.CELL_CONTENT, {
      effect: InterceptorEffectEnum.Style,
      handler: (cell, pos, next) => {
        const { row, col, unitId, subUnitId } = pos;
        if (this._sheetsNoteModel.getNote(unitId, subUnitId, {
          row,
          col
        })) {
          if (!cell || cell === pos.rawData) cell = { ...pos.rawData };
          cell.markers = {
            ...cell === null || cell === void 0 ? void 0 : cell.markers,
            tr: {
              color: "#FFBD37",
              size: 6
            }
          };
          return next(cell);
        }
        return next(cell);
      },
      priority: 100
    }));
  }
  _initSkeletonChange() {
    const markSkeletonDirty = () => {
      var _currentRender$mainCo;
      const workbook = this._univerInstanceService.getCurrentUnitOfType(UniverType.UNIVER_SHEET);
      if (!workbook) return;
      const unitId = workbook.getUnitId();
      const currentRender = this._renderManagerService.getRenderById(unitId);
      currentRender === null || currentRender === void 0 || (_currentRender$mainCo = currentRender.mainComponent) === null || _currentRender$mainCo === void 0 || _currentRender$mainCo.makeForceDirty();
    };
    this.disposeWithMe(this._sheetsNoteModel.change$.pipe(debounceTime(16)).subscribe(() => {
      markSkeletonDirty();
    }));
  }
};
SheetsCellContentController = __decorate2([
  __decorateParam2(0, Inject(SheetInterceptorService)),
  __decorateParam2(1, Inject(SheetsNoteModel)),
  __decorateParam2(2, IRenderManagerService),
  __decorateParam2(3, IUniverInstanceService)
], SheetsCellContentController);
var SHEET_NOTE_COMPONENT = "SHEET_NOTE_COMPONENT";
function _typeof2(o) {
  "@babel/helpers - typeof";
  return _typeof2 = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o2) {
    return typeof o2;
  } : function(o2) {
    return o2 && "function" == typeof Symbol && o2.constructor === Symbol && o2 !== Symbol.prototype ? "symbol" : typeof o2;
  }, _typeof2(o);
}
function toPrimitive2(t, r) {
  if ("object" != _typeof2(t) || !t) return t;
  var e = t[Symbol.toPrimitive];
  if (void 0 !== e) {
    var i = e.call(t, r || "default");
    if ("object" != _typeof2(i)) return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return ("string" === r ? String : Number)(t);
}
function toPropertyKey2(t) {
  var i = toPrimitive2(t, "string");
  return "symbol" == _typeof2(i) ? i : i + "";
}
function _defineProperty2(e, r, t) {
  return (r = toPropertyKey2(r)) in e ? Object.defineProperty(e, r, {
    value: t,
    enumerable: true,
    configurable: true,
    writable: true
  }) : e[r] = t, e;
}
var SheetsNotePopupService = class SheetsNotePopupService2 extends Disposable {
  get activePopup() {
    return this._activePopup;
  }
  constructor(_zenZoneService, _cellPopupManagerService) {
    super();
    this._zenZoneService = _zenZoneService;
    this._cellPopupManagerService = _cellPopupManagerService;
    _defineProperty2(this, "_lastPopup", null);
    _defineProperty2(this, "_activePopup", void 0);
    _defineProperty2(this, "_activePopup$", new BehaviorSubject(null));
    _defineProperty2(this, "activePopup$", this._activePopup$.asObservable());
    this._initZenVisible();
    this.disposeWithMe(() => {
      this._activePopup$.complete();
    });
  }
  _initZenVisible() {
    this.disposeWithMe(this._zenZoneService.visible$.subscribe((visible) => {
      if (visible) this.hidePopup();
    }));
  }
  dispose() {
    super.dispose();
    this.hidePopup();
  }
  showPopup(location, onHide) {
    var _this$activePopup;
    const { row, col, unitId, subUnitId } = location;
    if (this._activePopup && row === this._activePopup.row && col === this._activePopup.col && unitId === this._activePopup.unitId && subUnitId === ((_this$activePopup = this.activePopup) === null || _this$activePopup === void 0 ? void 0 : _this$activePopup.subUnitId)) {
      this._activePopup = location;
      this._activePopup$.next(location);
      return;
    }
    if (this._lastPopup) this._lastPopup.dispose();
    if (this._zenZoneService.visible) return;
    this._activePopup = location;
    this._activePopup$.next(location);
    const popupDisposable = this._cellPopupManagerService.showPopup({
      unitId,
      subUnitId,
      row,
      col
    }, {
      componentKey: SHEET_NOTE_COMPONENT,
      onClickOutside: () => {
        this.hidePopup();
      },
      direction: "horizontal",
      extraProps: { location },
      priority: 3
    });
    if (!popupDisposable) throw new Error("[SheetsNotePopupService]: cannot show popup!");
    const disposableCollection = new DisposableCollection();
    disposableCollection.add(popupDisposable);
    disposableCollection.add({ dispose: () => {
      onHide === null || onHide === void 0 || onHide();
    } });
    this._lastPopup = disposableCollection;
  }
  hidePopup(force) {
    if (!this._activePopup) return;
    if (!force && !this._activePopup.temp) return;
    if (this._lastPopup) this._lastPopup.dispose();
    this._lastPopup = null;
    this._activePopup = null;
    this._activePopup$.next(null);
  }
  persistPopup() {
    if (!this._activePopup || !this._activePopup.temp) return;
    this._activePopup = {
      ...this._activePopup,
      temp: false
    };
    this._activePopup$.next(this._activePopup);
  }
};
SheetsNotePopupService = __decorate2([__decorateParam2(0, IZenZoneService), __decorateParam2(1, Inject(CellPopupManagerService))], SheetsNotePopupService);
var SheetsNotePopupController = class SheetsNotePopupController2 extends Disposable {
  constructor(_sheetsNotePopupService, _sheetsNoteModel, _sheetSelectionService, _editorBridgeService, _renderManagerService, _hoverManagerService) {
    super();
    this._sheetsNotePopupService = _sheetsNotePopupService;
    this._sheetsNoteModel = _sheetsNoteModel;
    this._sheetSelectionService = _sheetSelectionService;
    this._editorBridgeService = _editorBridgeService;
    this._renderManagerService = _renderManagerService;
    this._hoverManagerService = _hoverManagerService;
    _defineProperty2(this, "_isSwitchingSheet", false);
    this._initSelectionUpdateListener();
    this._initEditorBridge();
    this._initHoverEvent();
    this._initDeleteNoteListener();
  }
  _handleSelectionChange(selections, unitId, subUnitId) {
    var _selections$, _render$with$getSkele, _range$rangeType;
    const range = (_selections$ = selections[0]) === null || _selections$ === void 0 ? void 0 : _selections$.range;
    const render = this._renderManagerService.getRenderById(unitId);
    const skeleton = render === null || render === void 0 || (_render$with$getSkele = render.with(SheetSkeletonManagerService).getSkeletonParam(subUnitId)) === null || _render$with$getSkele === void 0 ? void 0 : _render$with$getSkele.skeleton;
    if (!skeleton) return;
    if (!range) return;
    const actualCell = skeleton.getCellWithCoordByIndex(range.startRow, range.startColumn);
    if ((((_range$rangeType = range.rangeType) !== null && _range$rangeType !== void 0 ? _range$rangeType : RANGE_TYPE.NORMAL) !== RANGE_TYPE.NORMAL || range.endColumn - range.startColumn > 0 || range.endRow - range.startRow > 0) && !((actualCell.isMerged || actualCell.isMergedMainCell) && Rectangle.equals(actualCell.mergeInfo, range))) {
      this._sheetsNotePopupService.hidePopup();
      return;
    }
    const row = actualCell.actualRow;
    const col = actualCell.actualColumn;
    const note = this._sheetsNoteModel.getNote(unitId, subUnitId, {
      row,
      col
    });
    if (note === null || note === void 0 ? void 0 : note.show) return;
    if (note) this._sheetsNotePopupService.showPopup({
      unitId,
      subUnitId,
      noteId: note.id,
      row,
      col
    });
    else this._sheetsNotePopupService.hidePopup(true);
  }
  _initSelectionUpdateListener() {
    this.disposeWithMe(this._sheetSelectionService.selectionMoveEnd$.subscribe((selections) => {
      if (this._isSwitchingSheet) return;
      const current = this._sheetSelectionService.currentSelectionParam;
      if (!current) return;
      this._handleSelectionChange(selections, current.unitId, current.sheetId);
    }));
  }
  _initEditorBridge() {
    this.disposeWithMe(this._editorBridgeService.visible$.subscribe((visible) => {
      if (visible.visible) this._sheetsNotePopupService.hidePopup(true);
    }));
  }
  _initHoverEvent() {
    this.disposeWithMe(this._hoverManagerService.currentCell$.pipe(debounceTime(100)).subscribe((cell) => {
      var _render$with$getSkele2;
      if (!(cell === null || cell === void 0 ? void 0 : cell.location)) return;
      const { unitId, subUnitId, row, col } = cell.location;
      const render = this._renderManagerService.getRenderById(unitId);
      const skeleton = render === null || render === void 0 || (_render$with$getSkele2 = render.with(SheetSkeletonManagerService).getSkeletonParam(subUnitId)) === null || _render$with$getSkele2 === void 0 ? void 0 : _render$with$getSkele2.skeleton;
      let targetRow = row;
      let targetCol = col;
      let note = this._sheetsNoteModel.getNote(unitId, subUnitId, {
        row: targetRow,
        col: targetCol
      });
      if (!note && skeleton) {
        const { startRow, endRow, startColumn, endColumn } = skeleton.getCellWithCoordByIndex(row, col).mergeInfo;
        if (startRow !== endRow || startColumn !== endColumn) {
          const sheetNotes = this._sheetsNoteModel.getSheetNotes(unitId, subUnitId);
          if (sheetNotes) {
            for (const [_id, _note] of sheetNotes) if (_note.row >= startRow && _note.row <= endRow && _note.col >= startColumn && _note.col <= endColumn) {
              note = _note;
              targetRow = _note.row;
              targetCol = _note.col;
              break;
            }
          }
        }
      }
      if (note === null || note === void 0 ? void 0 : note.show) return;
      if (note) this._sheetsNotePopupService.showPopup({
        unitId,
        subUnitId,
        noteId: note.id,
        row: targetRow,
        col: targetCol,
        temp: true
      });
      else this._sheetsNotePopupService.hidePopup();
    }));
  }
  _initDeleteNoteListener() {
    this.disposeWithMe(this._sheetsNoteModel.change$.subscribe((change) => {
      if (!this._sheetsNotePopupService.activePopup) return;
      const { unitId, subUnitId, noteId, row, col } = this._sheetsNotePopupService.activePopup;
      const { oldNote, newNote } = change;
      if (newNote === null && change.unitId === unitId && change.subUnitId === subUnitId && ((oldNote === null || oldNote === void 0 ? void 0 : oldNote.id) && oldNote.id === noteId || (oldNote === null || oldNote === void 0 ? void 0 : oldNote.row) === row && oldNote.col === col)) this._sheetsNotePopupService.hidePopup(true);
    }));
  }
};
SheetsNotePopupController = __decorate2([
  __decorateParam2(0, Inject(SheetsNotePopupService)),
  __decorateParam2(1, Inject(SheetsNoteModel)),
  __decorateParam2(2, Inject(SheetsSelectionsService)),
  __decorateParam2(3, IEditorBridgeService),
  __decorateParam2(4, IRenderManagerService),
  __decorateParam2(5, Inject(HoverManagerService))
], SheetsNotePopupController);
var name2 = "@univerjs/sheets-note-ui";
var version2 = "0.25.1";
var SHEETS_NOTE_UI_PLUGIN_CONFIG_KEY = "sheets-note-ui.config";
var configSymbol2 = Symbol(SHEETS_NOTE_UI_PLUGIN_CONFIG_KEY);
var defaultPluginConfig2 = {};
var SheetsNoteAttachmentController = class SheetsNoteAttachmentController2 extends Disposable {
  constructor(_sheetsNoteModel, _univerInstanceService, _cellPopupManagerService, _sheetsNotePopupService) {
    super();
    this._sheetsNoteModel = _sheetsNoteModel;
    this._univerInstanceService = _univerInstanceService;
    this._cellPopupManagerService = _cellPopupManagerService;
    this._sheetsNotePopupService = _sheetsNotePopupService;
    _defineProperty2(this, "_noteMatrix", new ObjectMatrix());
    this._initNoteChangeListener();
  }
  _showPopup(unitId, sheetId, row, col) {
    this._sheetsNotePopupService.hidePopup(true);
    return this._cellPopupManagerService.showPopup({
      unitId,
      subUnitId: sheetId,
      row,
      col
    }, {
      componentKey: SHEET_NOTE_COMPONENT,
      direction: "horizontal",
      extraProps: { location: {
        unitId,
        subUnitId: sheetId,
        row,
        col
      } },
      priority: 3
    });
  }
  dispose() {
    super.dispose();
    this._noteMatrix.forValue((_, __, disposable) => {
      disposable.dispose();
    });
  }
  _initSheet(targetUnitId, targetSheetId) {
    var _this$_sheetsNoteMode;
    this._noteMatrix.forValue((_, __, disposable) => {
      disposable.dispose();
    });
    this._noteMatrix = new ObjectMatrix();
    const handleNote = (unitId, sheetId, row, col, note) => {
      const matrix = this._noteMatrix;
      const disposable = matrix.getValue(row, col);
      if (note === null || note === void 0 ? void 0 : note.show) {
        if (!disposable) {
          const newDisposable = this._showPopup(unitId, sheetId, row, col);
          if (newDisposable) matrix.setValue(row, col, newDisposable);
        }
      } else if (disposable) {
        disposable.dispose();
        matrix.realDeleteValue(row, col);
      }
    };
    (_this$_sheetsNoteMode = this._sheetsNoteModel.getSheetNotes(targetUnitId, targetSheetId)) === null || _this$_sheetsNoteMode === void 0 || _this$_sheetsNoteMode.forEach((note) => {
      handleNote(targetUnitId, targetSheetId, note.row, note.col, note);
    });
    return this._sheetsNoteModel.change$.subscribe((change) => {
      if (change.unitId !== targetUnitId || change.subUnitId !== targetSheetId) return;
      switch (change.type) {
        case "ref": {
          const { unitId, subUnitId, oldNote, newNote } = change;
          if (!newNote.show) return;
          const matrix = this._noteMatrix;
          const { row: oldRow, col: oldCol } = oldNote;
          const { row: newRow, col: newCol } = newNote;
          const disposable = matrix.getValue(oldRow, oldCol);
          if (disposable) {
            disposable.dispose();
            matrix.realDeleteValue(oldRow, oldCol);
          }
          const newDisposable = this._showPopup(unitId, subUnitId, newRow, newCol);
          if (newDisposable) matrix.setValue(newRow, newCol, newDisposable);
          break;
        }
        case "update": {
          const { unitId, subUnitId, oldNote, newNote } = change;
          handleNote(unitId, subUnitId, newNote ? newNote.row : oldNote.row, newNote ? newNote.col : oldNote.col, newNote);
          break;
        }
        default:
          break;
      }
    });
  }
  _initNoteChangeListener() {
    this.disposeWithMe(this._univerInstanceService.getCurrentTypeOfUnit$(UniverType.UNIVER_SHEET).pipe(switchMap((workbook) => {
      var _workbook$activeSheet;
      return (_workbook$activeSheet = workbook === null || workbook === void 0 ? void 0 : workbook.activeSheet$) !== null && _workbook$activeSheet !== void 0 ? _workbook$activeSheet : of(null);
    })).subscribe((sheet) => {
      if (sheet) {
        const disposable = this._initSheet(sheet.getUnitId(), sheet.getSheetId());
        return () => {
          disposable.unsubscribe();
        };
      } else {
        this._noteMatrix.forValue((_, __, disposable) => {
          disposable.dispose();
        });
        this._noteMatrix = new ObjectMatrix();
      }
    }));
  }
};
SheetsNoteAttachmentController = __decorate2([
  __decorateParam2(0, Inject(SheetsNoteModel)),
  __decorateParam2(1, Inject(IUniverInstanceService)),
  __decorateParam2(2, Inject(CellPopupManagerService)),
  __decorateParam2(3, Inject(SheetsNotePopupService))
], SheetsNoteAttachmentController);
var AddNotePopupOperation = {
  id: "sheet.operation.add-note-popup",
  type: CommandType.OPERATION,
  handler: async (accessor, params) => {
    var _params$trigger;
    const selectionService = accessor.get(SheetsSelectionsService);
    const notePopupService = accessor.get(SheetsNotePopupService);
    const workbook = accessor.get(IUniverInstanceService).getCurrentUnitOfType(UniverType.UNIVER_SHEET);
    if (!workbook) return false;
    const worksheet = workbook.getActiveSheet();
    const lastSelection = selectionService.getCurrentLastSelection();
    if (!(lastSelection === null || lastSelection === void 0 ? void 0 : lastSelection.primary)) return false;
    const { primary } = lastSelection;
    notePopupService.showPopup({
      unitId: workbook.getUnitId(),
      subUnitId: worksheet.getSheetId(),
      row: primary.actualRow,
      col: primary.actualColumn,
      temp: false,
      trigger: (_params$trigger = params === null || params === void 0 ? void 0 : params.trigger) !== null && _params$trigger !== void 0 ? _params$trigger : "add-note"
    });
    return true;
  }
};
function getHasNote$(accessor) {
  const sheetsSelectionsService = accessor.get(SheetsSelectionsService);
  const univerInstanceService = accessor.get(IUniverInstanceService);
  return sheetsSelectionsService.selectionMoveEnd$.pipe(map(() => {
    const selection = sheetsSelectionsService.getCurrentLastSelection();
    if (!(selection === null || selection === void 0 ? void 0 : selection.primary)) return false;
    const target = getSheetCommandTarget(univerInstanceService);
    if (!target) return false;
    const { actualColumn, actualRow } = selection.primary;
    const noteModel = accessor.get(SheetsNoteModel);
    return Boolean(noteModel.getNote(target.unitId, target.subUnitId, {
      row: actualRow,
      col: actualColumn
    }));
  }));
}
function sheetNoteContextMenuFactory(accessor) {
  return {
    id: AddNotePopupOperation.id,
    type: MenuItemType.BUTTON,
    title: "sheets-note-ui.rightClick.addNote",
    icon: "AddNoteIcon",
    hidden$: combineLatest([getMenuHiddenObservable(accessor, UniverType.UNIVER_SHEET), getHasNote$(accessor)]).pipe(map(([hidden, hasNote]) => hidden || hasNote)),
    disabled$: getCurrentRangeDisable$(accessor, {
      workbookTypes: [WorkbookEditablePermission],
      worksheetTypes: [WorksheetEditPermission]
    }),
    commandId: AddNotePopupOperation.id
  };
}
function sheetDeleteNoteMenuFactory(accessor) {
  return {
    id: SheetDeleteNoteCommand.id,
    type: MenuItemType.BUTTON,
    title: "sheets-note-ui.rightClick.deleteNote",
    icon: "DeleteNoteIcon",
    hidden$: getHasNote$(accessor).pipe(map((hasNote) => !hasNote)),
    disabled$: getCurrentRangeDisable$(accessor, {
      workbookTypes: [WorkbookEditablePermission],
      worksheetTypes: [WorksheetEditPermission]
    })
  };
}
function sheetNoteToggleMenuFactory(accessor) {
  return {
    id: SheetToggleNotePopupCommand.id,
    type: MenuItemType.BUTTON,
    title: "sheets-note-ui.rightClick.toggleNote",
    icon: "HideNoteIcon",
    hidden$: getHasNote$(accessor).pipe(map((hasNote) => !hasNote))
  };
}
var menuSchema = { [ContextMenuPosition.MAIN_AREA]: { [ContextMenuGroup.OTHERS]: {
  order: 0,
  [AddNotePopupOperation.id]: {
    order: 0,
    menuItemFactory: sheetNoteContextMenuFactory
  },
  [SheetDeleteNoteCommand.id]: {
    order: 0,
    menuItemFactory: sheetDeleteNoteMenuFactory
  },
  [SheetToggleNotePopupCommand.id]: {
    order: 0,
    menuItemFactory: sheetNoteToggleMenuFactory
  }
} } };
var SheetsNote = (props) => {
  var _popup$extraProps;
  const { popup } = props;
  const noteModel = useDependency(SheetsNoteModel);
  const localeService = useDependency(LocaleService);
  const renderManagerService = useDependency(IRenderManagerService);
  const notePopupService = useDependency(SheetsNotePopupService);
  const config = useConfigValue(SHEETS_NOTE_UI_PLUGIN_CONFIG_KEY);
  const activePopup = (_popup$extraProps = popup.extraProps) === null || _popup$extraProps === void 0 ? void 0 : _popup$extraProps.location;
  if (!activePopup) {
    console.error("Popup extraProps or location is undefined.");
    return null;
  }
  const textareaRef = (0, import_react.useRef)(null);
  const currentRender = renderManagerService.getRenderById(activePopup.unitId);
  const [note, setNote] = (0, import_react.useState)(null);
  (0, import_react.useEffect)(() => {
    var _ref, _note$width, _config$defaultNoteSi, _ref2, _note$height, _config$defaultNoteSi2;
    const { unitId, subUnitId, row, col } = activePopup;
    const note2 = noteModel.getNote(unitId, subUnitId, {
      row,
      col
    });
    const width = (_ref = (_note$width = note2 === null || note2 === void 0 ? void 0 : note2.width) !== null && _note$width !== void 0 ? _note$width : config === null || config === void 0 || (_config$defaultNoteSi = config.defaultNoteSize) === null || _config$defaultNoteSi === void 0 ? void 0 : _config$defaultNoteSi.width) !== null && _ref !== void 0 ? _ref : 160;
    const height = (_ref2 = (_note$height = note2 === null || note2 === void 0 ? void 0 : note2.height) !== null && _note$height !== void 0 ? _note$height : config === null || config === void 0 || (_config$defaultNoteSi2 = config.defaultNoteSize) === null || _config$defaultNoteSi2 === void 0 ? void 0 : _config$defaultNoteSi2.height) !== null && _ref2 !== void 0 ? _ref2 : 72;
    if (!note2) {
      const initNote = {
        id: generateRandomId(6),
        width,
        height,
        note: ""
      };
      setNote(initNote);
      updateNote(initNote);
    } else setNote(note2);
    if (textareaRef.current) {
      textareaRef.current.style.width = `${width}px`;
      textareaRef.current.style.height = `${height}px`;
    }
  }, [activePopup, textareaRef]);
  (0, import_react.useEffect)(() => {
    if (!activePopup || activePopup.temp || !activePopup.trigger) return;
    if (!textareaRef.current) return;
    const focusId = requestAnimationFrame(() => {
      var _textareaRef$current;
      (_textareaRef$current = textareaRef.current) === null || _textareaRef$current === void 0 || _textareaRef$current.focus();
    });
    return () => cancelAnimationFrame(focusId);
  }, [activePopup]);
  const commandService = useDependency(ICommandService);
  const updateNote = useDebounceFn((newNote) => {
    if (!activePopup) return;
    const { unitId, subUnitId, row, col } = activePopup;
    if (!commandService.syncExecuteCommand(SheetUpdateNoteCommand.id, {
      unitId,
      sheetId: subUnitId,
      row,
      col,
      note: newNote
    })) {
      const oldNote = noteModel.getNote(unitId, subUnitId, {
        noteId: newNote.id,
        row,
        col
      });
      if (oldNote) setNote(oldNote);
      else notePopupService.hidePopup(true);
    }
  });
  const handleNoteChange = (0, import_react.useCallback)((value) => {
    if (!note) return;
    if (value === note.note) return;
    const newNote = {
      ...note,
      note: value
    };
    setNote(newNote);
    updateNote(newNote);
  }, [note]);
  const handleResize = (0, import_react.useCallback)((width, height) => {
    if (!note) return;
    if (width === note.width && height === note.height) return;
    const newNote = {
      ...note,
      width,
      height
    };
    setNote(newNote);
    updateNote(newNote);
  }, [note]);
  return (0, import_jsx_runtime.jsx)(Textarea, {
    ref: textareaRef,
    "data-u-comp": "note-textarea",
    className: clsx("univer-ml-px univer-min-h-1 univer-min-w-1 univer-bg-white !univer-text-sm univer-shadow dark:!univer-bg-gray-800"),
    value: note === null || note === void 0 ? void 0 : note.note,
    placeholder: localeService.t("sheets-note-ui.note.placeholder"),
    onResize: handleResize,
    onValueChange: handleNoteChange,
    onWheel: (e) => {
      if (document.activeElement !== textareaRef.current) currentRender.engine.getCanvasElement().dispatchEvent(new WheelEvent(e.type, e.nativeEvent));
    }
  });
};
var SheetsNoteUIController = class SheetsNoteUIController2 extends Disposable {
  constructor(_componentManager, _menuManagerService, _commandService) {
    super();
    this._componentManager = _componentManager;
    this._menuManagerService = _menuManagerService;
    this._commandService = _commandService;
    this._initComponents();
    this._initMenu();
    this._initCommands();
  }
  _initComponents() {
    [
      [SHEET_NOTE_COMPONENT, SheetsNote],
      ["AddNoteIcon", AddNoteIcon],
      ["DeleteNoteIcon", DeleteNoteIcon],
      ["HideNoteIcon", HideNoteIcon]
    ].forEach(([key, comp]) => {
      this.disposeWithMe(this._componentManager.register(key, comp));
    });
  }
  _initMenu() {
    this._menuManagerService.mergeMenu(menuSchema);
  }
  _initCommands() {
    this._commandService.registerCommand(AddNotePopupOperation);
  }
};
SheetsNoteUIController = __decorate2([
  __decorateParam2(0, Inject(ComponentManager)),
  __decorateParam2(1, Inject(IMenuManagerService)),
  __decorateParam2(2, ICommandService)
], SheetsNoteUIController);
var UniverSheetsNoteUIPlugin = class UniverSheetsNoteUIPlugin2 extends Plugin {
  constructor(_config = defaultPluginConfig2, _injector, _configService) {
    super();
    this._config = _config;
    this._injector = _injector;
    this._configService = _configService;
    const { menu, ...rest } = merge_default({}, defaultPluginConfig2, this._config);
    if (menu) this._configService.setConfig("menu", menu, { merge: true });
    this._configService.setConfig(SHEETS_NOTE_UI_PLUGIN_CONFIG_KEY, rest);
  }
  onStarting() {
    [
      [SheetsNotePopupService],
      [SheetsCellContentController],
      [SheetsNotePopupController],
      [SheetsNoteUIController],
      [SheetsNoteAttachmentController]
    ].forEach((dependency) => {
      this._injector.add(dependency);
    });
  }
  onReady() {
    touchDependencies(this._injector, [[SheetsNoteUIController], [SheetsCellContentController]]);
  }
  onRendered() {
    touchDependencies(this._injector, [[SheetsNotePopupController], [SheetsNoteAttachmentController]]);
  }
};
_defineProperty2(UniverSheetsNoteUIPlugin, "pluginName", "SHEET_NOTE_UI_PLUGIN");
_defineProperty2(UniverSheetsNoteUIPlugin, "packageName", name2);
_defineProperty2(UniverSheetsNoteUIPlugin, "version", version2);
_defineProperty2(UniverSheetsNoteUIPlugin, "type", UniverType.UNIVER_SHEET);
UniverSheetsNoteUIPlugin = __decorate2([
  DependentOn(UniverSheetsNotePlugin),
  __decorateParam2(1, Inject(Injector)),
  __decorateParam2(2, IConfigService)
], UniverSheetsNoteUIPlugin);

// node_modules/@univerjs/sheets-note/lib/es/facade.js
var FSheetsNoteEventNameMixin = class extends FEventName {
  get SheetNoteAdd() {
    return "SheetNoteAdd";
  }
  get SheetNoteDelete() {
    return "SheetNoteDelete";
  }
  get SheetNoteUpdate() {
    return "SheetNoteUpdate";
  }
  get SheetNoteShow() {
    return "SheetNoteShow";
  }
  get SheetNoteHide() {
    return "SheetNoteHide";
  }
  get BeforeSheetNoteAdd() {
    return "BeforeSheetNoteAdd";
  }
  get BeforeSheetNoteDelete() {
    return "BeforeSheetNoteDelete";
  }
  get BeforeSheetNoteUpdate() {
    return "BeforeSheetNoteUpdate";
  }
  get BeforeSheetNoteShow() {
    return "BeforeSheetNoteShow";
  }
  get BeforeSheetNoteHide() {
    return "BeforeSheetNoteHide";
  }
};
FEventName.extend(FSheetsNoteEventNameMixin);
var FRangeSheetsNoteMixin = class extends FRange {
  createOrUpdateNote(note) {
    this._commandService.syncExecuteCommand(UpdateNoteMutation.id, {
      unitId: this.getUnitId(),
      sheetId: this.getSheetId(),
      row: this.getRow(),
      col: this.getColumn(),
      note
    });
    return this;
  }
  deleteNote() {
    this._commandService.syncExecuteCommand(RemoveNoteMutation.id, {
      unitId: this.getUnitId(),
      sheetId: this.getSheetId(),
      row: this.getRow(),
      col: this.getColumn()
    });
    return this;
  }
  getNote() {
    return this._injector.get(SheetsNoteModel).getNote(this.getUnitId(), this.getSheetId(), {
      row: this.getRow(),
      col: this.getColumn()
    });
  }
};
FRange.extend(FRangeSheetsNoteMixin);
var FUniverSheetsNoteMixin = class extends FUniver {
  _initialize(injector) {
    const commandService = injector.get(ICommandService);
    this.disposeWithMe(this.registerEventHandler(this.Event.SheetNoteAdd, () => {
      return injector.get(SheetsNoteModel).change$.subscribe((change) => {
        if (change.type === "update" && !change.oldNote && change.newNote) {
          const { unitId, subUnitId, newNote } = change;
          const target = this.getSheetTarget(unitId, subUnitId);
          if (!target) return;
          const { workbook, worksheet } = target;
          const eventParams = {
            workbook,
            worksheet,
            row: newNote.row,
            col: newNote.col,
            note: newNote
          };
          this.fireEvent(this.Event.SheetNoteAdd, eventParams);
        }
      });
    }));
    this.disposeWithMe(this.registerEventHandler(this.Event.SheetNoteDelete, () => {
      return injector.get(SheetsNoteModel).change$.subscribe((change) => {
        if (change.type === "update" && change.oldNote && !change.newNote) {
          const { unitId, subUnitId, oldNote } = change;
          const target = this.getSheetTarget(unitId, subUnitId);
          if (!target) return;
          const { workbook, worksheet } = target;
          const eventParams = {
            workbook,
            worksheet,
            row: oldNote.row,
            col: oldNote.col,
            oldNote
          };
          this.fireEvent(this.Event.SheetNoteDelete, eventParams);
        }
      });
    }));
    this.disposeWithMe(this.registerEventHandler(this.Event.SheetNoteUpdate, () => {
      return injector.get(SheetsNoteModel).change$.subscribe((change) => {
        if (change.type === "update" && change.oldNote && change.newNote) {
          const { unitId, subUnitId, oldNote, newNote } = change;
          const target = this.getSheetTarget(unitId, subUnitId);
          if (!target) return;
          const { workbook, worksheet } = target;
          const eventParams = {
            workbook,
            worksheet,
            row: newNote.row,
            col: newNote.col,
            note: newNote,
            oldNote
          };
          this.fireEvent(this.Event.SheetNoteUpdate, eventParams);
        }
      });
    }));
    this.disposeWithMe(this.registerEventHandler(this.Event.SheetNoteShow, () => {
      return injector.get(SheetsNoteModel).change$.subscribe((change) => {
        if (change.type === "update" && change.oldNote && change.newNote && !change.oldNote.show && change.newNote.show) {
          const { unitId, subUnitId, newNote } = change;
          const target = this.getSheetTarget(unitId, subUnitId);
          if (!target) return;
          const { workbook, worksheet } = target;
          const eventParams = {
            workbook,
            worksheet,
            row: newNote.row,
            col: newNote.col
          };
          this.fireEvent(this.Event.SheetNoteShow, eventParams);
        }
      });
    }));
    this.disposeWithMe(this.registerEventHandler(this.Event.SheetNoteHide, () => {
      return injector.get(SheetsNoteModel).change$.subscribe((change) => {
        if (change.type === "update" && change.oldNote && change.newNote && change.oldNote.show && !change.newNote.show) {
          const { unitId, subUnitId, newNote } = change;
          const target = this.getSheetTarget(unitId, subUnitId);
          if (!target) return;
          const { workbook, worksheet } = target;
          const eventParams = {
            workbook,
            worksheet,
            row: newNote.row,
            col: newNote.col
          };
          this.fireEvent(this.Event.SheetNoteHide, eventParams);
        }
      });
    }));
    this.disposeWithMe(this.registerEventHandler(this.Event.BeforeSheetNoteAdd, () => commandService.beforeCommandExecuted((command) => {
      if (command.id === SheetUpdateNoteCommand.id) {
        const { unitId, sheetId, row, col, note } = command.params;
        const target = this.getSheetTarget(unitId, sheetId);
        if (!target) return;
        if (injector.get(SheetsNoteModel).getNote(unitId, sheetId, {
          noteId: note.id,
          row,
          col
        })) return;
        const { workbook, worksheet } = target;
        const eventParams = {
          workbook,
          worksheet,
          row,
          col,
          note
        };
        if (this.fireEvent(this.Event.BeforeSheetNoteAdd, eventParams)) throw new CanceledError();
      }
    })));
    this.disposeWithMe(this.registerEventHandler(this.Event.BeforeSheetNoteDelete, () => commandService.beforeCommandExecuted((command) => {
      if (command.id === SheetDeleteNoteCommand.id) {
        const { unitId, sheetId, row, col } = command.params;
        const target = this.getSheetTarget(unitId, sheetId);
        if (!target) return;
        if (row === void 0 || col === void 0) return;
        const oldNote = injector.get(SheetsNoteModel).getNote(unitId, sheetId, {
          row,
          col
        });
        if (!oldNote) return;
        const { workbook, worksheet } = target;
        const eventParams = {
          workbook,
          worksheet,
          row,
          col,
          oldNote
        };
        if (this.fireEvent(this.Event.BeforeSheetNoteDelete, eventParams)) throw new CanceledError();
      }
    })));
    this.disposeWithMe(this.registerEventHandler(this.Event.BeforeSheetNoteUpdate, () => commandService.beforeCommandExecuted((command) => {
      if (command.id === SheetUpdateNoteCommand.id) {
        const { unitId, sheetId, row, col, note } = command.params;
        const target = this.getSheetTarget(unitId, sheetId);
        if (!target) return;
        const oldNote = injector.get(SheetsNoteModel).getNote(unitId, sheetId, {
          row,
          col
        });
        if (!oldNote) return;
        const { workbook, worksheet } = target;
        const eventParams = {
          workbook,
          worksheet,
          row,
          col,
          note,
          oldNote
        };
        if (this.fireEvent(this.Event.BeforeSheetNoteUpdate, eventParams)) throw new CanceledError();
      }
    })));
    this.disposeWithMe(this.registerEventHandler(this.Event.BeforeSheetNoteShow, () => commandService.beforeCommandExecuted((command) => {
      if (command.id === SheetToggleNotePopupCommand.id) {
        const target = getSheetCommandTarget(injector.get(IUniverInstanceService));
        if (!target) return;
        const { unitId, subUnitId } = target;
        const workbook = this.getUniverSheet(unitId);
        if (!workbook) return;
        const worksheet = workbook.getSheetBySheetId(subUnitId);
        if (!worksheet) return;
        const selection = injector.get(SheetsSelectionsService).getCurrentLastSelection();
        if (!(selection === null || selection === void 0 ? void 0 : selection.primary)) return;
        const sheetsNoteModel = injector.get(SheetsNoteModel);
        const { actualColumn, actualRow } = selection.primary;
        const note = sheetsNoteModel.getNote(unitId, subUnitId, {
          row: actualRow,
          col: actualColumn
        });
        if (!note || note.show) return;
        const eventParams = {
          workbook,
          worksheet,
          row: actualRow,
          col: actualColumn
        };
        if (this.fireEvent(this.Event.BeforeSheetNoteShow, eventParams)) throw new CanceledError();
      }
    })));
    this.disposeWithMe(this.registerEventHandler(this.Event.BeforeSheetNoteHide, () => commandService.beforeCommandExecuted((command) => {
      if (command.id === SheetToggleNotePopupCommand.id) {
        const target = getSheetCommandTarget(injector.get(IUniverInstanceService));
        if (!target) return;
        const { unitId, subUnitId } = target;
        const workbook = this.getUniverSheet(unitId);
        if (!workbook) return;
        const worksheet = workbook.getSheetBySheetId(subUnitId);
        if (!worksheet) return;
        const selection = injector.get(SheetsSelectionsService).getCurrentLastSelection();
        if (!(selection === null || selection === void 0 ? void 0 : selection.primary)) return;
        const sheetsNoteModel = injector.get(SheetsNoteModel);
        const { actualColumn, actualRow } = selection.primary;
        const note = sheetsNoteModel.getNote(unitId, subUnitId, {
          row: actualRow,
          col: actualColumn
        });
        if (!note || !note.show) return;
        const eventParams = {
          workbook,
          worksheet,
          row: actualRow,
          col: actualColumn
        };
        if (this.fireEvent(this.Event.BeforeSheetNoteHide, eventParams)) throw new CanceledError();
      }
    })));
  }
};
FUniver.extend(FUniverSheetsNoteMixin);
var FWorksheetNoteMixin = class extends FWorksheet {
  getNotes() {
    const notes = this._injector.get(SheetsNoteModel).getSheetNotes(this.getWorkbook().getUnitId(), this.getSheetId());
    if (!notes) return [];
    return Array.from(notes.values()).map((note) => ({ ...note }));
  }
};
FWorksheet.extend(FWorksheetNoteMixin);

// node_modules/@univerjs/preset-sheets-note/lib/es/index.js
function m() {
  return {
    plugins: [
      UniverSheetsNotePlugin,
      UniverSheetsNoteUIPlugin
    ].filter((e) => !!e)
  };
}
export {
  RemoveNoteMutation,
  SheetDeleteNoteCommand,
  SheetToggleNotePopupCommand,
  SheetUpdateNoteCommand,
  SheetsCellContentController,
  SheetsNote,
  SheetsNoteModel,
  SheetsNotePopupController,
  SheetsNotePopupService,
  SheetsNoteResourceController,
  ToggleNotePopupMutation,
  UniverSheetsNotePlugin,
  m as UniverSheetsNotePreset,
  UniverSheetsNoteUIPlugin,
  UpdateNoteMutation,
  UpdateNotePositionMutation
};
//# sourceMappingURL=@univerjs_preset-sheets-note.js.map
