import type { IRange } from '@univerjs/core';
import { ObjectMatrix } from '@univerjs/core';
export declare function getSparklinePlacement(sourceRanges: IRange[], targetRanges: IRange[]): ObjectMatrix<IRange>;
