import type { IAccessor } from '@univerjs/core';
import { CommandType } from '@univerjs/core';
export declare const OpenSparklinePanelOperation: {
    id: string;
    type: CommandType;
    handler: (accessor: IAccessor) => boolean;
};
