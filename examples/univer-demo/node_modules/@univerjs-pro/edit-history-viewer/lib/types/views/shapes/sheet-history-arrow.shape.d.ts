import type { IMouseEvent, IPointerEvent, IShapeProps } from '@univerjs/engine-render';
import { Shape } from '@univerjs/engine-render';
export declare const SHEET_HISTORY_ARROW_ZINDEX = 1;
export declare const SHEET_HISTORY_ARROW_BORDER_WIDTH = 4;
export declare const ARROW_HEAD_SIZE = 12;
export declare const ARROW_LABEL_HEIGHT = 20;
export declare const ARROW_LABEL_MAX_WIDTH = 200;
export declare const ARROW_LABEL_TEXT_PADDING_LR = 4;
export declare const ARROW_LABEL_TEXT_PADDING_TB = 5;
export interface ISheetHistoryArrowShapeProps extends IShapeProps {
    /** Color of the arrow */
    color: string;
    /** Direction of the arrow: 'horizontal' or 'vertical' */
    direction: 'horizontal' | 'vertical';
    /** Whether to highlight the arrow with blinking effect */
    highlight?: boolean;
    /** Duration in seconds for the highlight blinking effect */
    highlightSecond?: number;
    /** Display name for the arrow */
    name?: string;
    /** Whether the arrow is hovered */
    hovered?: boolean;
    /** Label position */
    labelPosition?: 'bottom' | 'top';
    /** Whether to always show the text label (not just on hover) */
    showText?: boolean;
}
/**
 * Render an arrow shape to indicate row height or column width changes.
 * The shape consists of a rectangle with arrow heads on both ends.
 */
export declare class SheetHistoryArrowShape<T extends ISheetHistoryArrowShapeProps = ISheetHistoryArrowShapeProps> extends Shape<T> {
    private _color;
    private _direction;
    private _highlight?;
    private _highlightSecond?;
    private _blinkTimer?;
    private _blinkIntervalTimer?;
    private _isBlinkVisible;
    private _name;
    private _hovered;
    private _labelPosition;
    private _showText?;
    constructor(key?: string, props?: T);
    setShapeProps(props: Partial<ISheetHistoryArrowShapeProps>): void;
    private _startBlinking;
    private _stopBlinking;
    triggerDblclick(evt: IPointerEvent | IMouseEvent): boolean;
    dispose(): void;
    protected _draw(ctx: CanvasRenderingContext2D): void;
    private _drawHorizontalArrow;
    private _drawVerticalArrow;
    private _drawTextBubble;
    private _getLighterColor;
}
