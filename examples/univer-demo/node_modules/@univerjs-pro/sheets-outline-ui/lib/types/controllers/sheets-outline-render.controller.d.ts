import type { IDimensionOutline } from '@univerjs-pro/sheets-outline';
import type { IRange, Workbook } from '@univerjs/core';
import type { IRenderContext, IRenderModule } from '@univerjs/engine-render';
import type { ISetSelectionsOperationParams } from '@univerjs/sheets';
import { DimensionOutlineAxis, SheetsOutlineModel } from '@univerjs-pro/sheets-outline';
import { Disposable, ICommandService, ThemeService } from '@univerjs/core';
import { IRenderManagerService } from '@univerjs/engine-render';
import { HeaderUnhideRangeService, SheetSkeletonManagerService } from '@univerjs/sheets-ui';
export declare class SheetsOutlineRenderController extends Disposable implements IRenderModule {
    private readonly _context;
    private readonly _sheetsOutlineModel;
    private readonly _sheetSkeletonManagerService;
    private readonly _renderManagerService;
    private readonly _commandService;
    private readonly _themeService;
    private readonly _headerUnhideRangeService?;
    private readonly _rowOutlineShape;
    private readonly _columnOutlineShape;
    private readonly _headerBaseSize;
    private _syncingHeaderSize;
    constructor(_context: IRenderContext<Workbook>, _sheetsOutlineModel: SheetsOutlineModel, _sheetSkeletonManagerService: SheetSkeletonManagerService, _renderManagerService: IRenderManagerService, _commandService: ICommandService, _themeService: ThemeService, _headerUnhideRangeService?: HeaderUnhideRangeService | undefined);
    private _initHeaderUnhideRangeRules;
    private _initOutlineShapes;
    private _initRefresh;
    private _refreshCurrentSheet;
    private _syncOutlineHeaderSize;
    private _getHeaderBaseSize;
    private _handleOutlinePointerMove;
    private _handleOutlinePointerLeave;
    private _handleOutlinePointerDown;
    private _handleOutlineLineDown;
    private _handleOutlineLevelButtonDown;
    private _getOutlineLocalPoint;
    private _getOutlineLevelLocalPoint;
}
export declare function shouldRenderRowUnhideIconForGroups(groups: IDimensionOutline[], hiddenRange: {
    startRow: number;
    endRow: number;
}): boolean;
export declare function shouldRenderColumnUnhideIconForGroups(groups: IDimensionOutline[], hiddenRange: {
    startColumn: number;
    endColumn: number;
}): boolean;
export declare function shouldRenderHeaderUnhideIconForGroups(groups: IDimensionOutline[], axis: DimensionOutlineAxis, hiddenRange: Partial<Pick<IRange, 'startRow' | 'endRow' | 'startColumn' | 'endColumn'>>): boolean;
export declare function getDimensionOutlineLineSelectionParams(unitId: string, subUnitId: string, group: Pick<IDimensionOutline, 'axis' | 'start' | 'end'>, rowCount: number, columnCount: number): ISetSelectionsOperationParams;
