import type { SheetsOutlineModel } from '@univerjs-pro/sheets-outline';
import type { ThemeService } from '@univerjs/core';
import type { IShapeProps, SpreadsheetSkeleton, UniverRenderingContext } from '@univerjs/engine-render';
import type { IOutlineButtonHit, IOutlineLevelButtonHit } from './outline-view-model';
import { DimensionOutlineAxis } from '@univerjs-pro/sheets-outline';
import { Shape } from '@univerjs/engine-render';
interface IOutlineButtonStyle {
    fill: string;
    stroke: string;
    text: string;
}
export declare class DimensionOutlineShape extends Shape<IShapeProps> {
    private readonly _axis;
    private readonly _sheetsOutlineModel;
    private readonly _themeService;
    private readonly _getSkeleton;
    private _buttons;
    private _levelButtons;
    private _lines;
    private _hoveredOutlineId;
    private _hoveredLevel;
    constructor(key: string, _axis: DimensionOutlineAxis, _sheetsOutlineModel: SheetsOutlineModel, _themeService: ThemeService, _getSkeleton: () => SpreadsheetSkeleton | null | undefined);
    refreshBounds(): void;
    setHoveredOutline(outlineId: string | null): void;
    setHoveredLevel(level: number | null): void;
    hitTestButton(x: number, y: number): IOutlineButtonHit | null;
    hitTestLevelButton(x: number, y: number): IOutlineLevelButtonHit | null;
    hitTestOutline(x: number, y: number): string | null;
    hitTestLine(x: number, y: number): string | null;
    protected _draw(ctx: UniverRenderingContext): void;
    private _drawRows;
    private _drawColumns;
    private _drawLevelButtons;
    private _drawButton;
    private _getHoverColor;
    private _getHoverBackgroundColor;
}
export declare function getOutlineButtonStyle(collapsed: boolean, hovered: boolean, themeService: ThemeService): IOutlineButtonStyle;
export declare function getOutlineButtonTopLeft(accumulation: number[], anchorIndex: number, total: number, buttonSize: number): number;
export declare function getOutlineCornerPosition(accumulation: number[], startIndex: number, _endIndex: number, total: number): number;
export {};
