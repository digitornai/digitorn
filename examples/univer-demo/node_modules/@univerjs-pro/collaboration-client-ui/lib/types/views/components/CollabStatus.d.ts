import type { Observable } from 'rxjs';
import { CollaborationStatus } from '@univerjs-pro/collaboration-client';
export interface IOnlineStatusProps {
    /** The collaboration state of the currently focused unit. */
    status$: Observable<CollaborationStatus>;
}
/**
 * A component to display collaboration status to users.
 *
 * @description
 * - Displays only an icon by default to minimize visual distraction
 * - Full status information available via tooltip on hover
 * - Fully accessible with proper ARIA attributes
 * - Interactive when disconnected (click to reconnect)
 *
 * @example
 * ```tsx
 * <CollaborationStatusDisplay status$={statusObservable} />
 * ```
 */
export declare function CollaborationStatusDisplay(props: Readonly<IOnlineStatusProps>): import("react/jsx-runtime").JSX.Element;
