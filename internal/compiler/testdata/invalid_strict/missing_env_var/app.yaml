schema_version: 2

app:
  app_id: chat
  name: Chat
  version: "0.1.0"

agents:
  - id: main
    role: worker
    brain:
      provider: anthropic
      model: claude-sonnet-4-6
      config:
        api_key: "{{env.THIS_ENV_VAR_DOES_NOT_EXIST_DIGITORN_STRICT_TEST}}"
    system_prompt: "hi"
